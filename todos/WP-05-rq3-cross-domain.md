# WP-05: RQ3 Experiments — Cross-Domain Generalization

**Owner:** Ali (run) + Baran (analysis)
**Depends-on:** [WP-01d, WP-02 (D1+D2+D3 ready), WP-03, WP-04, WP-07]
**Effort:** M
**Status:** BLOCKED on WP-04 (winner config must be known)
**Addresses instructor feedback:** [Hoca-1] (fills Table 8)

## Goal

Run the WP-03+WP-04 winning config (best pipeline × best model) on all three domains (D1, D2, D3). Produce Table 8 cells + per-domain failure-mode analysis. The paper already self-defends "3 domains is a probe, not a benchmark" framing — we keep that.

## Acceptance criteria

- [ ] `runs/rq3/<domain>_<run>/manifest.json` for all 15 runs (3 domains × 5 runs).
- [ ] Judge verdicts (WP-07) for all 15 runs.
- [ ] `LaTeX_DL_468198_240419/tables/rq3.tex` rendered with violations-by-Judge, TP, P, R/F1 × 3 domains.
- [ ] §7 analysis paragraph (line 705 AUTHOR-TODO): hardest domain + reason, common failure modes per domain, framework-level weakness if any (a violation type that degrades across all 3 domains).
- [ ] §7 summary (line 709): generalization commentary; explicit "3 domains is a probe, not a benchmark".
- [ ] Per-violation-type per-domain breakdown in replication package.

## Implementation steps

1. Verify D2/D3 pipelines from WP-02 actually run end-to-end on the winning config (chunking, AST parse, RAG indexing).
2. Run `make rq3` on the winning config × {D1, D2, D3} × 5 runs.
3. Judge verdicts + audit.
4. Build Table 8.
5. Inspect failure modes per domain: which violation types fail most, why (vocabulary overlap? context tightness? SRS quality?).
6. Write §7 analysis + summary.
7. Commit "WP-05 RQ3 done".

## Outputs (file paths)

- `runs/rq3/`
- `judge_verdicts/rq3/`
- `audit/rq3/`
- `LaTeX_DL_468198_240419/tables/rq3.tex`
- `paper.tex` §7 analysis + summary
- `replication_package/rq3_per_type_per_domain.csv`
- `docs/RQ3_RUNBOOK.md`

## Risks & mitigations

- **Risk:** One domain has *much* lower F1 (e.g., 0.4 vs 0.7 in others) — looks like a generalization failure. **Mitigation:** Frame honestly. Identify root cause (small SRS, vocabulary overlap with another context, weak Scout output for that domain). Section 9.3.5 already names "domain count" as an external-validity threat.
- **Risk:** WP-02 D2/D3 codebases were generated by an LLM and contain bugs unrelated to DDD. **Mitigation:** Light cleanup pass before runs (already in WP-02 acceptance criteria); separately, the Judge classifies framework reports as TP/FP — synthetic-bug noise doesn't enter the precision/recall numbers because the Judge audits each report.
