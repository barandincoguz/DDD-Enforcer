# Handoff — Iterations 43-46 + WP-CORE-28/32 specs drafted

**Written:** 2026-05-24 15:30 GMT+3
**Reason:** Session-end before `/clear`. Pyright tightening
complete, F-8 security follow-up closed, 10-item Minor sweep
shipped, WP-CORE-28 + 32 specs drafted via brainstorming.

---

## 1. Quick-start for next session

1. **Read this file first** (you are reading it).
2. Read `.planning/pipeline_audit/CURRENT.md` (status pointer).
3. Read `.planning/pipeline_audit/NEXT-WORK-PLAN.md` (ranked
   backlog with full per-WP detail).
4. Read `todos/WP-CORE-28-extension-ux-wave1.md` (next WP spec).
5. Read `todos/WP-CORE-32-extension-webviews.md` (follow-on WP).
6. Confirm baseline: `cd extension/backend && pytest -m "not integration" -q`
   MUST report `729 passed, 31 deselected`.
7. Confirm HEAD: `git log --oneline -1` → `ed13079`.
8. Confirm full pyright clean: `pyright` → 0 errors. CI gate is
   blocking now (continue-on-error dropped iter 44).
9. **Pick next iter** per NEXT-WORK-PLAN.md rank order, OR per
   the after-clear prompt.

---

## 2. Session totals (iterations 43-46 + planning)

- **4 ship iterations** (43 main.py pyright, 44 full pyright +
  gate drop, 45 F-8 XXE close-out, 46 Minor sweep × 6 commits).
- **WP-01d CANCELLED** per user 2026-05-24.
- **WP-CORE-28 + WP-CORE-32 specs drafted** via AskUserQuestion
  brainstorming (4 questions each, all answered).
- **16 commits this multi-turn session** (12 ship + 4 planning).
- **Baseline 716 → 729** (+13 tests net, zero regression).
- **Full repo pyright = 0 errors** (was 132 at iter 42).
- **CI gate** dropped `continue-on-error: true` for pyright.
- **F-8 (XXE) + 10 Minor deferred items**: ALL CLOSED.
- **NOT pushed**. 40 commits ahead of origin/main.

---

## 3. WPs shipped this session — table

| Iter | WP | Commits | Title | Δ tests |
|------|----|---------|-------|---------|
| 43 | Pyright tightening (main.py) | `7a5de0e`+`cc79e68` | 7 main.py type errors + .venv config | 0 |
| 44 | Pyright tightening (full repo + flag drop) | `99285e0`+`39dc89e` | 9 prod errors fixed, tests/ excluded, `continue-on-error` removed | 0 |
| - | WP-01d cancellation | `c4bd374` | Drop from backlog; re-rank NEXT-WORK-PLAN | 0 |
| 45 | F-8 XXE close-out | `9667e40` | pypdf floor + startup probe + 50MB cap + assessment doc | +8 |
| 46.A | Severity + edge | `ce7853b` | 3 fixes (1 dropped after investigation) | 0 |
| 46.B.1 | DRY: atomic write | `ee80021` | `io_atomic.write_text_atomic` helper | 0 |
| 46.B.2 | DRY: run_id sanitize | `998244b` | `sanitize_path_segment` shared helper | 0 |
| 46.B.3 | DRY: parse_target_ctx | `c1cc251` | Delegates to `_issue_stage` | 0 |
| 46.B.4 | schema_version guard | `85cd4d0` | Pydantic `field_validator` + `SUPPORTED_SCHEMA_VERSIONS` | +3 |
| 46.C | pipeline=None grouping | `a25e93c` | 2 coverage tests | +2 |
| - | CURRENT.md update | `8e44dfa`+`ee16aa2` | Iter 45+46 sweep documentation | 0 |
| - | WP-CORE-28 + 32 specs | `ed13079` | 2 spec docs from brainstorm | 0 |

**Cumulative (multi-day):** 348 → 729 (+381 tests, ~95 commits).

---

## 4. WP-CORE-28 spec — locked design (next WP)

`todos/WP-CORE-28-extension-ux-wave1.md` — Extension UX wave 1.

**Scope-defining brainstorm answers (user 2026-05-24):**
- 4 pain points (all selected): API key onboarding, status bar
  progress, validation peek/hover, backend lifecycle resilience.
- Refactor scope: **NONE** (just add features to monolith).
- Backend coupling: **NONE** (extension-only UX).
- Manual smoke: per-iter F5 with feedback loop.

**Iteration order (ROI + risk):**

| Iter | Feature | Effort | Smoke |
|------|---------|--------|-------|
| 47 | API key onboarding | ~2h | Try invalid key, migration flow |
| 48 | Backend lifecycle resilience | ~2h | `kill -9` backend, watch dialog |
| 49 | Multi-stage status bar progress | ~2-3h | Run generate-model on real SRS |
| 50 | Validation peek (HoverProvider) | ~3h | Validate file, hover on diagnostic |

**Total: ~10h across 4 iterations.**

---

## 5. WP-CORE-32 spec — locked design (after WP-CORE-28)

`todos/WP-CORE-32-extension-webviews.md` — PaperRunManifest
viewer.

**Scope-defining brainstorm answers:**
- MVP surface: **Per-run PaperRunManifest viewer** (NOT violations
  dashboard, NOT metrics charts, NOT cost panel — those are
  future WPs).
- Tech stack: **Vanilla HTML + JS** (no Vue, React, or
  webview-ui-toolkit).
- Data flow: **File-load from disk** (workspace `runs/`).
- Sequence: **After WP-CORE-28**.

**5-step implementation** (single iter, ~3-5h):
1. Extension command + WebviewPanel scaffold (~1h)
2. List view rendering (~1h)
3. Detail pane (~1h)
4. Refresh + file watcher (~30min)
5. Tests + F5 smoke (~30min)

---

## 6. NEXT-WORK-PLAN.md — current rank

(See `.planning/pipeline_audit/NEXT-WORK-PLAN.md` for full
content.)

| Rank | WP | Status |
|------|----|--------|
| 1 | F-8 XXE close-out | ✅ SHIPPED iter 45 |
| 2 | Minor deferred sweep | ✅ SHIPPED iter 46 |
| 3 | VerifierResult dual-type unification | TBD (4-6h SDD; no longer blocked) |
| 4 | Tests/ pyright re-enable | TBD (3-5h; cosmetic) |
| 5 | **WP-CORE-28 Extension UX wave 1** | **SPEC READY → next** (iter 47-50) |
| 6 | **WP-CORE-32 PaperRunManifest viewer** | **SPEC READY → after 28** |
| 7 | paper.tex `\input{rqN.tex}` integration | Human-coordinator task |

**~~WP-01d (P1/P2/P3 pipelines)~~** — CANCELLED 2026-05-24 per
user.

---

## 7. Git state at handoff

```
HEAD: ed13079  docs(planning): WP-CORE-28 + 32 specs from brainstorming session
Branch: main
Ahead of origin/main: 40 commits (NOT pushed)
Working tree: clean except runtime artifacts (intermediate JSONs,
              ast_signals_diagnostics.json, validation_metrics_report.json)
```

Recent commits (this session):
```
ed13079 docs(planning): WP-CORE-28 + 32 specs from brainstorming session
ee16aa2 chore(planning): CURRENT.md baseline metadata catch-up
8e44dfa chore(planning): CURRENT.md update for iter 45 + 46 (F-8 + Minor sweep complete)
a25e93c test(aggregate): iter 46 commit C — pipeline=None grouping coverage
85cd4d0 feat(aggregate): iter 46 commit B.4 — schema_version writer guard
c1cc251 refactor(architect): iter 46 commit B.3 — _parse_target_ctx delegates to _issue_stage
998244b refactor(run_id): iter 46 commit B.2 — share sanitize_path_segment helper
ee80021 refactor(io): iter 46 commit B.1 — extract write_text_atomic helper
ce7853b fix(severity, edge): iter 46 commit A — 3 deferred quality fixes
9667e40 fix(security): F-8 XXE close-out — defense-in-depth (iter 45)
c4bd374 docs(planning): cancel WP-01d + re-rank backlog
8e3be78 docs(planning): NEXT-WORK-PLAN.md ranked queue from 5-subagent audit
cc79e68 chore(planning): CURRENT.md update for iter 43 (Pyright partial scope)
7a5de0e fix(types): pyright tightening — main.py 7 type errors + venv config
99285e0 fix(types): pyright tightening complete — drop CI gate
39dc89e chore(planning): CURRENT.md update for iter 44 (Pyright tightening complete)
```

---

## 8. Memory state at handoff

`/Users/barandincoguz/.claude/projects/-Users-barandincoguz-Desktop-AI---NLP-DDD-Enforcer/memory/MEMORY.md`:

```
- [User role and Turkish communication]
- [DDD-Enforcer EMSE submission — locked scope]
- [Workflow: plan→feedback→revised plan loop]
- [Planning artifacts pointer]
- [Credentials setup]
- [Accuracy over cost]
- [Ownership disestablished]
- [SDD default for cross-stage WPs]
- [Phase 1 (WP-01 chain) complete] (WP-01d updated to CANCELLED)
- [Pyright tightening COMPLETE] (iter 44 closure)
```

10 entries. Updated this session: `project_wp01_complete`
(WP-01d cancellation), `project_pyright_status` (iter-44 closure
status).

---

## 9. Locked invariants this session added or reinforced

| ID | Invariant |
|----|-----------|
| Iter 43-44 | `pyright` (full repo) MUST report 0 errors. Tests excluded via `pyrightconfig.json:exclude → extension/backend/tests`. CI `continue-on-error: true` is REMOVED — type errors block merges. |
| Iter 44 | `core/orchestration/pipeline.py:355-357` carries a `cast(...)` bridge between `core.verifier.types.VerifierResult` (dataclass) and `core.pipeline_contracts.VerifierResult` (Pydantic). Single-type refactor is rank-3 follow-up (NEXT-WORK-PLAN). |
| Iter 45 | F-8 XXE: `assert_xxe_safe_parsers()` MUST run in FastAPI lifespan. Approach is BEHAVIOR PROBE (parse custom-entity payload), not attribute read (lxml C-ext makes attribute path infeasible). `SRSDocumentParser.parse_file` enforces `DEFAULT_MAX_SRS_BYTES=50MB` cap before any third-party parser is invoked. `OversizedSRSDocumentError` subclasses `ValueError`. |
| Iter 46.A | `core/architect.py:_to_legacy_issue` MUST log loud (`[WARN] _to_legacy_issue: unknown severity ...`) when severity is not "ERROR" / "WARN". `_specialist_with_feedback` MUST log when result length differs from `arch.contexts` length (invariant violation). |
| Iter 46.B.1 | `core/io_atomic.py:write_text_atomic` is the single source of truth for `tmp + fsync + os.replace`. Three callers (run_manifest, aggregate, latex_tables) delegate. Test patches that target `core.X.os.replace` continue to work because `os` is a shared module reference. |
| Iter 46.B.2 | `core/run_manifest.py:sanitize_path_segment(s)` is the shared regex helper for filesystem-safe slugs. `compose_run_id` and `compose_aggregate_key` use it. Pre-compiled regex `_RUN_ID_UNSAFE_CHARS = re.compile(r'[./\s:<>"|?*\\]')`. |
| Iter 46.B.4 | `AggregatedConfiguration.schema_version` field_validator enforces `SUPPORTED_SCHEMA_VERSIONS = frozenset({"1.0"})`. Migration contract: add to frozenset when bumping; remove old entry only after all artifacts migrated. |
| WP-01d | CANCELLED 2026-05-24 per user. Do NOT propose, plan, or start it. The `PaperRunManifest.pipeline: Optional[Literal["P1","P2","P3"]]` field stays on the schema (no migration cost) but only the current `DomainArchitect` chain will produce RQ1 data. |
| WP-CORE-28 spec | Scope LOCKED: 4 pain points, NO refactor, NO backend changes, per-iter F5 smoke. Future implementer must not add backend coupling without re-asking the user. |
| WP-CORE-32 spec | Scope LOCKED: per-run manifest viewer ONLY (no violations dashboard, no charts, no cost panel — those are future WPs). Vanilla HTML+JS only. File-load only. AFTER WP-CORE-28. |

---

## 10. Operational rules carried forward

- Caveman mode ACTIVE (full). Drop articles / filler /
  pleasantries. Code/commits/security: write normal.
- Communication Turkish (per user_role memory); code/commit
  messages English.
- Accuracy > cost. NO model-tier downgrades.
- **Ownership disestablished.** Any agent picks up any WP.
- NO `git push` without explicit "push it".
- NO `--no-verify`.
- TDD strict: RED → GREEN → DOC → COMMIT (when applicable).
- **SDD default** for cross-stage / multi-file / Codex-REQUIRE
  WPs. Surgical 1-2-file fixes can be direct.
- Atomic commits: one logical change per commit; conventional-
  commits trailer + `Co-Authored-By: Claude Opus 4.7 (1M context)`.
- Per-iter F5 smoke for WP-CORE-28 features (manual, between
  iterations).
- Full repo pyright (no path arg) must pass before commit.
  `.venv/` is excluded; `extension/backend/legacy_pre_emse/` is
  excluded; `extension/backend/tests/` is excluded.

---

## 11. Files NOT to touch

- `LaTeX_DL_468198_240419/paper.tex` — shared writing space;
  human paper-coordinator only.
- `todos/WP-09-*.md`, `todos/WP-18-*.md` — DROPPED (RQ5).
- `todos/WP-01d-*.md` — CANCELLED (do NOT execute the spec).
- `extension/backend/core/intermediate/*.json` (current-session
  writes) + `validation_metrics_report.json` — runtime artifacts;
  refresh in dedicated `chore(artifacts):` commits.
- `extension/backend/.env` — never committed.
- `core/llm_client.py` — REMOVED in WP-01a; do NOT re-introduce.
- `core/llm/gemini.py:_RUNTIME_FALLBACKS` — must stay empty.

---

## 12. Loop ritual for next session

1. Read this handoff (you are here).
2. Confirm baseline 729. Confirm HEAD `ed13079`.
3. Confirm caveman mode active. Confirm full pyright = 0.
4. Pick WP per NEXT-WORK-PLAN.md OR per the after-clear prompt.
5. **Default if user says "devam":** start WP-CORE-28 iter 47
   (Feature 1: API key onboarding). Spec at
   `todos/WP-CORE-28-extension-ux-wave1.md`.
6. For each WP-CORE-28 iter: use `superpowers:writing-plans`
   skill to produce a plan, then `superpowers:subagent-driven-development`
   to execute. F5 smoke between iters.
7. WP-CORE-32 follows WP-CORE-28 with the same skill chain.
8. NO `git push` without explicit "push it".
9. Update CURRENT.md after each iter ships.

End of handoff.
