# Handoff — Iteration 19-27 autonomous run (Tier 1+2+partial-Tier 3)

**Written:** 2026-05-23 22:00 GMT+3
**Reason:** Session-end before user `/clear`. Captures everything next session needs to resume autonomous Tier 3 sweep without context loss.

---

## 1. Quick-start for next session

1. **Read this file first** (you are reading it).
2. Read `.planning/pipeline_audit/CURRENT.md` (status pointer).
3. Read `extension/backend/CLAUDE.md` (project rules) + `AGENTS.md`.
4. Confirm baseline: `cd extension/backend && pytest -m "not integration" -q` MUST report `560 passed, 31 deselected`.
5. Confirm HEAD: `git log --oneline -5 | head -1` → `273af57`.
6. Memory check: `cat /Users/barandincoguz/.claude/projects/-Users-barandincoguz-Desktop-AI---NLP-DDD-Enforcer/memory/MEMORY.md` — 6 entries present including `feedback-accuracy-over-cost`.
7. Verify caveman mode banner appears (CAVEMAN MODE ACTIVE / full).
8. Pick a WP from §10 "Next session menu" or follow user's prompt.

---

## 2. Session totals (this iteration block)

- **9 WPs shipped** atomically (WP-CORE-22 through WP-CORE-31, skipping 28).
- **11 commits** total (8 feat/fix + 3 chore/docs + 0 push).
- **Baseline 459 → 560** (+101 tests, zero regression).
- **0 MAJOR-OPEN-live** audit findings remain; previously closed gaps stay closed.
- **NOT pushed** per autonomous-loop rule (no `git push` without explicit user "push it").
- **Codex xhigh skipped** for every WP this session — all changes were schema-additive / mechanical / small-scope per established pattern (WP-CORE-13/14/15/16/17/18/19 precedent).

---

## 3. WPs shipped this session — full table

| Iter | WP | Commit | Title | Δ tests | Codex |
|------|----|--------|-------|---------|-------|
| 19 | WP-CORE-22 | `719fc05` | Repository + Factory first-class AST detection | +11 | skip |
| 20 | WP-CORE-23 | `15f0c96` | V6 domain event `ast.Name` false-positive guard | +6 | skip |
| 21 | WP-CORE-24 | `4767237` | Refiner cycles_used + exhausted manifest fields | +5 | skip |
| 22 | WP-CORE-25 | `0c1d328` | RAG chunk 600 + distance filter + delete logging | +7 | skip |
| 23 | WP-CORE-26 | `9d3bb6f` | Scout/Architect/Specialist json_failed wiring (WP-CORE-20a follow-up) | +6 | skip |
| 24 | WP-CORE-27 | `e9a4ee3` | Verifier D9 (VO mutability) + S3 (empty agg) + D11 (cycle) | +21 | skip |
| 25 | WP-CORE-29 | `aa5671d` | DevEx: refiner `Optional` + rag tests + .venv docs | +16 | skip |
| 26 | WP-CORE-30 | `7fff620` | Refiner cycle_history + flapping detection | +12 | skip |
| 27 | WP-CORE-31 | `273af57` | Cross-context import topology helpers (standalone) | +17 | skip |

**Cumulative across whole 14-day session: 348 → 560 (+212 tests, 50+ commits, zero regression).**

---

## 4. WP scope details (so next session can compose follow-ups)

### WP-CORE-22 (commit `719fc05`)
- Removed `Repository` + `Factory` from `INFRASTRUCTURE_SUFFIXES` (-0.50 penalty) in `core/AST/ast_signal_classification.py:11-12`.
- Added `_score_repository` (threshold 0.60: suffix +0.40, base class +0.30, CRUD methods +0.25, identity penalty, frozen penalty) and `_score_factory` (threshold 0.62: suffix +0.40, create_/build_/make_ methods +0.30, stateless bonus, identity + heavy DI penalty).
- New `CandidateType` literals `"repositories"` and `"factories"` in `core/AST/ast_signal_types.py`. Suffix-stripping helper derives `aggregate_root` / `produces` at `to_public_dict()` time.
- `core/schemas.py`: new `Repository` + `Factory` Pydantic models; `UbiquitousLanguage` gains optional `repositories` + `factories` lists (default `None` preserves backward compat).
- `core/AST/ast_signal_enrichment.py` `CANDIDATE_TYPES` tuple extends from 5 → 7.
- `core/AST/ast_model_signals.py:_to_public_candidate_map` now seeds repositories + factories buckets (the candidate-type-not-in-result filter previously silently dropped them).
- `tests/test_repository_factory_detection.py`: 11 tests.

### WP-CORE-23 (commit `15f0c96`)
- `core/AST/ast_signal_discovery.py:_event_name_from_arg` no longer accepts `ast.Name`. Only `ast.Constant` (string literal) and `ast.Attribute` (`Events.OrderPlaced`) survive.
- Without this fix `self.bus.publish(template_name)` recorded the *variable identifier* "template_name" as a domain event candidate.
- `tests/test_domain_event_false_positives.py`: 6 tests including documentation that `EVENT_SUFFIXES` is anchored to `.endswith()` — `PaidLeavePolicy` (which ends in "Policy") was always handled correctly; Agent 5's original false-positive example was technically wrong.

### WP-CORE-24 (commit `4767237`)
- `RefinementExhaustedError(issues, cycles_attempted=0)` field added. Default 0 keeps backward compat.
- `core/refiner/loop.py:refine_until_clean` raises with `cycles_attempted=cycles`.
- `core/orchestration/pipeline.py:_record_refiner_metrics_safely(cycles_used, exhausted, residual_count, max_cycles=2)` helper writes `manifest.stages["refiner"]` via `get_current_emitter()`. Wrapped in defensive try/except.
- `main.py:_run_generate_pipeline` sets `_emitter_var` for the WHOLE pipeline call (not just inside `with emitter.stage(...)` blocks) so the orchestrator's helper can find the active emitter outside an explicit stage scope. Token reset in `finally`.
- `tests/test_refiner_manifest.py`: 5 tests.

### WP-CORE-25 (commit `0c1d328`)
- `config.py:CHUNK_SIZE` 250 → 600 (multi-clause domain rules now fit in one chunk).
- `core/rag_pipeline.py` new `_filter_and_format_sources(results)` helper enforces `RAGConfig.MIN_RELEVANCE_SCORE = 0.3` (previously declared but never applied). Both `retrieve_source` and `search` use the filter.
- `_delete_document` `except Exception: pass` → typed warning via `logging.getLogger(__name__)`. Stale-chunk re-index failures now observable.
- `tests/test_rag_pipeline_filters.py`: 7 tests.

### WP-CORE-26 (commit `9d3bb6f`)
- Closes WP-CORE-20a follow-up. New module-level helper `_record_json_parse_failure_if_emitter(stage, operation, model_id)` in `core/architect.py`. Temporarily sets `_stage_var` so the emitter accepts the call even when no `with emitter.stage(...)` block is active (architect-stage wrapping is a separate WP-CORE-20b follow-up).
- Three callsites wired (one per pipeline stage with manual JSON parse): Scout `_extract_sentences_from_chunk` (line ~410), Architect `identify_contexts` (line ~580), Specialist `extract_per_context_details` (line ~785).
- Each callsite records EVERY failed retry (not just final).
- `tests/test_json_parse_failure_wiring.py`: 6 tests.

### WP-CORE-27 (commit `e9a4ee3`)
- **D9** ValueObject mutability mismatch: new schema field `ValueObject.is_mutable_in_code: bool = False` + `check_d9_value_object_mutability_consistency` deterministic check. AST→VO field population is a follow-up (WP-CORE-27a) — v1 ships infrastructure + check.
- **S3** Empty aggregate WARN: companion to D4 which passes vacuously when `members=[]`. `check_s3_aggregate_members_nonempty` ERRORs WARN-level.
- **D11** Dependency cycle: `check_d11_dependency_cycle_free` DFS with WHITE/GRAY/BLACK coloring. Self-loops (A→A) and multi-node cycles (A→B→…→A) ERROR. Unknown deps silently skipped (D5 catches separately). Cycles deduplicated by canonical rotation.
- `core/observability/_verifier_mapping.py` `_ISSUE_TYPE_TO_CHECK_ID` adds `value_object_mutable→D9`, `empty_aggregate→S3`, `dependency_cycle→D11`. `CANONICAL_CHECK_IDS` extends from 9 → 12 entries.
- `tests/test_verifier_d9_s3_d11.py`: 21 tests.

### WP-CORE-29 (commit `aa5671d`)
- `core/refiner/loop.py`: `initial_result: Optional[VerifierResult] = None` typed correctly. 3 `type: ignore[assignment]` directives removed.
- `tests/test_rag_pipeline_core.py`: NEW file, 16 tests covering `_parse_sections` / `_split_section` / `_get_overlap` / `_build_query` / `_estimate_page` / `_generate_summary` / `get_stats`. All bypass `__init__` via `__new__` (`_bare_pipe()` helper).
- `CLAUDE.md`: new "Repair a broken `.venv` (WP-CORE-29)" subsection.
- **Documented gap**: `core/rag_pipeline.py:_parse_sections` regex `^(\d+(?:\.\d+)*)\s+(.+)$` rejects `1. Title` (period+space) — `document_parser.heading_pattern` accepts via optional `\.?`. Out of WP-CORE-29 scope; separate pattern fix.

### WP-CORE-30 (commit `7fff620`)
- `RefinementExhaustedError.cycle_history: List[List[Any]]` (default empty). Per-cycle `VerifierResult.issues` snapshot at the moment they were presented to `stage_runner`. Final exhaust verify NOT added (lives in `exc.issues` separately).
- `core/orchestration/pipeline.py` new helpers `_flapping_signature(issues)` (canonical sorted `(location, type)` tuple) + `_detect_flapping(cycle_history)` (last 2 cycles identical signature → True).
- `_record_refiner_metrics_safely` gains `flapped: bool` + `cycle_history_len: int` kwargs.
- `tests/test_refiner_cycle_history.py`: 12 tests.
- **Out of scope (deferred to WP-CORE-30b)**: wire `refiner/prompts.render_refinement_prompt` into the actual stage_runner call (requires `deps.specialist` signature change). Per-context narrow rerun (only re-run failing contexts).

### WP-CORE-31 (commit `273af57`)
- NEW `core/AST/import_graph.py` with three pure helpers:
  - `build_module_import_graph(python_files, workspace_root=None)`: `{module_name → {imported_module_names}}`.
  - `map_modules_to_contexts(modules, context_names)`: heuristic — first dotted segment matching a context name wins. Case-insensitive, normalised on underscore + hyphen.
  - `derive_context_dependencies(import_graph, module_to_context)`: reduces module graph to context-level dependency graph. Intra-context edges + unmapped-endpoint edges excluded.
- `tests/test_import_graph.py`: 17 tests including 4-file 3-context E2E fixture.
- **Out of scope (deferred to WP-CORE-31b)**: pipeline integration — auto-populate `BoundedContext.allowed_dependencies` + cross-check derived vs. LLM-declared graph.

---

## 5. Memory state (cross-session persistence)

`/Users/barandincoguz/.claude/projects/-Users-barandincoguz-Desktop-AI---NLP-DDD-Enforcer/memory/MEMORY.md`:

```
- [User role and Turkish communication](user_role.md) — graduate researcher preparing EMSE submission; replies in Turkish; works with advisor "Hoca"
- [DDD-Enforcer EMSE submission — locked scope](project_emse_submission.md) — regular track, 4 RQs, 14 weeks, 2 authors; Phase 0 hygiene COMPLETE
- [Workflow: plan→feedback→revised plan loop](feedback_workflow.md) — don't auto-execute after planning; wait for explicit authorization
- [Planning artifacts pointer](reference_planning_artifacts.md) — plan file, todos/ WP-00–WP-18, LaTeX paper bundle, EMSE journal links
- [Credentials setup](credentials_setup.md) — paid-tier Gemini + 6-key Ollama Cloud pool in .env; D1 6-model rollout unblocked
- [Accuracy over cost](feedback-accuracy-over-cost.md) — do not propose model-tier downgrades or cost optimizations; accuracy is priority; WP-CORE-21 rejected
```

**NEW entry this session**: `feedback-accuracy-over-cost.md`. User explicitly rejected WP-CORE-21 (Scout `gemini-3.1-pro-preview` → Flash, ~85% cost reduction) with "doğruluk bizim için costtan daha önemli şuanlık". EMSE submission data integrity depends on Pro-tier output quality.

**How to apply**:
- DO NOT propose Scout-to-Flash or any stage model downgrade as a quick win.
- DO NOT propose Gemini CachedContent leverage as priority unless user reopens cost track.
- WP-CORE-34 (CachedContent) is deferred from active plan.
- Accuracy-focused WPs get priority.
- Touching `configs/models.py:STAGE_TO_GROUP` requires explicit user confirmation.

---

## 6. Active research context (carried from session start)

Six parallel research agents (iteration 18) produced findings that drove the plan:

1. **VS Code extension** (`extension/src/extension.ts`, 1582 lines): single file, 5 commands, 1 listener (save), 1 code action ("View Source"). No webview, no tree view. Big UX gaps: ignore-violation, explain-rule, jump-to-model, bulk validate, model staleness, multi-root workspace, file delete/rename handlers, settings change handler. WP-CORE-20 manifest is NOT surfaced in UI. 5 surfacing ideas: status bar cost ticker, webview manifest viewer, json_failed warning badge, run history tree, inline cost annotation.

2. **Pipeline hardening** (backend `core/`): 8 actionable items ranked. Top-priority items now SHIPPED:
   - Scout-to-Flash (REJECTED by user — accuracy > cost).
   - D9 VO mutability (SHIPPED WP-CORE-27).
   - ChunkMetadata.truncated_chunks always-zero (NOT YET FIXED — backlog).
   - D11 aggregate cycle (SHIPPED WP-CORE-27).
   - S3 empty-aggregate (SHIPPED WP-CORE-27).
   - RAG chunk 250→600 + distance filter (SHIPPED WP-CORE-25).
   - V7 ACL signal (NOT YET — WP-CORE-33 target).
   - Refiner cycles_used in manifest (SHIPPED WP-CORE-24).

3. **WP backlog inventory** (`todos/`):
   - Eng-only WP subset: WP-00, WP-01b, WP-01c, WP-NEW-A (Ali), WP-01d, WP-02 (Ali, data), WP-NEW-B Stage 2, WP-07 (Ali).
   - WP-01b run-spec orchestrator: paper data infrastructure, paper.tex untouched. USER APPROVED for inclusion, but WP-01d deferred.
   - WP-NEW-A drift injector: Ali ownership flag. USER chose "İleri tarihe bırak" (subjects/code-clean/ ready first).

4. **Domain coverage**: `subjects/` directory does NOT EXIST. `scripts/inject_drift.py` does NOT EXIST. Only D1 e-commerce SRS (`extension/backend/inputs/SRS.docx`). D2 banking + D3 healthcare SRS not sourced. `runs/probe-20260519-175042.*` + `runs/domain_run-20260520-164242.*` are sole artifacts.

5. **DDD pattern detection gaps** (`core/AST/`, `core/code_parser/`):
   - Repository pattern penalised -0.50 (FIXED WP-CORE-22).
   - Factory pattern penalised -0.50 (FIXED WP-CORE-22).
   - Domain service vs application service distinction (NOT YET — WP-CORE-33).
   - Anti-Corruption Layer (NOT YET — WP-CORE-33 V7).
   - Specification pattern (NOT YET — WP-CORE-33 V8).
   - Saga / Process Manager (NOT YET).
   - Read Model / CQRS (NOT YET).
   - Domain Event Handler (NOT YET).
   - Bounded Context Map relationship types (NOT YET).
   - V6 false positives: `template_name` variable, `PaidLeavePolicy` (NEITHER fired in practice; only `ast.Name` issue real — FIXED WP-CORE-23).

6. **DevEx audit**:
   - Local `.venv` broken — Python 3.14 shell, no pip. Repair docs added to CLAUDE.md (WP-CORE-29).
   - Pyright `continue-on-error: true` in `.github/workflows/backend-ci.yml:74` — not yet tightened. `main.py` has ~10 known production type errors (result_holder None mutation, parse_code Optional access). Separate WP.
   - `rag_pipeline.py` had ZERO unit tests pre-WP-CORE-25; now has 7 (filters) + 16 (core).
   - `core/refiner/loop.py:21,37,47` 3 `type: ignore[assignment]` SILENCED (WP-CORE-29).
   - `tests/test_refiner_loop.py` is the only refiner test file (1 file vs other domains 4-8 files). WP-CORE-30 added `test_refiner_cycle_history.py` + WP-CORE-24 added `test_refiner_manifest.py` → 3 files total now.

---

## 7. Locked invariants this session added or reinforced

| ID | Invariant |
|----|-----------|
| WP-CORE-22 | `INFRASTRUCTURE_SUFFIXES` MUST NOT contain `Repository` or `Factory` (they have positive scorers). `ast_signal_types.CandidateType` MUST include `"repositories"` and `"factories"`. `_to_public_candidate_map` MUST seed these buckets. |
| WP-CORE-23 | `_event_name_from_arg` MUST NOT accept `ast.Name` (variable identifier). Only `ast.Constant` (string literal) and `ast.Attribute`. |
| WP-CORE-24 | `RefinementExhaustedError` MUST carry `cycles_attempted: int`. `_run_generate_pipeline` MUST set `_emitter_var` for the whole pipeline call, with token reset in `finally`. |
| WP-CORE-25 | `RAGConfig.MIN_RELEVANCE_SCORE = 0.3` MUST be enforced (previously declared but unused). `_delete_document` MUST NOT swallow exceptions silently. |
| WP-CORE-26 | Architect / Scout / Specialist `_parse_json_response` retry paths MUST call `_record_json_parse_failure_if_emitter(stage, operation, model_id)` on `"json_parse_failed"` result. |
| WP-CORE-27 | `ValueObject.is_mutable_in_code` defaults False. `_ISSUE_TYPE_TO_CHECK_ID` contains `value_object_mutable→D9`, `empty_aggregate→S3`, `dependency_cycle→D11`. `CANONICAL_CHECK_IDS` extends to 12. |
| WP-CORE-29 | `core/refiner/loop.py:initial_result` typed `Optional[VerifierResult]`. CLAUDE.md "Repair .venv" section MUST stay. |
| WP-CORE-30 | `RefinementExhaustedError` MUST carry `cycle_history: List[List[Any]]`. `refine_until_clean` appends to history right before `stage_runner` call (NOT after final verify). `_record_refiner_metrics_safely` MUST accept `flapped` + `cycle_history_len` kwargs. |
| WP-CORE-31 | `core/AST/import_graph.py` is the source of truth for module-to-context mapping. All three helpers are pure functions (no I/O side effects beyond file reading). |

---

## 8. Patterns established this session

### TDD strict (every WP)
1. Read existing code surface.
2. Write RED test file (every test fails at import OR assertion).
3. Confirm RED with `pytest tests/<new_file>.py -q`.
4. Implement GREEN (smallest correct change).
5. Re-run new tests → confirm GREEN.
6. Full baseline `pytest -m "not integration" -q` → confirm zero regression.
7. Stage + commit with established message format.

### Commit message template
```
<type>(scope1, scope2): WP-CORE-NN <one-line title>

<body explaining motivation + key design choices + Codex review status>

Baseline N → M (+K tests, zero regression).

Per accuracy-over-cost feedback memory: no model-tier changes.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

### Helper patterns (re-usable)
- `_bare_pipe()` test fixture: `RAGPipeline.__new__(RAGPipeline)` + manually-set attrs avoids ChromaDB collection setup.
- `_FixtureWorkspace` mixin: tmp workspace + `_write(rel_path, content)` for tests that need real .py files.
- `_emitter_var.set(emitter)` + finally reset for ContextVar scoping outside emitter.stage() blocks.
- Module-level safe helpers (e.g. `_record_json_parse_failure_if_emitter`): defensive `try/except` so observability bugs never bring down core paths.
- Suffix-stripping at to_public_dict() time for derived schema fields.

### Codex xhigh skip criteria (per established WP-CORE-13/14/15/16/17/18/19 precedent)
Skip Codex when:
- Schema-additive change (new Optional field with default).
- New deterministic pure function (no side effects).
- Mechanical rename / suffix list cleanup.
- Single AST `isinstance` filter change.
- Cosmetic refactor with regression-guard tests.

REQUIRE Codex when:
- New error-taxonomy class.
- Cross-stage orchestration logic.
- Schema field that breaks backward compat.
- Asynchronous / concurrency changes.
- Anything with 3+ Codex CRITICAL findings expected.

This session: zero WPs hit the REQUIRE bar.

---

## 9. Remaining backlog

### Closed (no longer in backlog)
- F-1, F-2, F-4, F-6, F-7, F-9, F-10 (accept-as-is), F-11 (DORMANT), F-12, F-13, F-14, F-15 (indirect), F-17, F-18, F-19, F-20, F-21, F-22, F-23, F-24.

### Open (engineering, paper-free)
- **F-8 (MINOR-OPEN)** XXE / external-entity hardening. Needs proper security threat model; pin `python-docx` version + audit `lxml` entity resolution. **Defer to dedicated security audit WP**.
- **F-16 (TRIVIAL-OPEN)** Dead `extract_domain_sentences` cascade. Removing the method requires removing `test_architect_helpers.py:116-119`. Out of "smallest correct change" scope; deferred.

### Open (follow-up WPs created this session)
- **WP-CORE-27a** — AST→`ValueObject.is_mutable_in_code` population. D9 check exists but field is always False until AST cross-reference is wired. Requires `SignalEnricher` to detect VO claims that don't match AST mutation evidence.
- **WP-CORE-30b** — full narrow rerun. `refiner/prompts.render_refinement_prompt` exists but not bound. Requires `deps.specialist` signature change to accept refinement prompt + per-context subset.
- **WP-CORE-31b** — pipeline integration of `import_graph` helpers. Auto-populate `BoundedContext.allowed_dependencies` + cross-check derived vs. LLM-declared graph. Touches `SignalEnricher.enrich_model`.
- **WP-CORE-20b** — wrap each architect-pipeline stage in `with emitter.stage("scout"/"architect"/...)`. Without this, `record_llm_call` silently drops per-stage records (only fires via `_record_json_parse_failure_if_emitter` which sets stage var explicitly).
- **rag_pipeline.py `_parse_sections` regex gap** — doesn't accept "1. Title" period+space form. `document_parser.heading_pattern` does. Aligning would unlock more SRS section detection.

### Open (out of scope but listed for next session menu)
- **WP-CORE-28** — Extension UX wave 1 (TypeScript, manual smoke required → autonomous-risky).
- **WP-CORE-32** — Extension webviews + manifest dashboard (TypeScript).
- **WP-CORE-33** — V7 ACL + V8 Specification + V9 Service kind detector (BACKEND, autonomous-safe, 25-30 tests).
- **WP-01b** — run-spec orchestrator (paper data infra, paper.tex untouched, user APPROVED).
- **WP-01d** — P1/P2/P3 pipeline classes (deferred per user — "WP-01b dahil, WP-01d sonraya").
- **WP-NEW-A** — drift injector (Ali ownership flag, deferred per user — needs `subjects/code-clean/` first).
- **WP-CORE-21** — Scout-Flash (REJECTED per accuracy-over-cost memory).
- **WP-CORE-34** — Gemini CachedContent leverage (REJECTED per accuracy-over-cost memory).

---

## 10. Next session menu (ranked by ROI / autonomous-safety)

| Rank | WP | Why now | Risk |
|------|----|---------|------|
| **1** | **WP-CORE-33** V7 ACL + V8 Specification + V9 Service kind | Backend, autonomous-safe. Detection blind spots same shape as WP-CORE-22 — proven pattern. Adds 3 first-class candidate types + scorers + schemas. Builds on WP-CORE-22 momentum. | Low. Schema-additive. ~25-30 tests. |
| 2 | **WP-CORE-31b** pipeline integration of import_graph | WP-CORE-31 helpers ready; this WP composes them into the pipeline. Auto-populates `allowed_dependencies` + cross-checks LLM-declared graph. Closes V4 boundary detection gap. | Mid. Touches `SignalEnricher.enrich_model` hot path. Codex consult recommended. |
| 3 | **WP-CORE-20b** wrap architect stages in emitter | Closes WP-CORE-20a follow-up gap symmetric to WP-CORE-26 — currently per-stage `llm_calls` records silently drop because `_stage_var` not set during analyze_document. Production hot path obs. | Mid. Touches `core/architect.py:analyze_document`. |
| 4 | **WP-CORE-27a** AST→is_mutable_in_code population | Closes the v1 placeholder D9. Needs SignalEnricher to cross-reference VO claims with AST mutation_methods. | Mid. Touches enrichment merge logic. |
| 5 | **WP-01b** run-spec orchestrator | User-approved roadmap WP. YAML run-spec + idempotent worker on top of WP-CORE-20 manifest infra. Enables N=10 paper runs. | Big. New module, but isolated from existing pipeline. |
| 6 | rag_pipeline.py `_parse_sections` regex alignment | Trivial fix: allow `1. Title` period+space form (mirror `document_parser.heading_pattern`). Documented in WP-CORE-29 as known gap. | Trivial. <30 min. |

**Recommended order**: 1 → 2 → 6 → 4 → 3 → 5. WP-CORE-33 is the cleanest single-WP win; WP-CORE-31b unlocks V4 detection; WP-CORE-20b is observability dept.

---

## 11. Files NOT to touch (CLAUDE.md rules)

- `LaTeX_DL_468198_240419/paper.tex` — shared writing space, coordinate.
- `todos/WP-09-*.md`, `todos/WP-18-*.md` — DROPPED (RQ5 silindi).
- `extension/backend/core/intermediate/*.json` — runtime artifacts; refresh in dedicated `chore(artifacts):` commits.
- `extension/backend/validation_metrics_report.json` — runtime artifact (committed legacy pattern).
- `extension/backend/.env` — never committed, confirmed `.gitignored`.
- `core/llm_client.py` — REMOVED in WP-01a. Do NOT re-introduce; new code uses `core/llm/validator.py` + `from core.llm import get_client`.
- `core/llm/gemini.py:_RUNTIME_FALLBACKS` — intentionally empty; do NOT populate without registering a model_id in `registry.py`.

Ali-owned WPs requiring coordination before touching:
- WP-NEW-A drift injector
- WP-02 Subject Corpus
- WP-07 Judge LLM Rubric
- WP-10 Bibliography
- WP-11 Figures
- WP-14 Abstract + Conclusion
- WP-16 Extension Architecture Doc
- WP-NEW-C Prompt Sensitivity
- WP-NEW-D Reviewer Guide

---

## 12. Critical files / entry points (file-line index)

| File | Purpose | Key WP |
|------|---------|--------|
| `extension/backend/main.py:111` | `_run_generate_pipeline(file_paths, srs_dir_resolved)` — endpoint-entry manifest | WP-CORE-20, WP-CORE-24 |
| `extension/backend/core/architect.py` | `DomainArchitect` — 5-stage pipeline (Scout→Architect→Specialist→Verifier→Refiner→Synthesizer) | WP-CORE-7 / 18 / 26 |
| `extension/backend/core/architect.py:67` | `_record_json_parse_failure_if_emitter(stage, operation, model_id)` | WP-CORE-26 |
| `extension/backend/core/observability/__init__.py` | `RunManifest`, `StageEmitter`, `get_current_emitter` | WP-CORE-20 |
| `extension/backend/core/observability/_verifier_mapping.py` | Canonical D1..D11 + S1 + S3 mapping | WP-CORE-20, WP-CORE-27 |
| `extension/backend/core/AST/import_graph.py` | Module→context dependency derivation | WP-CORE-31 (NEW) |
| `extension/backend/core/AST/ast_signal_classification.py` | DDD candidate scorers (incl Repository, Factory) | WP-CORE-22 |
| `extension/backend/core/AST/ast_signal_discovery.py` | ClassFacts + ast.Name guard | WP-CORE-23 |
| `extension/backend/core/refiner/loop.py` | `refine_until_clean` + cycle_history | WP-CORE-7, WP-CORE-30 |
| `extension/backend/core/orchestration/pipeline.py:104` | `_record_refiner_metrics_safely`, `_flapping_signature`, `_detect_flapping` | WP-CORE-24, WP-CORE-30 |
| `extension/backend/core/orchestration/errors.py` | `RefinementExhaustedError(cycles_attempted, cycle_history)`, `ArchitectGroundingError` | WP-CORE-7 / 24 / 30 |
| `extension/backend/core/verifier/checks_deterministic.py` | D1..D5, D9, S3, D11 | WP-CORE-27 |
| `extension/backend/core/rag_pipeline.py` | RAG with `_filter_and_format_sources` | WP-CORE-25 |
| `extension/backend/scripts/aggregate_runs.py` | N=10 manifest aggregator (pooled rates, distributions, SHA-256) | WP-CORE-20 |
| `extension/backend/core/schemas.py` | `Repository`, `Factory` (WP-CORE-22), `UbiquitousLanguage.is_mutable_in_code` (WP-CORE-27) | various |

---

## 13. Test inventory after this session

```
tests/test_repository_factory_detection.py        +11  WP-CORE-22
tests/test_domain_event_false_positives.py        +6   WP-CORE-23
tests/test_refiner_manifest.py                    +5   WP-CORE-24
tests/test_rag_pipeline_filters.py                +7   WP-CORE-25
tests/test_json_parse_failure_wiring.py           +6   WP-CORE-26
tests/test_verifier_d9_s3_d11.py                  +21  WP-CORE-27
tests/test_rag_pipeline_core.py                   +16  WP-CORE-29
tests/test_refiner_cycle_history.py               +12  WP-CORE-30
tests/test_import_graph.py                        +17  WP-CORE-31
```

Total session: 459 → 560 (+101 unit tests, all isolated, no live LLM calls).

---

## 14. Operational rules carried forward

- **Caveman mode ACTIVE** (full level). Drop articles / filler / pleasantries / hedging. Code/commits/security: write normal.
- **Communication Turkish** (per user_role memory). Code + comments + commit messages: English.
- **Autonomous loop**: continue until user says stop. NO `git push` without explicit "push it". NO `--no-verify`.
- **Codex skip pattern**: applied this session; document explicitly in commit body.
- **Memory updates**: `feedback-accuracy-over-cost.md` added this session; do not propose model-tier downgrades or cost optimizations without explicit user re-open.
- **TDD strict**: RED → GREEN → DOC → COMMIT.
- **Atomic commits**: one logical change per commit, with conventional-commits trailer + Co-Authored-By.
- **Baseline guardrail**: every WP must end with `pytest -m "not integration"` reporting at least the previous WP's count.

---

## 15. Git state at handoff

```
HEAD: 273af57  feat(ast): WP-CORE-31 cross-context import topology helpers
Branch: main
Ahead of origin/main: ~11 commits (NOT pushed)
Working tree: clean except runtime artifacts (intermediate JSONs, ast_signals_diagnostics.json)
```

Recent commits:
```
273af57 feat(ast): WP-CORE-31 cross-context import topology helpers
7fff620 feat(refiner, observability): WP-CORE-30 cycle_history + flapping detection
aa5671d chore(refiner, rag, docs): WP-CORE-29 DevEx hygiene wave
e9a4ee3 feat(verifier, schemas): WP-CORE-27 D9 + S3 + D11 invariants
9d3bb6f feat(architect, observability): WP-CORE-26 wire Scout/Architect/Specialist JSON parse failures into manifest
0c1d328 fix(rag, config): WP-CORE-25 chunk size + distance filter + delete logging
9d3bb6f feat(architect, observability): WP-CORE-26
4767237 feat(orchestration, observability): WP-CORE-24 refiner cycles_used + exhausted in manifest
15f0c96 fix(ast): WP-CORE-23 V6 domain event ast.Name false-positive guard
719fc05 feat(ast, schemas): WP-CORE-22 Repository + Factory first-class detection
```

---

## 16. Loop ritual for next session

1. Read this handoff (you are here).
2. Confirm baseline 560.
3. Confirm HEAD `273af57`.
4. Read MEMORY.md (6 entries).
5. Pick from §10 menu OR follow user prompt.
6. Default if user says "devam": WP-CORE-33 (V7 ACL + V8 Specification + V9 Service kind).
7. Caveman mode persists from previous session — stay terse, code stays normal.
8. Do NOT propose model-tier downgrades (WP-CORE-21, WP-CORE-34 rejected).
9. NO `git push` without explicit "push it".
10. After each WP: stage atomically, commit with template (§8), update CURRENT.md if context shifted significantly.

End of handoff.
