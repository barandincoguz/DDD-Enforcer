# Handoff — Domain Pipeline Hardening Loop (iteration 5 → 6)

**Written:** 2026-05-22 21:30 GMT+3
**Reason:** Iteration 5 SHIPPED — WP-CORE-6 (F-21 D1 verifier non-vacuous) closed end-to-end. Natural breakpoint before iteration 6.

---

## State of the loop

### Baseline (sacred)

```
pytest -m "not integration" → 348 passed, 31 deselected
Pre-WP-CORE-6 baseline was 338. Net +10 tests, zero regression.
Cumulative: pre-loop 272 → post-iteration-5 348 (+76 tests over 5 iterations).
```

### HEAD at handoff (after DOC + planning commits)

```
{pending}  chore(planning): WP-CORE-6 spec v2 + plan into git history
{pending}  chore(artifacts): WP-CORE-6 dev_doc + audit state update + F-22 backlog
a86bbbb    fix(architect, verifier, orchestration): WP-CORE-6 Architect populates supporting_sentence_ids end-to-end
fd7f203    test(architect, verifier, orchestration): WP-CORE-6 red-phase tests
9608495    chore(planning): WP-CORE-5b spec v2 + plan + abandoned WP-CORE-5 spec   ← pre-iteration-5 HEAD
```

4 commits on main during iteration 5. **NOT pushed.** Loop rule: only push on explicit user "push it".

### Iteration 5 — what actually happened (the bigger-than-expected fix)

1. **Initial pick:** F-21 per Codex W-8 priority bump from WP-CORE-5b. Production-LIVE bug (every project run passes D1 vacuously).
2. **Close-lookup confirmed audit text mostly accurate** — Architect builds `ContextHypothesis` without `supporting_sentence_ids`; D1 check iterates empty list → vacuous pass.
3. **Spec v1** proposed narrow fix: Architect prompt + parsing + signature change.
4. **Codex xhigh review on v1:** **4 CRITICAL + 4 WARN + 6 NIT + 1 OQ.** Big finding — Codex exposed that v1 was scope-insufficient:
   - **A7 (final-model-loss)** — Specialist rebuilds `ContextHypothesis` fresh at architect.py:707, discarding IDs. Final DomainModel still vacuous post-v1.
   - **A2-d3-mask** — Refiner exhausts on D1 ERROR → degrades to best-effort. D-3's ERROR is logged-and-discarded.
   - **A3-integration-gap** — no E2E test.
   - **A5-risk4** — degrade-log discards issue list.
   - **W-1** — truncation chops `[N]` mid-prefix.
   - **W-2** — top-level-list branch bypasses strict-shape.
5. **User decision (Option A):** handle all CRITICAL+WARN inline → spec v2.
6. **Spec v2** widens scope: signature change extends to Specialist (`extract_per_context_details: List[ContextHypothesis]`); new `_truncate_numbered_pairs` helper; strict-shape parser; degrade-log enrichment; F-22 NEW backlog entry for Refiner extension; 10 tests instead of 5.
7. **WP-CORE-6 shipped clean.** RED `fd7f203` (8 RED-by-design failures predicted exactly); GREEN `a86bbbb` (348 passing, zero regression). 1 OQ (A6-srs-path) deferred with concrete revisit trigger — 4-iteration zero-deferred streak ends by design.

### WP-CORE-6 — SHIPPED (F-21 end-to-end fix)

- Spec v2: `docs/superpowers/specs/2026-05-21-wp-core-6-d1-verifier-non-vacuous-design.md`
- Plan: `docs/superpowers/plans/2026-05-21-wp-core-6-d1-verifier-non-vacuous.md`
- Dev doc: `development_docs/WP-CORE-6-d1-verifier-non-vacuous.md`
- Production code:
  - `core/architect.py` — prompt rewrite, strict parser, signature change, `_truncate_numbered_pairs` helper, architect_fn/specialist_fn thread IDs, extract_per_context_details preserves input ctx
  - `core/verifier/checks_deterministic.py` — D1 non-empty clause + docstring
  - `core/orchestration/pipeline.py` — RefinementExhaustedError branch with issues-list log
- Tests: 10 new (T-D1-NV-1..2 + T-ARCH-1..2b..3 + T-PROP-1..2 + T-INT-1 + T-DEGRADE-LOG-1) across 4 files (2 NEW: `test_architect_identify_contexts.py`, `test_architect_id_propagation.py`).
- Fixture updates: `test_specialist_per_context_loop.py` + `test_intermediate_save.py` (signature + parse-shape adaptations).
- Net diff: 9 files, +627 / -69 LOC.

---

## Backlog state (`.planning/pipeline_audit/improvements_backlog.md`)

### Ingestion layer (10 findings — unchanged from iteration 4)

| status | finding | severity | component |
|---|---|---|---|
| OPEN | F-1 | MAJOR | document_parser_readers.py — `read_pdf` no defensive handling |
| OPEN | F-2 | MAJOR | document_parser_readers.py — `read_txt` cp1254 silent binary garbage |
| SHIPPED | F-3 | MAJOR | empty-input contract + 6 callsite migration |
| OPEN | F-4 | MAJOR (uncertain) | TOC heuristic 120-line + cluster<2 |
| SHIPPED | F-5 | MAJOR | Kaynaklar parity + position guard |
| OPEN | F-6 | MINOR | `_should_merge` quote/bracket terminator gap |
| OPEN | F-7 | MINOR | DOCX zero try/except |
| OPEN | F-8 | MINOR (uncertain) | XXE / external-entity hardening |
| OPEN | F-9 | MINOR | zero logging anywhere |
| OPEN | F-10 | TRIVIAL | same SRS parsed twice per /generate-model |

### Orchestrator layer (12 findings)

| status | finding | severity | component |
|---|---|---|---|
| OPEN (DORMANT) | F-11 | MAJOR | parallel Scout rate-limit race (Scout path dead from prod) |
| OPEN | F-12 | MINOR | Specialist shape-error retry token tracking gap |
| SHIPPED | F-13 | MAJOR | _save_intermediate silent swallow + _current_srs_path |
| SHIPPED | F-14 | MAJOR | SynthesizerEmptyModelError taxonomy preservation |
| OPEN | F-15 | MINOR | Refiner exhaustion fallback observability |
| OPEN | F-16 | TRIVIAL | dead `_split_text_into_chunks` |
| OPEN | F-17 | MINOR | stage_config validation deferred to first LLM call |
| OPEN | F-18 | MINOR | synthetic context descriptions in intermediate JSON |
| OPEN | F-19 | MINOR | exponential backoff comment off |
| OPEN | F-20 | MINOR (downgraded) | thread-safety docs gap |
| SHIPPED | F-21 | MAJOR | D1 verifier non-vacuous (Architect populates supporting_sentence_ids E2E) |
| **OPEN (NEW)** | **F-22** | **MAJOR** | **Refiner only re-runs Specialist; Architect-stage failures degrade silently. Tracks the enforcement extension needed for D1 ERRORs to fail-fast.** |

**Total OPEN MAJOR (live): 5** (F-1, F-2, F-4-uncertain, F-22) + **1 DORMANT** (F-11).

---

## Recommended next iteration (iteration 6)

### Option A (RECOMMENDED): F-22 Refiner stage-aware re-runs

- **Effort:** M-L. Refiner's `_re_run_specialist` becomes `_re_run_failing_stage(issues)`; dispatch by `exc.issues[].stage`; Architect-stage failures trigger re-running `identify_contexts` with issue-aware prompt feedback (or hard-fail via new `ArchitectGroundingError`).
- **Blast radius:** PIPELINE. Completes the D1 enforcement story started by WP-CORE-6. Currently D1 ERROR is honest signal but not enforcement.
- **Why now:** discovered fresh during WP-CORE-6 (Codex W-4); cache-hot; closes the asymmetry where Specialist errors are recoverable but Architect errors silently degrade.
- **Risk:** Refiner refactor touches the pipeline orchestration layer; non-trivial concurrency / control-flow change. Mitigate with stage-by-stage RED tests.

### Alternative: pivot to priority-3 audit walk

- Synthesizer (`core/synthesizer/*`) or Verifier (`core/verifier/*`) deeper close-lookup. Surfaces fresh findings; broadens audit coverage. Marked PENDING in component catalog.

### Alternative: pivot to ingestion-layer MAJOR-OPEN

- F-1 (PDF defensive handling), F-2 (cp1254 binary garbage), or F-4 (TOC heuristic). Refreshes audit-walk lens after 4 consecutive orchestrator iterations.

**Coordinator recommendation for iteration 6:** **Option A (F-22)**. Closes the half-fix from WP-CORE-6; restores end-to-end fail-fast for the EMSE methodology claim.

---

## Loop ritual reminder for the next coordinator

1. Read CLAUDE.md, AGENTS.md (full, fresh).
2. `git log --oneline -8` + `git status --short`.
3. Read this handoff + `.planning/pipeline_audit/CURRENT.md`.
4. Confirm baseline:

   ```bash
   cd extension/backend
   /Library/Frameworks/Python.framework/Versions/3.13/bin/python3 -m pytest -m "not integration" --tb=no -q 2>&1 | tail -3
   ```
   Must be `348 passed, 31 deselected`. If not, STOP — baseline broken before you started.
5. Choose iteration target per §"Recommended next iteration" (F-22 recommended).
6. **Production reachability subsection mandatory in spec §Motivation** — loop discipline lesson from iteration 4 (F-11 dormant scope miss).
7. Each commit gated on pytest ≥ 348. No `--no-verify`. No silent fallbacks.

---

## Non-negotiables carried forward

- Baseline pytest never drops.
- Smallest correct change; no overengineering.
- Each WP has spec + Codex review + plan + dev_doc.
- Atomic Conventional Commits + Claude trailer.
- No `git push` without explicit user "push it."
- Caveman mode active (technical preserve, fluff drop) for terminal chatter; code/commits/PRs/security stay normal.
- Turkish ↔ user, English ↔ code/commits.
- RQ5 deleted per D6; do not reference.
- No backwards-compat shims; no silent fallbacks.
- WP-CORE-4 invariant: any future stage retry wrapper using `_save_intermediate` MUST include `except IntermediateSaveError: raise` before generic `except Exception`.
- WP-CORE-5b invariant: any future pipeline-orchestration code constructing `SynthesizerEmptyModelError` MUST pass `srs_path` (default `"<unknown>"`).
- **WP-CORE-6 invariant (NEW)**: any future Architect stage that produces context proposals MUST include `supporting_sentence_ids` from the numbered-sentence prompt. D1 verifier WILL flag empty IDs as `ungrounded_context` ERROR.
- **WP-CORE-6 invariant (NEW)**: `extract_per_context_details` signature is `List[ContextHypothesis]`, NOT `List[str]`. Callers must pass full ContextHypothesis instances so IDs propagate into SpecialistAnalysis.context.

---

## Open follow-ups from iteration 5

- **F-22 elevated to top priority** by Codex W-4. Iteration 6 target.
- **F-11 stays DORMANT.** Same as iteration-4 handoff.
- **Audit-text-vs-code-reality mismatches** for two consecutive WPs (F-14, F-21). Lesson reinforced: spec §Motivation MUST include production-reachability subsection that re-verifies the finding before drafting the fix.
- **4-iteration zero-deferred streak ends.** WP-CORE-6 defers OQ-1 (A6-srs-path) with concrete revisit trigger (post-F-22). Honest scope-bounded defer ≠ drift; the streak is a tool, not a target.
- **EMSE paper Methods section needs honest update.** Pre-WP-CORE-6 claim "D1 catches contexts citing un-emitted sentences" was empirically vacuous. Post-WP-CORE-6: "D1 evaluated non-vacuously per run; failures logged in run manifest; F-22 tracks fail-fast enforcement." Flag for advisor (Hoca) at next review.
- **Pattern reinforced: layer-cake defense at three scopes.** WP-CORE-6 has three defense layers (Architect prompt validation, D1 verifier ERROR, pipeline-layer degrade-log enrichment). Pattern matches WP-CORE-5b's three-layer empty-model defense.
- **Pattern reinforced: TDD with genuine-RED-fail.** RED commit accepted 8 known-failing tests (predicted exactly). GREEN turned all green + updated 2 pre-existing fixture sites that broke under signature change.

---

## Files this iteration produced

**Source/test:**
- `extension/backend/core/architect.py` — modified (a86bbbb) — prompt + parser + signature + helpers
- `extension/backend/core/verifier/checks_deterministic.py` — modified (a86bbbb) — D1 non-empty clause
- `extension/backend/core/orchestration/pipeline.py` — modified (a86bbbb) — degrade-log enrichment
- `extension/backend/tests/test_verifier_deterministic.py` — modified (fd7f203) — T-D1-NV-1..2 appended
- `extension/backend/tests/test_architect_identify_contexts.py` — created (fd7f203 + fixture fix a86bbbb) — T-ARCH-1..3
- `extension/backend/tests/test_architect_id_propagation.py` — created (fd7f203) — T-PROP-1..2 + T-INT-1
- `extension/backend/tests/test_pipeline_orchestration.py` — modified (fd7f203) — T-DEGRADE-LOG-1 appended
- `extension/backend/tests/test_specialist_per_context_loop.py` — modified (a86bbbb) — signature adaptation
- `extension/backend/tests/test_intermediate_save.py` — modified (a86bbbb) — signature + parse-shape adaptation

**Persistent dev memory:**
- `development_docs/WP-CORE-6-d1-verifier-non-vacuous.md` — created (this commit)
- `development_docs/INDEX.md` — modified (this commit, ACTIVE row #7)

**Audit state:**
- `.planning/pipeline_audit/CURRENT.md` — modified (this commit)
- `.planning/pipeline_audit/improvements_backlog.md` — modified (this commit; F-21 → SHIPPED; F-22 NEW)
- `.planning/pipeline_audit/decision_log.md` — modified (this commit; D-PICK-WP-CORE-6 + D-CODEX-REVIEW-WP-CORE-6 appended)
- `.planning/pipeline_audit/findings/architect.md` — modified (this commit; §F-21 SHIPPED status + reframe)
- `.planning/pipeline_audit/handoff-2026-05-22-2130.md` — this file

**Planning artifacts:**
- `docs/superpowers/specs/2026-05-21-wp-core-6-d1-verifier-non-vacuous-design.md` — v2 (landed in next commit)
- `docs/superpowers/plans/2026-05-21-wp-core-6-d1-verifier-non-vacuous.md` — landed in next commit

---

## Goal-backward verification (iteration-level)

| Iteration-5 goal | Evidence |
|---|---|
| Pick F-21 per WP-CORE-5b handoff Codex W-8 | F-21 picked; LIVE-in-production bug confirmed. |
| Spec → Codex xhigh review → plan → SDD → dev_doc → state update | Spec v1 → review (4 CRITICAL + 4 WARN + 6 NIT + 1 OQ) → v2 → plan → RED + GREEN → dev_doc + state. |
| Each commit gated on pytest ≥ baseline (RED accepts known-failing tests) | RED `fd7f203`: 340 passed + 8 failed = 348 collected; GREEN `a86bbbb`: 348 passed, 0 failed, 0 regression. |
| No `git push` | Confirmed; main remains local. |
| Atomic Conventional Commits + Claude trailer | All 4 commits conform. |
| Zero-deferred standard | **Ends here by design.** 1 OQ (A6-srs-path) deferred with concrete revisit trigger (post-F-22). Honest scope-bounded defer ≠ drift. 4 CRITICAL + 4 WARN handled inline. |
| Production reachability subsection in spec | YES — §Motivation explicitly notes F-21 is LIVE (every run), contrasts F-11/F-14 dormant. Loop discipline lesson applied. |

Iteration 5 closed clean. Next coordinator: pick up after the DOC + planning commits land, re-enter via ritual above. **F-22 strongly recommended** — closes the half-fix from WP-CORE-6's honest-signal D-3.
