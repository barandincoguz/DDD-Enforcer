# Handoff — Domain Pipeline Hardening Loop (iteration 8 → 9)

**Written:** 2026-05-23 14:00 GMT+3
**Reason:** Iteration 8 SHIPPED — WP-CORE-9 (F-2 mislabeled-file detection in read_txt) closed end-to-end. Autonomous loop continues.

## State

### Baseline (sacred)

```
pytest -m "not integration" → 373 passed, 31 deselected
Pre-WP-CORE-9 baseline was 365. Net +8 tests, zero regression.
Cumulative: pre-loop 272 → post-iteration-8 373 (+101 tests over 8 iterations).
```

### HEAD

```
{pending}  chore(planning): WP-CORE-9 spec v2 + plan
{this}     chore(artifacts): WP-CORE-9 dev_doc + audit state + F-2 SHIPPED
ff28324    fix(document_parser): WP-CORE-9 MisLabeledFileError + magic-byte detection
45d9cdf    test(document_parser): WP-CORE-9 red-phase tests
0e43812    chore(planning): WP-CORE-8 ← pre-iteration-8 HEAD
```

4 commits during iteration 8. **NOT pushed.**

### Iteration 8 — what happened

1. F-2 picked per ingestion-layer pivot recommendation.
2. Spec v1 → Codex xhigh: 1 CRITICAL + 8 WARN + 4 NIT + 1 OQ. Heavy revisions:
   - C-1: T-MFE-5 reclassified GREEN-from-start.
   - W-1: ZIP signature variants added.
   - W-2: fixtures expanded to long no-NUL printable; T-MFE-6 added for realistic ZIP w/ NULs.
   - W-3: BOM + later-substring test.
   - W-4: near-miss helper test.
   - W-5: reachability evidence cites VSCode UI path.
   - W-6: motivation reframed dual benefit (rare silent + common diagnostic).
   - W-7: downstream impact corrected (routes through _parse_srs_batch).
   - W-8: explicit .txt-only non-goal.
   - OQ: MisLabeledFileError re-exported from core.document_parser.
3. RED `45d9cdf` (6 fail + 2 GREEN-from-start regression guards).
4. GREEN `ff28324` (373 passing, zero regression).

## WP-CORE-9 — SHIPPED (F-2)

- Spec v2 / Plan / Dev doc paths in dev_doc front matter.
- Code:
  - `core/document_parser_readers.py` — `MisLabeledFileError`, signature table (10 entries), `_detect_binary_signature`, read_txt pre-decode check
  - `core/document_parser.py` — re-export via `__all__`
- Tests: 8 new (T-MFE-1..8) in NEW file `tests/test_document_parser_mislabeled_file.py`.

## Backlog state

### Ingestion layer (10 findings)

| status | finding |
|---|---|
| SHIPPED | F-3 (empty-input contract), F-5 (Kaynaklar parity), F-2 (mislabeled detection) — **3 shipped** |
| OPEN-MAJOR | F-1 (read_pdf defensive), F-4-uncertain (TOC heuristic) |
| OPEN-MINOR | F-6, F-7 (DOCX zero try/except), F-8 (XXE) |
| OPEN | F-9 (logging), F-10 (trivial) |

### Orchestrator layer (14 findings)

| status | finding |
|---|---|
| SHIPPED | F-13, F-14, F-21, F-22, F-23 (5 total) |
| OPEN-DORMANT | F-11 |
| OPEN-MINOR | F-12, F-15-downgraded, F-17, F-18, F-19, F-20-downgraded, F-24-NEW |
| OPEN-TRIVIAL | F-16 |

**Total OPEN MAJOR (live): 2** (F-1, F-4-uncertain) + 1 DORMANT (F-11). Both layers reduced.

## Recommended next iteration (iteration 9)

### Option A (RECOMMENDED): F-1 read_pdf defensive handling

- **Effort:** M. Symmetric to WP-CORE-9 for PDF reader. New typed exceptions: `CorruptedPDFError`, `EncryptedPDFError`, `EmptyPDFError(EmptySRSDocumentError)`. Also magic-byte detection: a `.pdf` extension wrapping non-`%PDF-` content → `MisLabeledFileError("PDF expected, got: X")` (re-use existing helper).
- **Blast radius:** PIPELINE.
- **Why now:** continues ingestion-layer momentum. Same audit lens, similar pattern.

### Option B: F-7 DOCX zero try/except (paired with F-1)

- **Effort:** S. Wrap `docx.Document()` in defensive try/except → raise typed errors. Smaller scope than F-1 alone.

### Option C: F-4 TOC heuristic (content quality)

- **Effort:** M-L. Heuristic refactor; risk of regression on legitimate SRS layouts.

### Option D: F-24 srs_path in VerifierIssue (orchestrator pivot)

- **Effort:** M (13-callsite migration). Lower priority than ingestion MAJOR.

**Coordinator recommendation for iteration 9:** **Option A (F-1)** — completes the ingestion-layer "render→reject mislabeled" story across PDF + TXT.

## Loop ritual

1. CLAUDE.md, AGENTS.md fresh.
2. `git log --oneline -8` + `git status --short`.
3. This handoff + `.planning/pipeline_audit/CURRENT.md`.
4. baseline `373 passed, 31 deselected`.
5. Pick iteration target; production reachability mandatory.
6. Each commit gated on pytest ≥ 373.

## Non-negotiables carried forward

- All prior invariants intact.
- **WP-CORE-9 NEW invariant**: `read_txt` raises `MisLabeledFileError` (not return gibberish) when input starts with a known binary magic-byte signature.
- Baseline pytest never drops.
- Smallest correct change.
- Spec + Codex review + plan + dev_doc per WP.
- Atomic commits + Claude trailer.
- No `git push` without "push it".

## Open follow-ups from iteration 8

- **F-2 SHIPPED**. Ingestion-layer MAJOR count: 2 → 2 (F-2 shipped, F-1 still open).
- **Magic-byte detection pattern**: `_detect_binary_signature` helper is now available for re-use in F-1 (read_pdf) and F-7 (read_docx). Co-locate signatures or extract to module-level.
- **TS handler upgrade still future work** (`extension.ts:680-687` swallows SSE error events).

## Goal-backward verification (iteration-level)

| Iteration-8 goal | Evidence |
|---|---|
| Pick F-2 per WP-CORE-8 handoff ingestion-layer pivot | F-2 picked; LIVE per VSCode UI evidence. |
| Spec → Codex xhigh → plan → SDD → dev_doc → state | All 4 commits clean. |
| Each commit gated | RED: 365 + 6 fail + 2 GREEN; GREEN: 373 pass, 0 regression. |
| Codex CRITICAL+WARN inline | 1 CRITICAL + 8 WARN all inline. |
| Codex NIT inlined | 4 NIT all inlined. |
| Codex OQ resolved/tracked | OQ resolved via re-export. |
| Production reachability subsection | YES. |

Iteration 8 closed clean. Iteration 9: F-1 recommended.
