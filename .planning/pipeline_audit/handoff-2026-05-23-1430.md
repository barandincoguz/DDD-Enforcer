# Handoff — Domain Pipeline Hardening Loop (iteration 9 → 10)

**Written:** 2026-05-23 14:30 GMT+3
**Reason:** Iteration 9 SHIPPED — WP-CORE-10 (F-1 read_pdf defensive handling) closed end-to-end.

## State

```
pytest -m "not integration" → 388 passed, 31 deselected
Pre-WP-CORE-10 baseline was 373. Net +15 tests, zero regression.
Cumulative: pre-loop 272 → post-iteration-9 388 (+116 tests over 9 iterations).
```

HEAD: `5df3df6` (GREEN) → DOC `{this}` → PLANNING pending. **NOT pushed.**

## Iteration 9 — what happened

1. F-1 picked per WP-CORE-9 handoff ingestion-layer continuation.
2. Spec v1 → Codex xhigh: 2 CRITICAL + 6 WARN + 1 NIT + 1 OQ.
3. Spec v2 adopted all CRITICAL+WARN inline.
4. RED `12a984a` (12 RED-by-design + 3 GREEN-from-start guards).
5. GREEN `5df3df6` (388 passing, zero regression).

## Backlog state

### Ingestion layer (10 findings)

| status | finding |
|---|---|
| SHIPPED | F-3 (empty-input), F-5 (Kaynaklar), F-2 (mislabeled .txt), **F-1 (PDF defensive)** — 4 shipped |
| OPEN-MAJOR | F-4-uncertain (TOC heuristic) |
| OPEN-MINOR | F-6, F-7 (DOCX zero try/except), F-8, F-9 |
| OPEN-TRIVIAL | F-10 |

### Orchestrator layer (14 findings, unchanged from iteration 8)

5 SHIPPED + 1 DORMANT + 7 MINOR + 1 TRIVIAL.

**Total OPEN MAJOR (live): 1** (F-4-uncertain) + 1 DORMANT (F-11). Down from 5 live MAJORs at iteration 1.

## Recommended next iteration (iteration 10)

### Option A (RECOMMENDED): F-7 DOCX defensive handling

- **Effort:** S-M. Symmetric to WP-CORE-9/-10 for `read_docx`. Same magic-byte detection pattern; add typed exceptions for corrupted/encrypted DOCX.
- **Why now:** completes the ingestion-layer reader-defense trilogy (TXT/PDF/DOCX). Codex W-8 / OQ-3 from WP-CORE-9/-10 explicitly opened this path.

### Option B: F-4 TOC heuristic refactor

- **Effort:** M-L. Content-quality MAJOR-uncertain.

### Option C: F-24 srs_path in VerifierIssue (orchestrator pivot)

- **Effort:** M (13-callsite migration).

**Recommendation:** Option A (F-7) — completes ingestion trilogy.

## Non-negotiables

All prior invariants intact. **WP-CORE-10 NEW invariant**: `read_pdf` rejects non-`%PDF-` byte-0 content via `MisLabeledFileError`; raises typed `EncryptedPDFError`/`CorruptedPDFError`/`EmptyPDFError` for those failure modes.

## Goal-backward verification

| Goal | Evidence |
|---|---|
| F-1 picked | YES, ingestion continuation. |
| Spec → Codex → v2 → atomic | All 4 commits. |
| pytest baseline | 373 + 12 fail + 3 GREEN guards in RED; 388 in GREEN. |
| Codex CRITICAL+WARN inline | YES. |
| Production reachability subsection | YES. |

Iteration 9 closed. Iteration 10: F-7 recommended.
