# Handoff — Domain Pipeline Hardening Loop (iteration 2 → 3)

**Written:** 2026-05-21 09:37 GMT+3
**Reason:** Iteration 2 (WP-CORE-3) fully shipped; natural breakpoint before iteration 3.

---

## State of the loop

### Baseline (sacred)

```
pytest -m "not integration" → 321 passed, 31 deselected
Pre-WP-CORE-3 baseline was 305.  Net +16 tests, zero regression.
Cumulative: pre-loop 272 → post-iteration-2 321 (+49 tests over 2 iterations).
```

### HEAD at handoff

```
2646856 chore(planning): WP-CORE-3 spec v2 + plan into git history
48e3d98 chore(artifacts): WP-CORE-3 dev_doc + audit state update
daefeb0 fix(parser, main): WP-CORE-3 EmptySRSDocumentError — explicit empty-input contract
91dbeb4 test(parser, main): WP-CORE-3 red-phase tests for empty-input contract
3d13f26 chore(planning): WP-CORE-2 iteration 1 handoff + CURRENT pointer   ← pre-iteration-2 HEAD
```

4 new commits on main during iteration 2. **NOT pushed.** Loop rule: only push on explicit user "push it."

### WP-CORE-3 — SHIPPED (F-3 empty-input contract)

- Spec v2: `docs/superpowers/specs/2026-05-21-wp-core-3-empty-input-contract-design.md` (Codex xhigh reviewed; 2 CRITICAL + 5 WARN all handled inline — zero WARNs accepted-with-rationale this round).
- Plan: `docs/superpowers/plans/2026-05-21-wp-core-3-empty-input-contract.md`.
- Dev doc: `development_docs/WP-CORE-3-empty-input-contract.md`.
- Production code:
  - `core/document_parser.py` — new `EmptySRSDocumentError(ValueError)` class; raise at end of `parse_file` when post-processed text empty.
  - `main.py` — new `_parse_srs_batch(parser, file_paths) -> (combined_text, srs_docs, error_or_none)` helper; six call sites migrated to per-path policy (HARD propagate / SOFT skip+log / MIXED batch via helper).
- Tests: 16 new (11 T-EMPTY-* in `tests/test_document_parser.py` + 4 T-WIRE-* in new `tests/test_main_wiring.py`; T-EMPTY-11 parametrized expands to 2).
- Latent bug folded in: post-loop `combined_text.strip()` guards at `/generate-model{,-stream}` were dead code (separator headers always made strip non-empty); replaced with `srs_docs`-emptiness aggregate check.
- Documented behavior change: mixed batches (one empty + one good file) now succeed cleanly; previously the empty file's separator-only fragment silently degraded the combined input.

---

## Backlog state (`.planning/pipeline_audit/improvements_backlog.md`)

| status | finding | severity | component |
|---|---|---|---|
| OPEN | F-1 | MAJOR | document_parser_readers.py — `read_pdf` no defensive handling for encrypted/image-only/malformed PDFs |
| OPEN | F-2 | MAJOR | document_parser_readers.py — `read_txt` silently emits binary garbage under cp1254 |
| SHIPPED (daefeb0) | F-3 | MAJOR | document_parser.py + main.py — empty-input contract + 6 callsite migration + latent post-loop guard fix |
| OPEN | F-4 | MAJOR (uncertain) | document_parser.py — TOC heuristic 120-line + cluster<2 |
| SHIPPED (25e6880) | F-5 | MAJOR | document_parser.py — Kaynaklar parity + position guard |
| OPEN | F-6 | MINOR | document_parser.py — `_should_merge` quote/bracket terminator gap |
| OPEN | F-7 | MINOR | document_parser_readers.py — DOCX zero try/except |
| OPEN | F-8 | MINOR (uncertain) | document_parser_readers.py — XXE / external-entity hardening |
| OPEN | F-9 | MINOR | both — zero logging anywhere |
| OPEN | F-10 | TRIVIAL | main.py:366,480 — same SRS parsed twice per `/generate-model` |

**3 MAJOR + 3 MINOR + 1 MINOR(uncertain) + 1 TRIVIAL = 8 OPEN** (was 9 pre-iteration-2; F-3 now SHIPPED).

---

## Component catalog state (`.planning/pipeline_audit/component_catalog.md`)

- Priority 1 (ingestion): `document_parser.py` + `document_parser_readers.py` — **DONE** (10 findings catalogued; 2 shipped, 4 OPEN MAJOR, 3 OPEN MINOR, 1 OPEN TRIVIAL).
- Priority 2 (orchestrator): `core/architect.py` (752 LOC) + `core/orchestration/pipeline.py` (60 LOC) + `core/orchestration/errors.py` (51 LOC) — **PENDING**.
- Priorities 3–12 — all PENDING.

---

## Recommended next iteration (iteration 3)

**Default candidate per loop spec:** close-lookup on `core/architect.py` (priority 2; 752 LOC; biggest single surface in the pipeline; iteration 2 deferred this in favor of cohesive F-3 follow-up).

**Alternative consideration:** continue ingestion-layer cleanup — F-1 (`read_pdf` defensive handling) or F-2 (`read_txt` cp1254 binary garbage) are both small-effort MAJORs in the already-audited ingestion layer.

**Rationale to prefer priority-2 architect.py close-lookup:**
- Ingestion layer now has 2 shipped + 4 OPEN MAJORs; further ingestion work is on the steeply diminishing-return curve.
- `architect.py` is the orchestrator — findings there have larger blast radius (PIPELINE > MODULE).
- Loop spec's stated priority ordering (close-lookup priority 2 next).
- 752 LOC will likely surface higher-severity findings than the ingestion remainders.

**Rationale to prefer ingestion continuation:**
- Cache-hot context (no fresh audit walk needed).
- F-1 (encrypted PDFs) is on the EMSE reviewer's checklist for any tool that ingests user-uploaded documents.
- Smaller-than-architect lift; quick win to clear ingestion-layer MAJORs to zero.

**Coordinator recommendation for iteration 3:** **`core/architect.py` close-lookup.** Two consecutive ingestion-layer WPs (CORE-2 + CORE-3) have closed the highest-value ingestion gaps; continuing there is increasingly marginal. Priority 2 was promised in iteration 1's handoff and remains the highest-leverage next audit surface.

---

## Loop ritual reminder for the next coordinator

1. Read CLAUDE.md, AGENTS.md (full, fresh).
2. `git log --oneline -6` + `git status --short`.
3. Read this handoff + `.planning/pipeline_audit/CURRENT.md`.
4. Confirm baseline:

   ```bash
   cd extension/backend
   /Library/Frameworks/Python.framework/Versions/3.13/bin/python3 -m pytest -m "not integration" --tb=no -q 2>&1 | tail -3
   ```
   Must be `321 passed, 31 deselected`. If not, STOP — baseline broken before you started.
5. Choose: **`core/architect.py` close-lookup** (recommended) **OR** F-1 / F-2 ingestion continuation.
6. If close-lookup: dispatch Explore subagent (very thorough) to populate `.planning/pipeline_audit/findings/architect.md` + extend `component_catalog.md`. THEN pick a finding and run spec → Codex xhigh review → plan → SDD execute → dev_doc.
7. If F-1 / F-2: skip directly to spec → Codex xhigh review → plan → SDD execute → dev_doc.
8. Either way: each commit gated on pytest ≥ 321. No `--no-verify`. No silent fallbacks.

---

## Non-negotiables carried forward

- Baseline pytest never drops.
- Smallest correct change; no overengineering.
- Each WP has spec + Codex review + plan + dev_doc.
- Atomic Conventional Commits + Claude trailer.
- No `git push` without explicit user "push it."
- Caveman mode active (technical preserve, fluff drop).
- Turkish ↔ user, English ↔ code/commits.
- RQ5 deleted per D6; do not reference.
- No backwards-compat shims; no silent fallbacks.

---

## Open follow-ups from iteration 2

- **No deferred WARNs this round.** All 7 Codex findings (2 CRITICAL + 5 WARN) were handled inline in spec v2. Contrast with iteration 1's WP-CORE-2 where 4 of 6 WARNs were accepted-with-rationale.
- **Atomic GREEN commit pattern** (parser + callsite migration + helper extraction in one commit) was new this iteration; worked cleanly. Recommended for future contract-changing WPs.
- **`_StubParser(SRSDocumentParser)` subclass pattern** for wiring tests worked (pyright accepted via Liskov substitution); use as template for future helper-extraction tests.
- T-EMPTY-9 rewrite mid-GREEN was a learning: WP-CORE-2's position guard intentionally protects single-line `"References\n"` files; cross-WP integration tests need whitespace-padded content to push past the 0.5 fraction guard. Worth a comment in the test for future maintainers.
- `extension/backend/core/intermediate/*.json`, `validation_metrics_report.json`, `domain/model.PRE-FRESH-RUN-BACKUP.json` remain untracked runtime artifacts; loop did not touch.
- `AGENTS.md` shows as `M` (auto-memory hook); leave alone.
- Local `.venv` still broken per CLAUDE.md (`pip`/`pytest` not present); CI uses 3.12; loop runs 3.13 from `/Library/Frameworks/...`.

---

## Files this iteration produced

**Source/test:**
- `extension/backend/core/document_parser.py` (modified, daefeb0)
- `extension/backend/main.py` (modified, daefeb0)
- `extension/backend/tests/test_document_parser.py` (modified, 91dbeb4)
- `extension/backend/tests/test_main_wiring.py` (created, 91dbeb4)

**Persistent dev memory:**
- `development_docs/WP-CORE-3-empty-input-contract.md` (created, 48e3d98)
- `development_docs/INDEX.md` (modified, 48e3d98)

**Audit state:**
- `.planning/pipeline_audit/CURRENT.md` (modified, 48e3d98)
- `.planning/pipeline_audit/improvements_backlog.md` (modified, 48e3d98)
- `.planning/pipeline_audit/decision_log.md` (modified, 48e3d98)
- `.planning/pipeline_audit/handoff-2026-05-21-0937.md` (this file, next commit)

**Planning artifacts:**
- `docs/superpowers/specs/2026-05-21-wp-core-3-empty-input-contract-design.md` (created, 2646856)
- `docs/superpowers/plans/2026-05-21-wp-core-3-empty-input-contract.md` (created, 2646856)

---

## Goal-backward verification (iteration-level)

| Iteration-2 goal | Evidence |
|---|---|
| Pick WP-CORE-3 = F-3 per iteration-1 handoff recommendation | This iteration shipped it; baseline 305 → 321 |
| Each commit gated on pytest ≥ baseline | RED commit `91dbeb4` accepted red signal (ImportError on `EmptySRSDocumentError`); GREEN commit `daefeb0` hit 321; subsequent commits artifact-only |
| Spec → Codex xhigh review → plan → SDD → dev_doc → state update | All 5 phases produced artifacts; commits 91dbeb4, daefeb0, 48e3d98, 2646856 |
| No `git push` | Confirmed; main remains local |
| Atomic Conventional Commits + Claude trailer | All 4 iteration-2 commits conform |

Iteration 2 closed clean. Next coordinator: pick up at `2646856` HEAD after committing this handoff doc.
