# Handoff — Domain Pipeline Hardening Loop

**Written:** 2026-05-21 02:20 GMT+3
**Reason:** Iteration 1 (WP-CORE-2) fully shipped; main context approaching ~75 % budget; natural breakpoint before iteration 2.

---

## State of the loop

### Baseline (sacred)

```
pytest -m "not integration" → 305 passed, 31 deselected
Pre-WP baseline was 272.  Net +33 tests, zero regression.
```

### HEAD at handoff

```
d4ad32f chore(planning): WP-CORE-2 spec + plan + audit findings into git history
81ad45e chore(artifacts): WP-CORE-2 dev_doc + INDEX + audit state
25e6880 fix(parser): WP-CORE-2 reference truncation — locale parity + position guard
4f932d2 test(parser): WP-CORE-2 red-phase tests for reference-truncation correctness
029f187 docs(todos): sync INDEX status board with shipped WPs   ← pre-loop HEAD
```

4 new commits on main. **NOT pushed.** Loop rule: only push on explicit user "push it."

### WP-CORE-2 — SHIPPED (F-5 reference-heading truncation)

- Spec: `docs/superpowers/specs/2026-05-21-wp-core-2-reference-truncate-fix-design.md` (v2, Codex xhigh reviewed; REVISE → handled 3 WARN in spec, accepted 4 WARN in decision_log).
- Plan: `docs/superpowers/plans/2026-05-21-wp-core-2-reference-truncate-fix.md`.
- Dev doc: `development_docs/WP-CORE-2-reference-truncate-fix.md`.
- Three production edits in `extension/backend/core/document_parser.py`:
  - New constant `REFERENCE_HEADING_MIN_DOCUMENT_FRACTION = 0.5`.
  - Regex adds `|kaynaklar` + optional `(?:\s*[:：])?` (U+FF1A fullwidth colon).
  - `_truncate_at_references` now position-guarded (scans from `int(len(lines) * 0.5)` onward).
- 33 new tests (7 integration T1–T7 + 26 parametrized grammar audit T-regex).
- Codex consult disposition: 3 WARN handled in spec v2, 4 WARN accepted-with-rationale in `decision_log.md` D-CODEX-REVIEW-WP-CORE-2.

---

## Backlog state (`.planning/pipeline_audit/improvements_backlog.md`)

| status | finding | severity | component |
|---|---|---|---|
| OPEN | F-1 | MAJOR | document_parser_readers.py — `read_pdf` no defensive handling for encrypted/image-only/malformed PDFs |
| OPEN | F-2 | MAJOR | document_parser_readers.py — `read_txt` silently emits binary garbage under cp1254 |
| OPEN | F-3 | MAJOR | document_parser.py — `parse_file` returns empty string for empty inputs |
| OPEN | F-4 | MAJOR (uncertain) | document_parser.py — TOC heuristic 120-line + cluster<2 |
| SHIPPED (25e6880) | F-5 | MAJOR | document_parser.py — Kaynaklar parity + position guard |
| OPEN | F-6 | MINOR | document_parser.py — `_should_merge` quote/bracket terminator gap |
| OPEN | F-7 | MINOR | document_parser_readers.py — DOCX zero try/except |
| OPEN | F-8 | MINOR (uncertain) | document_parser_readers.py — XXE / external-entity hardening |
| OPEN | F-9 | MINOR | both — zero logging anywhere |
| OPEN | F-10 | TRIVIAL | main.py:366,480 — same SRS parsed twice per `/generate-model` |

**4 MAJOR + 3 MINOR + 1 MINOR(uncertain) + 1 TRIVIAL = 9 OPEN**.

---

## Component catalog state (`.planning/pipeline_audit/component_catalog.md`)

- Priority 1 (ingestion): `document_parser.py` + `document_parser_readers.py` — **DONE** (10 findings catalogued).
- Priority 2 (orchestrator): `core/architect.py` (752 LOC) + `core/orchestration/pipeline.py` (60 LOC) + `core/orchestration/errors.py` (51 LOC) — **PENDING**.
- Priorities 3–12 — all PENDING.

---

## Recommended next iteration (iteration 2)

**Default candidate per loop spec:** close-lookup on `core/architect.py` (priority 2; 752 LOC; biggest single surface in the pipeline).

**Alternative consideration:** before tackling priority 2, finish ingestion-layer cleanup by picking F-3 (empty-input contract) as WP-CORE-3 — same file scope (`document_parser.py`), same audit context, smaller-than-architect lift. Then move to priority 2 with a clean ingestion slate.

**Rationale to prefer WP-CORE-3 = F-3:**
- Stays in the document_parser audit context (hot in cache).
- Empty-input contract leak has 4 downstream guard duplicates in `main.py:61, 101, 326, 449` — removing the leaky contract has a real downstream payoff.
- TDD path is clear: 3 unit tests (empty PDF, empty DOCX, empty TXT) all raise a new `EmptySRSDocumentError`.

**Rationale to prefer priority-2 architect.py close-lookup:**
- Larger blast radius — architect orchestrates the whole pipeline.
- Loop spec's stated priority ordering.
- 752 LOC will likely surface higher-severity findings.

**Reviewer recommendation:** WP-CORE-3 = F-3 first (smaller, cohesive with iteration 1), THEN switch to priority-2 architect.py close-lookup. But this is a judgement call — the loop coordinator at the next firing should re-evaluate against backlog priority rules.

---

## Loop ritual reminder for the next coordinator

1. Read CLAUDE.md, AGENTS.md (full, fresh).
2. `git log --oneline -5` + `git status --short`.
3. Read this handoff + `.planning/pipeline_audit/CURRENT.md`.
4. Confirm baseline:

   ```bash
   cd extension/backend
   /Library/Frameworks/Python.framework/Versions/3.13/bin/python3 -m pytest -m "not integration" --tb=no -q 2>&1 | tail -3
   ```
   Must be `305 passed, 31 deselected`. If not, STOP — baseline broken before you started.
5. Choose: WP-CORE-3 (F-3) **OR** close-lookup architect.py.
6. Either way: spec → Codex xhigh adversarial review → plan → SDD execute → dev_doc → state update.
7. Each commit: pytest ≥ 305. No `--no-verify`. No silent fallbacks.

---

## Non-negotiables carried forward

- Baseline pytest never drops.
- Smallest correct change; no overengineering.
- Each WP has spec + Codex review + plan + dev_doc.
- Atomic Conventional Commits + Claude trailer.
- No `git push` without explicit user "push it."
- Caveman mode active (technical preserve, fluff drop).
- Turkish ↔ user, English ↔ code/commits.
- RQ5 deleted per D6; do not reference.
- No backwards-compat shims; no silent fallbacks.

---

## Open follow-ups noted during iteration 1

- The 4 ACCEPTED Codex WARNs (W-2b multiword Turkish, W-3 code-fence, W-4 Unicode lookalikes, W-6 ASCII-folded kaynakca) live in `decision_log.md` as tracked follow-ups; they should be re-evaluated if D2/D3 live corpus runs surface them.
- `extension/backend/core/intermediate/*.json` and `domain/model.PRE-FRESH-RUN-BACKUP.json` remain untracked runtime artifacts; the loop has not touched them.
- `AGENTS.md` shows as `M` (auto-memory hook appends the `<claude-mem-context>` block at session start); leave alone — not the loop's modification.
- Local `.venv` still broken per CLAUDE.md (`pip`/`pytest` not present); CI uses 3.12; loop runs 3.13 from `/Library/Frameworks/...`.

---

## Files this iteration produced

**Source/test:**
- `extension/backend/core/document_parser.py` (modified, 25e6880)
- `extension/backend/tests/test_document_parser.py` (modified, 4f932d2)

**Persistent dev memory:**
- `development_docs/WP-CORE-2-reference-truncate-fix.md` (created, 81ad45e)
- `development_docs/INDEX.md` (modified, 81ad45e)

**Audit state:**
- `.planning/pipeline_audit/CURRENT.md` (created + updated, 81ad45e)
- `.planning/pipeline_audit/improvements_backlog.md` (created + updated, 81ad45e)
- `.planning/pipeline_audit/decision_log.md` (created + updated, 81ad45e)
- `.planning/pipeline_audit/component_catalog.md` (created, d4ad32f)
- `.planning/pipeline_audit/findings/document_parser.md` (created, d4ad32f)
- `.planning/pipeline_audit/handoff-2026-05-21-0220.md` (this file, next commit)

**Planning artifacts:**
- `docs/superpowers/specs/2026-05-21-wp-core-2-reference-truncate-fix-design.md` (created, d4ad32f)
- `docs/superpowers/plans/2026-05-21-wp-core-2-reference-truncate-fix.md` (created, d4ad32f)
