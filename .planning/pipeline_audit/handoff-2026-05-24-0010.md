# Handoff — Iterations 33-42 (post-/cont, WP-01 chain complete)

**Written:** 2026-05-24 00:10 GMT+3 (session crossed midnight)
**Reason:** Session-end before `/clear`. WP-01 chain (a/b/c done,
d deferred) closed per user's stop condition. 26 commits ahead of
origin, NOT pushed.

---

## 1. Quick-start for next session

1. **Read this file first** (you are reading it).
2. Read `.planning/pipeline_audit/CURRENT.md` (status pointer).
3. Read `.planning/pipeline_audit/handoff-2026-05-23-1933.md` (prior block).
4. Read `extension/backend/CLAUDE.md` + `AGENTS.md`.
5. Confirm baseline: `cd extension/backend && pytest -m "not integration" -q`
   MUST report `716 passed, 31 deselected`.
6. Confirm HEAD: `git log --oneline -1` → `939719d` (CURRENT.md commit).
7. Verify caveman mode active (full level). Verify ownership-disestablished
   memory entry still loaded.
8. Pick a WP from §10 "Next session menu" or follow user prompt.

---

## 2. Session totals (iterations 33-42)

- **9 WPs shipped** atomically.
- **26 commits** total. All conventional-commits with
  `Co-Authored-By: Claude Opus 4.7 (1M context)` trailer.
- **Baseline 611 → 716** (+105 tests net, zero regression).
- **0 MAJOR-OPEN-live findings.**
- **32 subagent dispatches** across SDD pattern (implementer + spec
  reviewer + quality reviewer + fix loops + final integration).
- **NOT pushed** per autonomous-loop rule. 26 commits ahead of origin.

---

## 3. WPs shipped this session — full table

| Iter | WP | Commits | Title | Δ tests | Pattern |
|------|----|---------|-------|---------|---------|
| 33 | F-16 | `562d648` | Dead extract_domain_sentences cascade cleanup | -1 (deleted) | direct |
| 34 | WP-CORE-20c | `8a85213` | Rerun StageRecord preservation (extend=True) | +10 | TDD direct |
| 35 | ChunkMetadata fix | `7fa4449` | truncated_chunks count via count_truncated_sections | +5 | TDD direct |
| 36 | WP-CORE-30b | `ec62253`+`b16335a`+`02d93e7` | render_refinement_prompt narrow per-context rerun | +7 (4+3) | **SDD** (9 dispatch) |
| 37 | Ownership | `4111410`+`6972638` | CURRENT.md handoff + WP-ownership disestablishment | 0 | direct |
| 38 | WP-01b Task A | `26e79e5`+`bd2cd25`+`e18ee53` | PaperRunManifest schema + writer + provenance | +17 | **SDD** (7 dispatch) |
| 39 | WP-01b Task B | `5ffb518`+`d5a0e09` | metrics.py precision/recall/F1 | +25 (23+2) | **SDD** (4 dispatch) |
| 40 | WP-01b Task C | `162f26e`+`cc21607` | aggregate.py N-runs CI | +16 (14+1+1) | **SDD** (4 dispatch) |
| 41 | WP-01b Task D | `22d11e7`+`1b5495c` | build_tables.py LaTeX renderer | +15 | **SDD** (3 dispatch) |
| 41 | WP-01b Task E | `6291cb0` | Makefile + tables/ scaffolding | 0 | direct (paper.tex untouched) |
| 41 | WP-01b Task F | `dd35db4` | E2E smoke + 227-file legacy archive | +1 | direct (mv + smoke) |
| 42 | WP-01c | `833723b`+`4ca1301` | cost_estimate CLI + multi-provider tracker test | +11 | **SDD** (3 dispatch) |
| 42 | Handoff | `d617af1`+`939719d` | CURRENT.md state pointer updates | 0 | direct |

**Cumulative across the multi-day run:** 348 → 716 (+368 tests, ~85
commits).

---

## 4. WP-01 chain status (user's stop condition)

| Sub-WP | Title | Status |
|--------|-------|--------|
| WP-01a | Provider abstraction (`core/llm/`) | ✅ shipped prior session (2026-05-19) |
| WP-01b | Run orchestrator + metrics + tables (Tasks A-F) | ✅ FULLY SHIPPED this session |
| WP-01c | Token tracking + cost telemetry | ✅ CLOSED this session (most criteria already satisfied by WP-01a + Task A) |
| WP-01d | P1/P2/P3 pipeline classes | ⏸ DEFERRED per user (not in this session's scope) |

User's stop condition met. WP-01d's un-defer is a user decision; not on
this session's autonomous path.

---

## 5. WP-01b Task scope notes (per-task)

### Task A (commits `26e79e5` + `bd2cd25` + `e18ee53`)

- New `core/run_manifest.py`. **DISTINCT** from
  `core/observability/run_manifest.py` — observability one is internal
  telemetry; this one is paper-data deliverable.
- `PaperRunManifest` Pydantic v2 with 17 fields. Run-id sanitization
  strips `[./\s:<>"|?*\\]` (POSIX + Windows safe).
- `Violation` schema with `severity: Literal["ERROR", "WARN"]`.
- Provenance hashing: `sha256_of_file` + `sha256_of_code_tree`
  (deterministic by relpath sort + chunk-by-chunk `<rel>\0<bytes>\0`).
- Empty-tree sentinel `EMPTY_TREE_SHA256` exported as module constant.
- `code_root` ↔ `code_sha256` mutual-presence invariant enforced via
  `@model_validator(mode="after")`.
- Both SHA fields constrained by `pattern=r"^[0-9a-f]{64}$"` to reject
  empty / non-hex / wrong-length values at validation time.
- Atomic writer (tmp + fsync + os.replace).
- 17 tests cover all of the above plus T-01b-A-11 atomic-write crash
  (with `core.run_manifest.os.replace` patch target, not bare).

### Task B (commits `5ffb518` + `d5a0e09`)

- New `core/metrics.py`. Consumes `PaperRunManifest` + `JudgeVerdict`.
- 4 schemas: `JudgeVerdictEntry`, `JudgeMissedExpectation`,
  `JudgeVerdict`, `PrecisionRecallF1`.
- `JudgeVerdict.judge_provider: Literal["gemini", "ollama", "human"]`
  (the `"human"` value is intentional per D3 Fleiss's κ raters).
- `compute_metrics`: TP/FP/FN counting, division-by-zero → 0.0 (no
  NaN), raises `ValueError` on missing judge entry (predicted-without-
  ground-truth is a manifest authoring bug), **AND** on duplicate
  `(violation_type, location)` predictions (the consumed-set guard
  closed a Critical count-inflation bug surfaced by review).
- `compute_metrics` also raises on `verdict.run_id != manifest.run_id`
  mismatch (Task C aggregator joins on run_id).
- `aggregate_scoreline`: micro-average across counts, idempotency guard
  against double-counting the `__aggregate__` entry.
- 25 tests (23 from initial, +2 from review fixes covering the
  duplicate-prediction + run_id-mismatch raises).

### Task C (commits `162f26e` + `cc21607`)

- New `core/aggregate.py` + `scripts/aggregate.py` CLI.
- 3 schemas: `StatisticalSummary`, `AggregatedScoreline`,
  `AggregatedConfiguration`.
- 7 functions: `summarize_metric`, `aggregate_scorelines`,
  `discover_run_pairs`, `group_by_configuration`,
  `aggregate_configuration`, `write_aggregated_configuration_atomic`,
  `compose_aggregate_key`.
- Bootstrap CI: `numpy.random.default_rng(seed)`, 1000 resamples,
  percentile method. **Per-metric seed offset (seed/seed+1/seed+2)**
  for precision/recall/f1 — closes a code-quality finding that
  shared-seed bootstrap produced artificially-correlated CIs (paper-
  data integrity).
- `DEFAULT_BOOTSTRAP_RESAMPLES = 1000` + `DEFAULT_BOOTSTRAP_SEED = 42`
  + `SCHEMA_VERSION = "1.0"` promoted to module constants.
- `discover_run_pairs` raises `ValueError` on ambiguous multiple
  `*.judge.json` files in a run dir (instead of lex-first silent pick).
- CLI rejects `--runs-root <file>` with `is_dir()` guard.
- 16 tests (14 initial + 1 ambiguous-judge + 1 etc).

### Task D (commits `22d11e7` + `1b5495c`)

- New `core/latex_tables.py` + `scripts/build_tables.py` CLI.
- 3 schemas: `TableCell`, `TableRow`, `TableSpec`.
- 11 functions including all 4 RQ builders + `render_table_spec` +
  atomic writer.
- `sanitize_latex_label` whitelists `[A-Za-z0-9_-]`.
- `escape_latex_value` wraps in `\detokenize{...}`.
- `format_mean_std(0.8234, 0.0451, 3)` → `"0.823 ± 0.045"` (Unicode
  U+00B1, not `$\pm$`).
- Injection probe uses a brace-depth walker `_mask_detokenize_blocks`
  (not a naive `[^}]*` regex) so nested-brace inputs are correctly
  masked.
- CLI accepts both `--rq <N>` and `--all --output-dir <dir>`.
- 15 tests (T-01b-D-13 split into round-trip + replace-spy).

### Task E (commit `6291cb0`)

- Repo-root `Makefile` with `tables` target (aggregate → build_tables
  → output `LaTeX_DL_468198_240419/tables/`).
- `LaTeX_DL_468198_240419/tables/README.md` with candidate line
  numbers + `TODO(human-coordinator)` markers for inserting RQ1-4
  into `paper.tex` (line 227, 394, 456, 479 candidates).
- `LaTeX_DL_468198_240419/tables/.gitkeep` holds the directory.
- **`paper.tex` DELIBERATELY NOT TOUCHED.** Assigning RQ numbers to
  specific `\begin{table}` blocks requires paper-writing judgment.

### Task F (commit `dd35db4`)

- New `tests/test_wp01b_smoke.py` with T-01b-F-1 end-to-end smoke
  exercising the full WP-01b chain (3 pipelines × 1 model × 1 domain
  × 2 runs = 6 manifests → aggregate → render → 4 valid .tex files).
- 227 tracked `core/intermediate/*.json` files moved via `git mv` to
  `extension/backend/legacy_pre_emse/intermediate/`. History preserved.
- `core/intermediate/` retained for new pipeline writes (with `.gitkeep`
  if it became empty after the move — actually it stayed non-empty
  because untracked files remained).
- Side-fix: `tests/test_synthesizer_replay_historical.py:_DUMP_PATH`
  updated to point at `legacy_pre_emse/intermediate/` since one of the
  moved files was its fixture.

---

## 6. WP-01c closure (commit `833723b` + `4ca1301`)

- New `core/cost_estimate.py` + `scripts/cost_estimate.py` CLI.
- 3 helpers: `estimate_single_call_cost`, `estimate_run_cost`,
  `estimate_per_stage_cost`. Use `core.llm.registry.Pricing.cost_for`
  as the SSOT (no `configs/pricing.yaml` needed — pricing lives in
  the registry).
- `estimate_per_stage_cost` falls back to `configs.models.MODELS` for
  stage models not in the D1 registry (e.g., `gemini-3-flash-preview`
  Validator).
- CLI: `--model`, `--runs`, `--prompt-tokens`, `--completion-tokens`,
  `--per-stage`. Exit 2 on unknown model or invalid args.
- 11 tests (T-01c-1 through T-01c-10, with T-01c-7 split into 2 sub-
  tests). Multi-provider regression confirms `LLMResponseAdapter`
  correctly normalizes both Gemini and Ollama LLMResponse shapes into
  the tracker's legacy Gemini interface.
- `autouse=True` fixture resets `TokenTracker` singleton before AND
  after every test in the module.
- T-01c-2 honestly named `test_ollama_response_shape_normalises_via_
  adapter` (review fix: original name claimed "zero cost" which the
  test never asserted).
- `estimate_run_cost` return type `Dict[str, Any]` (the `runs` key
  holds an `int`, not a float).

---

## 7. WP-CORE-30b notes (commits `ec62253` + `b16335a` + `02d93e7`)

- Closes WP-CORE-30 documented follow-up. Specialist refine path now
  invokes `render_refinement_prompt` PER affected context, copies
  unaffected contexts verbatim from `prev_output` (narrow rerun, not
  full re-run).
- `PipelineDeps.specialist_with_feedback: Optional[...]` field,
  default None (backward-compat with all pre-existing PipelineDeps
  constructions).
- `_re_run_specialist` filters issues via `_issue_stage(i) ==
  "specialist"` before forwarding.
- `_specialist_with_feedback` method on `DomainArchitect` invokes the
  LLM with `render_refinement_prompt(stage_name="specialist",
  previous_output_json=<serialized>, issues=<adapted>)`. Operation
  label: `specialist_with_feedback:<ctx_name>:attempt-<N>` so the
  manifest tracks it as a rerun bucket.
- `_to_legacy_issue` adapter bridges Pydantic VerifierIssue (`.target`,
  `.severity: str`) → dataclass VerifierIssue (`.location`,
  `.severity: enum`). Adapter is exhaustive over the contract's two-
  value `severity` Literal.
- 7 tests + 1 cleanup commit removing the dead `_AO`/`_SO` local
  import + the dead `numbered_sentences_text` computation.

---

## 8. Locked invariants this session added or reinforced

| ID | Invariant |
|----|-----------|
| F-16 | Dead `extract_domain_sentences` cascade + `ThreadPoolExecutor` import removed from `core/architect.py`. `scout_max_workers` kwarg retained on `__init__` for future Scout module re-wire — 8 test files still set it. |
| WP-CORE-20c | `StageEmitter.stage(name, extend=False)` keyword-only `extend` flag. extend=True REUSES `manifest.stages[name]` across re-enters; per-attempt summaries in `record.metrics["attempts"]`; ContextVar resets ALWAYS run (nested try/finally) even if attempt-summary append raises. |
| ChunkMetadata | `core/scout/chunking.py:count_truncated_sections(chunks)` counts UNIQUE base section IDs with `.chunk_N` suffix. NOT the number of extra chunks; that would over-count subdivided sections. |
| WP-CORE-30b | `_re_run_specialist` filters issues by `_issue_stage(i) == "specialist"` before forwarding. `specialist_with_feedback_fn` MUST copy unaffected context analyses verbatim from `prev_output` (narrow rerun). Operation label format `specialist_with_feedback:<ctx>:attempt-<N>` preserves manifest separation from first-attempt records. |
| Ownership | WP_DAGILIM_BARAN_ALI.md "X owns WP-NN, others do not touch" rule REMOVED. Any agent picks up any WP. Allocation file historical only; risk matrix + sync points still active. |
| WP-01b Task A | `PaperRunManifest.compose_run_id` regex `[./\s:<>"|?*\\]` (POSIX + Windows safe). `code_root` iff `code_sha256` (mutual presence invariant via `@model_validator`). SHA fields `pattern=r"^[0-9a-f]{64}$"`. Empty-tree sentinel `EMPTY_TREE_SHA256` module constant. |
| WP-01b Task B | `compute_metrics` MUST raise on (a) predicted-violation-without-judge-entry, (b) duplicate `(violation_type, location)` predictions, (c) `verdict.run_id != manifest.run_id`. Division-by-zero → 0.0 (no NaN). Aggregate is MICRO-average (sum counts first, then ratio), not macro-average. `aggregate_scoreline` MUST skip any input with `violation_type == "__aggregate__"` (idempotency). |
| WP-01b Task C | Per-metric seed offset (seed / seed+1 / seed+2) for P/R/F1 bootstrap CIs. `DEFAULT_BOOTSTRAP_*` module constants. `discover_run_pairs` raises on ambiguous multiple judge files (not lex-first silent). |
| WP-01b Task D | `sanitize_latex_label` whitelist `[A-Za-z0-9_-]`. `escape_latex_value` produces exactly `\detokenize{...}`. Injection probe MUST use brace-depth walker (not regex with `[^}]*`). |
| WP-01b Task E | `paper.tex` UNTOUCHED. Integration is human-coordinator manual step per `LaTeX_DL_468198_240419/tables/README.md`. |
| WP-01c | `core/llm/registry.py` is the SSOT for pricing. No `configs/pricing.yaml` (the WP-01c spec mention is superseded). `LLMResponseAdapter` normalizes Ollama LLMResponse into the tracker's legacy Gemini interface. |

---

## 9. SDD telemetry

The Subagent-Driven Development pattern (`superpowers:subagent-driven-
development` skill) was used for cross-stage / multi-file / Codex-
REQUIRE WPs.

**Pattern:** implementer subagent (general-purpose, sonnet) →
spec compliance reviewer → code quality reviewer → fix loop until
approved → final integration check (for multi-task WPs).

| WP | Dispatches | Outcome |
|----|-----------|---------|
| WP-CORE-30b | 9 | 2 Task split, 1 fix loop, 1 final integration. Zero regression. |
| WP-01b Task A | 7 | 2 fix loops (colon + 5-finding). Zero regression. |
| WP-01b Task B | 4 | 1 fix loop (1 Critical + 4 Important). Zero regression. |
| WP-01b Task C | 4 | 1 fix loop (3 Important + 3 Minor). Zero regression. |
| WP-01b Task D | 3 | 1 fix loop (1 Important + 4 Minor). Combined spec+quality review. |
| WP-01b Task E | 1 | Pure scaffolding, review skipped (Makefile + docs only). |
| WP-01b Task F | 1 | Smoke + git mv, review skipped (mechanical archive). |
| WP-01c | 3 | 1 fix loop (1 Medium + 2 Low). Combined review. |
| **Total** | **32** | **Zero regression across 25 commits.** |

**Pattern proven:**
- High throughput (multiple WPs per session)
- Low regression (zero across 25 atomic commits)
- Quality findings consistently surfaced and fixed before merge
- "Smallest correct change" preserved via tight fix-prompt scope

---

## 10. Next session menu (ranked by autonomous-safety + ROI)

| Rank | WP | Why now | Risk |
|------|----|---------|------|
| **1** | **Pyright tightening** | Drop `continue-on-error: true` from `.github/workflows/backend-ci.yml:74`. Need ~10 type fixes in `main.py`. Deterministic; CI signal strengthened. ~1-2h. | Mid. Touches main.py prod paths; type fix may surface latent contract bugs. |
| 2 | **Minor deferred-concerns sweep** | Close 10+ accumulated Minor findings from WP-CORE-30b + WP-01b/c reviews in one batch (severity-log, regex dedup, etc.). | Trivial-Mid. Each fix tiny; aggregate context heavy. |
| 3 | **WP-01d** P1/P2/P3 pipeline classes | If user un-defers. Big WP (~6-10h, 3-5 SDD tasks). PaperRunManifest already has the Optional[Literal["P1","P2","P3"]] field — wiring is the deliverable. | High. New abstraction across architect.py; many tests to update. |
| 4 | **WP-CORE-28 / WP-CORE-32** Extension UX wave 1 / webviews | TypeScript work. Manual VS Code smoke required. NOT autonomous-safe. | High. Requires human smoke session. |
| 5 | **F-8 XXE hardening** | Security audit follow-up. Needs threat model first. | Mid. Defensive; depth of work unclear. |
| 6 | **paper.tex `\input{rqN.tex}` integration** | Paper-coordinator manual step. NOT autonomous-safe. Human reads `tables/README.md` and decides which `\begin{table}` blocks become RQ1-4. | N/A. Human task. |

**Recommended default for next session:** Rank 1 (Pyright tightening).
Single coherent WP, no human-in-loop required, makes CI gate honest.
TDD via running `pyright` then `pytest`.

---

## 11. Files NOT to touch (CLAUDE.md rules — unchanged)

- `LaTeX_DL_468198_240419/paper.tex` — shared writing space; manual
  paper-coordinator only (per Task E rationale).
- `todos/WP-09-*.md`, `todos/WP-18-*.md` — DROPPED (RQ5).
- `extension/backend/core/intermediate/*.json` (current-session writes)
  + `validation_metrics_report.json` — runtime artifacts; refresh
  in dedicated `chore(artifacts):` commits.
- `extension/backend/.env` — never committed.
- `core/llm_client.py` — REMOVED in WP-01a; do NOT re-introduce.
- `core/llm/gemini.py:_RUNTIME_FALLBACKS` — must stay empty unless a
  fallback is also registered as a model_id in `registry.py`.

Ownership-based "do not touch" rules have been REMOVED this session.
See memory entry `feedback-ownership-disestablished`.

---

## 12. Critical files / entry points (file-line index — additions this session)

| File | Purpose | WP |
|------|---------|-----|
| `core/run_manifest.py` | `PaperRunManifest` + `Violation` + SHA-256 + atomic writer + `compose_run_id` | WP-01b Task A |
| `core/metrics.py` | `compute_metrics` + `aggregate_scoreline` + 4 Judge / scoreline schemas | WP-01b Task B |
| `core/aggregate.py` | N-runs `summarize_metric` (mean/std/IQR/bootstrap CI) + `aggregate_configuration` + 3 schemas | WP-01b Task C |
| `scripts/aggregate.py` | CLI for N-runs aggregation | WP-01b Task C |
| `core/latex_tables.py` | 4 RQ table builders + `render_table_spec` + LaTeX sanitization helpers | WP-01b Task D |
| `scripts/build_tables.py` | CLI for per-RQ LaTeX rendering | WP-01b Task D |
| `Makefile` | `make tables` target | WP-01b Task E |
| `LaTeX_DL_468198_240419/tables/README.md` | Human-coordinator integration guide | WP-01b Task E |
| `core/cost_estimate.py` | Pre-flight USD cost projection (single + run + per-stage) | WP-01c |
| `scripts/cost_estimate.py` | CLI for cost projection | WP-01c |
| `tests/test_paper_run_manifest.py` | 17 tests for Task A | WP-01b Task A |
| `tests/test_metrics.py` | 25 tests for Task B | WP-01b Task B |
| `tests/test_aggregate.py` | 16 tests for Task C | WP-01b Task C |
| `tests/test_latex_tables.py` | 15 tests for Task D | WP-01b Task D |
| `tests/test_wp01b_smoke.py` | 1 E2E smoke for full WP-01b chain | WP-01b Task F |
| `tests/test_token_tracker_multi_provider.py` | 11 tests for WP-01c | WP-01c |
| `tests/test_stage_extend_mode.py` | 10 tests for WP-CORE-20c | WP-CORE-20c |
| `tests/test_specialist_with_feedback.py` | 7 tests for WP-CORE-30b | WP-CORE-30b |
| `extension/backend/legacy_pre_emse/intermediate/` | 227-file legacy intermediate JSON archive | WP-01b Task F |

---

## 13. Memory state at handoff

`/Users/barandincoguz/.claude/projects/-Users-barandincoguz-Desktop-AI---NLP-DDD-Enforcer/memory/MEMORY.md`:

```
- [User role and Turkish communication](user_role.md)
- [DDD-Enforcer EMSE submission — locked scope](project_emse_submission.md)
- [Workflow: plan→feedback→revised plan loop](feedback_workflow.md)
- [Planning artifacts pointer](reference_planning_artifacts.md)
- [Credentials setup](credentials_setup.md)
- [Accuracy over cost](feedback-accuracy-over-cost.md)
- [Ownership disestablished](feedback-ownership-disestablished.md)
```

7 entries. New this session: `feedback-ownership-disestablished.md`.

---

## 14. Operational rules carried forward

- Caveman mode ACTIVE (full). Drop articles / filler / pleasantries.
  Code/commits/security: write normal.
- Communication Turkish (per user_role memory); code/commit messages
  English.
- Accuracy > cost. NO model-tier downgrades. WP-CORE-21 + WP-CORE-34
  rejected per `feedback-accuracy-over-cost`.
- **Ownership disestablished.** Any agent picks up any WP. See
  `feedback-ownership-disestablished` memory entry.
- NO `git push` without explicit "push it".
- NO `--no-verify`.
- Codex xhigh skip pattern documented in commit body for every skip.
- TDD strict: RED → GREEN → DOC → COMMIT.
- SDD default for cross-stage / multi-file / Codex-REQUIRE WPs.
- Atomic commits: one logical change per commit; conventional-commits
  trailer + Co-Authored-By.
- Baseline guardrail: every WP ends with `pytest -m "not integration"`
  reporting at least the previous WP's count.

---

## 15. Git state at handoff

```
HEAD: 939719d  chore(planning): CURRENT.md update for iter 33-42 (WP-01 chain complete)
Branch: main
Ahead of origin/main: 26 commits (NOT pushed)
Working tree: clean except runtime artifacts (intermediate JSONs,
              ast_signals_diagnostics.json, validation_metrics_report.json)
```

Recent commits (this session):
```
939719d chore(planning): CURRENT.md update for iter 33-42 (WP-01 chain complete)
4ca1301 fix(cost): WP-01c — address 3 quality findings from review
833723b feat(cost): WP-01c closure — cost_estimate CLI + multi-provider tracker test
dd35db4 chore(legacy): WP-01b Task F — archive legacy intermediate JSONs + E2E smoke
6291cb0 chore(build): WP-01b Task E — Makefile + tables/ scaffolding
1b5495c fix(tables): WP-01b Task D — harden injection probe + doc empty-dir case
22d11e7 feat(tables): WP-01b Task D — LaTeX renderer + CLI
cc21607 fix(aggregate): WP-01b Task C — address 3 Important + 3 Minor findings
162f26e feat(aggregate): WP-01b Task C — N-runs aggregator + CLI
d5a0e09 fix(metrics): WP-01b Task B — address 1 Critical + 4 Important findings
5ffb518 feat(metrics): WP-01b Task B — per-violation-type precision/recall/F1
d617af1 chore(planning): CURRENT.md update for iter 33-37 (WP-01b Task A shipped via SDD)
e18ee53 fix(run_manifest): WP-01b Task A — address 5 code quality findings
bd2cd25 fix(run_manifest): WP-01b Task A — sanitize colon in compose_run_id
26e79e5 feat(run-manifest): WP-01b Task A — PaperRunManifest schema + atomic writer
6972638 docs(planning): disestablish WP-ownership gating across CLAUDE.md + todos
4111410 chore(planning): CURRENT.md update for iter 33-36 (F-16 + 20c + ChunkMetadata + 30b SDD)
02d93e7 fix(architect): WP-CORE-30b clean up dead code from Task 2 review
b16335a feat(architect): WP-CORE-30b Task 2 — specialist_with_feedback_fn closure
ec62253 feat(orchestration): WP-CORE-30b add specialist_with_feedback dep wiring
7fa4449 fix(scout, architect): plumb real truncated_chunks count into ChunkMetadata
8a85213 feat(observability, orchestration): WP-CORE-20c preserve rerun StageRecord
562d648 chore(architect): F-16 delete dead extract_domain_sentences cascade
```

---

## 16. Loop ritual for next session

1. Read this handoff (you are here).
2. Confirm baseline 716. Confirm HEAD `939719d`. Confirm 26 commits
   ahead origin/main (not pushed).
3. Read MEMORY.md (7 entries; ownership-disestablished is the new
   one). Verify caveman mode active.
4. Pick from §10 menu OR follow user prompt.
5. Default if user says "devam" with no further direction:
   **Pyright tightening** (rank 1). ~10 type fixes in `main.py` +
   drop `continue-on-error: true` from `.github/workflows/backend-ci.yml`.
6. Caveman mode persists. Code/commits stay normal English.
7. Do NOT propose model-tier downgrades.
8. NO `git push` without explicit "push it".
9. SDD pattern is the default for any WP touching 3+ files or cross-
   stage orchestration. Direct edits OK for surgical 1-2-file fixes.
10. After each WP: stage atomically, commit with template, update
    CURRENT.md if status pointer shifts.

End of handoff.
