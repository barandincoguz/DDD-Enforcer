# Handoff — Domain Pipeline Hardening Loop (iteration 4 → 5)

**Written:** 2026-05-21 11:45 GMT+3
**Reason:** Iteration 4 closed. WP-CORE-5 abandoned mid-iteration; pivoted to WP-CORE-5b (F-14) and shipped clean. Natural breakpoint before iteration 5.

---

## State of the loop

### Baseline (sacred)

```
pytest -m "not integration" → 338 passed, 31 deselected
Pre-WP-CORE-5b baseline was 332.  Net +6 tests, zero regression.
Cumulative: pre-loop 272 → post-iteration-4 338 (+66 tests over 4 iterations).
```

### HEAD at handoff (anticipated after DOC + planning commits)

```
{pending}  chore(planning): WP-CORE-5b spec v2 + plan + abandoned WP-CORE-5 spec into git history
{pending}  chore(artifacts): WP-CORE-5b dev_doc + audit state update
27a5d98    fix(orchestration, architect): WP-CORE-5b SynthesizerEmptyModelError pre-call guard + srs_path
cc82e64    test(orchestration): WP-CORE-5b red-phase tests for SynthesizerEmptyModelError guard placement + srs_path propagation
2b8602f    chore(planning): WP-CORE-4 spec v2 + plan into git history   ← pre-iteration-4 HEAD
```

4 new commits on main during iteration 4 (2 production + 2 planning/artifacts). **NOT pushed.** Loop rule: only push on explicit user "push it."

### Iteration 4 — what actually happened (the abandon + pivot story)

1. **Initial pick:** F-11 (parallel Scout rate-limit race) per Option A from prior handoff. Drafted WP-CORE-5 spec v1.
2. **Codex xhigh review on WP-CORE-5 v1:** 3 CRITICALs killed the WP.
   - **C-1:** `extract_domain_sentences` (where `ThreadPoolExecutor`-based parallel Scout lives) is dead from production. `analyze_document.scout_fn` only calls `section_aware_chunks()` — no LLM, no `_wait_for_rate_limit`. F-11 symptom dormant.
   - **C-2:** Reservation pattern paces `_wait_for_rate_limit()` returns, not `client.chat()` send-time. Doesn't close the actual wire-level microslip.
   - **C-3:** Proposed RED tests pass against current code (lock-held sleep already spaces returns + totals).
3. **User decision:** abandon WP-CORE-5; pivot to F-14. Marked WP-CORE-5 spec v1 ABANDONED with audit-trail banner (file preserved). F-11 moved to OPEN (DORMANT) status in backlog.
4. **Pivot pick:** F-14 (SynthesizerEmptyModelError pipeline escape). Drafted WP-CORE-5b spec v1.
5. **Close-lookup audit revealed:** F-14 finding text was misframed. Hard-fail policy already explicit (`test_create_fallback_model_is_gone`). `SynthesizerEmptyModelError` unreachable for in-tree synthesizer (Pydantic `_non_empty` validator fires first → `ValidationError` escapes taxonomy). Real gap: taxonomy preservation, not policy choice.
6. **Codex xhigh review on WP-CORE-5b v1:** **GO with 3 conditions.** 0 CRITICAL + 6 WARN + 3 NITS + 3 OQ. Key adoptions:
   - W-1: missed refiner-shrink-to-empty success path → added T-EMPTY-3.
   - W-3: don't delete post-call check (injected synthesizers can bypass Pydantic via `model_construct`) → retained as belt-and-suspenders; added T-EMPTY-4 to cover.
   - OQ-2: add `srs_path` for WP-CORE-4 symmetry → adopted.
   - W-7: reframe as "contract cleanup for paper-methodology integrity" not "production hardening" → done.
   - W-8: F-21 has higher paper impact → queued for iteration 5.
7. **WP-CORE-5b shipped clean.** Spec v2 → RED commit `cc82e64` (6 RED-by-design failures, all predicted correctly) → GREEN commit `27a5d98` (338 passing, zero regression) → DOC + PLANNING commits landing now.

### WP-CORE-5b — SHIPPED (F-14 taxonomy preservation)

- Spec v2: `docs/superpowers/specs/2026-05-21-wp-core-5b-synthesizer-empty-model-policy-design.md` (Codex xhigh reviewed; 6 WARN + 3 NITS + 3 OQ all handled inline — zero deferred this round).
- Plan: `docs/superpowers/plans/2026-05-21-wp-core-5b-synthesizer-empty-model-policy.md`.
- Dev doc: `development_docs/WP-CORE-5b-synthesizer-empty-model-policy.md`.
- Production code:
  - `core/orchestration/errors.py` — `SynthesizerEmptyModelError` now carries `srs_path` (default `"<unknown>"`) + class docstring + updated default message format.
  - `core/orchestration/pipeline.py` — `run_pipeline` widened with `srs_path: Optional[str] = None`; pre-call guard added (catches `refined_specialist == []` for initial-empty + refiner-shrink-success paths per Codex W-1); post-call boundary check retained as belt-and-suspenders for injected synthesizers (Codex W-3); message includes "bypassed Pydantic" to distinguish layers.
  - `core/architect.py` — `analyze_document` threads `srs_path=self._current_srs_path` to `run_pipeline` (uses WP-CORE-4 invariant).
- Tests: 6 new (4 T-EMPTY-1..T-EMPTY-4 in `tests/test_pipeline_orchestration.py` + 2 T-EMPTY-5..T-EMPTY-6 in `tests/test_synthesizer_empty_model_error.py`).
- Net diff: 3 files, +72 / -6 LOC (~50 of the additions are doc comments; code-only ~22 LOC).

---

## Backlog state (`.planning/pipeline_audit/improvements_backlog.md`)

### Ingestion layer (10 findings — unchanged from iteration 3)

| status | finding | severity | component |
|---|---|---|---|
| OPEN | F-1 | MAJOR | document_parser_readers.py — `read_pdf` no defensive handling for encrypted/image-only/malformed PDFs |
| OPEN | F-2 | MAJOR | document_parser_readers.py — `read_txt` silently emits binary garbage under cp1254 |
| SHIPPED (daefeb0) | F-3 | MAJOR | document_parser.py + main.py — empty-input contract + 6 callsite migration + latent post-loop guard fix |
| OPEN | F-4 | MAJOR (uncertain) | document_parser.py — TOC heuristic 120-line + cluster<2 |
| SHIPPED (25e6880) | F-5 | MAJOR | document_parser.py — Kaynaklar parity + position guard |
| OPEN | F-6 | MINOR | document_parser.py — `_should_merge` quote/bracket terminator gap |
| OPEN | F-7 | MINOR | document_parser_readers.py — DOCX zero try/except |
| OPEN | F-8 | MINOR (uncertain) | document_parser_readers.py — XXE / external-entity hardening |
| OPEN | F-9 | MINOR | both — zero logging anywhere |
| OPEN | F-10 | TRIVIAL | main.py:366,480 — same SRS parsed twice per `/generate-model` |

### Orchestrator layer (11 findings)

| status | finding | severity | component |
|---|---|---|---|
| OPEN (DORMANT) | F-11 | MAJOR | core/architect.py — parallel Scout rate-limit reentrancy race (parallel Scout path dead from production; reopen when rewired) |
| OPEN | F-12 | MINOR | core/architect.py — Specialist shape-error retry token tracking gap |
| SHIPPED (02e0fe9) | F-13 | MAJOR | core/architect.py + core/orchestration/errors.py + main.py — `_save_intermediate` silent swallow + `_current_srs_path` anomaly fold-in |
| SHIPPED (27a5d98) | F-14 | MAJOR | core/orchestration/errors.py + pipeline.py + architect.py — `SynthesizerEmptyModelError` taxonomy preservation; pre-call guard + post-call belt-and-suspenders + `srs_path` symmetry |
| OPEN | F-15 | MINOR | core/orchestration/pipeline.py — Refiner exhaustion fallback observability |
| OPEN | F-16 | TRIVIAL | core/architect.py — dead `_split_text_into_chunks` |
| OPEN | F-17 | MINOR | core/architect.py — stage_config validation deferred to first LLM call |
| OPEN | F-18 | MINOR | core/architect.py — synthetic context descriptions in intermediate JSON |
| OPEN | F-19 | MINOR | core/architect.py — exponential backoff comment off |
| OPEN | F-20 | MINOR (downgraded) | core/architect.py + core/token_tracker.py — thread-safety docs gap |
| OPEN | F-21 | MAJOR | core/architect.py + core/verifier/checks_deterministic.py — D1 verifier passes vacuously (recommended next iteration) |

**Total OPEN MAJOR (live): 5** (F-1, F-2, F-4-uncertain, F-21) + **1 DORMANT** (F-11). F-21 is the highest-paper-impact remaining MAJOR per Codex W-8.

---

## Component catalog state (unchanged from iteration 3)

- Priority 1 (ingestion): `document_parser.py` + `document_parser_readers.py` — **DONE** (10 findings; 2 shipped, 4 OPEN MAJOR, 3 OPEN MINOR, 1 OPEN TRIVIAL).
- Priority 2 (orchestrator): `core/architect.py` + `core/orchestration/pipeline.py` + `core/orchestration/errors.py` — **DONE** (11 findings; 2 shipped, 1 OPEN MAJOR live (F-21), 1 OPEN MAJOR dormant (F-11), 6 OPEN MINOR, 1 OPEN TRIVIAL).
- Priorities 3–12 — all PENDING.

---

## Recommended next iteration (iteration 5)

**Codex W-8 priority bump**: F-21 (vacuous D1 verifier pass) has clearer paper impact than any remaining MAJOR-OPEN. The D1 deterministic verifier check has been passing vacuously for every project run in history because `ContextHypothesis.supporting_sentence_ids` is always `[]` (Architect never populates it). EMSE methodology paper claims D1 catches sentence-grounding violations — that claim is currently empirically vacuous.

### Option A (STRONGLY RECOMMENDED): F-21 vacuous D1 verifier

- **Effort:** M (Architect prompt + parsing + cross-stage data flow).
- **Blast radius:** PIPELINE (EMSE methodology validity — D1 deterministic check is dead code in current state; every project run passes a check it should fail).
- **Why now:** Codex W-8 explicitly flagged this as higher-priority than F-14 was; the gap affects every run, not a dormant edge. Architect prompt + parsing change is well-scoped; downstream `check_d1_supporting_sentence_ids_subset` doesn't need changes (already correctly implemented for the case when IDs ARE populated).
- **Risk:** changes the Architect LLM prompt — model behavior under the new prompt needs validation against a real SRS run, not just unit tests. Mitigate by including a live D1 SRS E2E test in the GREEN gate.
- **Loop-coherence:** continues priority-2 audit work; no new audit walk required (F-21 was discovered during WP-CORE-4 spec drafting; details cache-hot in `findings/architect.md`).

### Alternative: pivot to priority-3 audit walk

If F-21's LLM-behavior risk feels too high, pivot to a new close-lookup of `core/synthesizer/*` or `core/verifier/*` to surface fresh findings. Priority-3 components are PENDING in component catalog.

**Coordinator recommendation for iteration 5:** **Option A (F-21)**. Per Codex W-8, this is the highest-impact remaining MAJOR. Effort estimate M aligns with WP-CORE-3 / WP-CORE-4 cadence.

---

## Loop ritual reminder for the next coordinator

1. Read CLAUDE.md, AGENTS.md (full, fresh).
2. `git log --oneline -8` + `git status --short`.
3. Read this handoff + `.planning/pipeline_audit/CURRENT.md`.
4. Confirm baseline:

   ```bash
   cd extension/backend
   /Library/Frameworks/Python.framework/Versions/3.13/bin/python3 -m pytest -m "not integration" --tb=no -q 2>&1 | tail -3
   ```
   Must be `338 passed, 31 deselected`. If not, STOP — baseline broken before you started.
5. Choose iteration target per §"Recommended next iteration" (F-21 strongly recommended).
6. For F-21: spec → Codex xhigh review → plan → SDD execute → dev_doc → state update. No new audit walk required (F-21 details cache-hot in `findings/architect.md`).
7. Each commit gated on pytest ≥ 338. No `--no-verify`. No silent fallbacks.
8. Include a live D1 SRS E2E test in F-21 GREEN gate to validate LLM-behavior under the new prompt.

---

## Non-negotiables carried forward

- Baseline pytest never drops.
- Smallest correct change; no overengineering.
- Each WP has spec + Codex review + plan + dev_doc (Codex review may be waived for pure-cosmetic WPs; document in decision_log).
- Atomic Conventional Commits + Claude trailer.
- No `git push` without explicit user "push it."
- Caveman mode active (technical preserve, fluff drop) for terminal chatter; code/commits/PRs/security stay normal.
- Turkish ↔ user, English ↔ code/commits.
- RQ5 deleted per D6; do not reference.
- No backwards-compat shims; no silent fallbacks.
- WP-CORE-4 invariant: any future stage retry wrapper using `_save_intermediate` MUST include `except IntermediateSaveError: raise` before generic `except Exception`. Pattern lives in `identify_contexts:487-490`.
- WP-CORE-5b invariant: any future pipeline-orchestration code that constructs `SynthesizerEmptyModelError` MUST pass `srs_path` (default `"<unknown>"`). The error class signature requires it for diagnostic clarity per WP-CORE-4 symmetry.

---

## Open follow-ups from iteration 4

- **F-21 elevated to top priority** by Codex W-8 review. Iteration 5 target.
- **F-11 stays DORMANT.** Will reopen when `extract_domain_sentences` is rewired into `analyze_document.scout_fn` or `section_aware_chunks` gains an LLM call. See `decision_log.md` D-CODEX-REVIEW-WP-CORE-5.
- **Codex review framing precedent.** Two consecutive WPs (5 and 5b) had their audit text overhauled by Codex review — F-11 audit was correct mechanism but wrong production reachability; F-14 audit was reverse-engineered policy gap that didn't exist. **Lesson:** audit text is a starting hypothesis; spec drafting must re-verify production reachability via close-lookup of every code path the finding names. Add to loop discipline: spec §Motivation must include explicit "production reachability" subsection.
- **Pattern reinforced: TDD with genuine-RED-fail.** WP-CORE-5b RED commit accepted 6 known-failing tests (5 GREEN-required + 1 regression-lock). GREEN turned all 6 green with zero regression. Codex OQ-5 agreed this is honest TDD vs the per-test-import skip workaround from WP-CORE-3/4. Both patterns are valid; choose based on whether the missing symbol breaks test collection.
- **Pattern reinforced: layer cake defense.** WP-CORE-5b ships three layers of empty-model defense at three different scopes (pre-call DI / Pydantic schema / post-call injected-bypass). Each layer has a single clear purpose. Pattern for future contract-cleanup WPs.
- `extension/backend/core/intermediate/*.json`, `validation_metrics_report.json`, `domain/model.PRE-FRESH-RUN-BACKUP.json` remain untracked runtime artifacts; loop did not touch.
- `AGENTS.md` shows as `M` (auto-memory hook); leave alone.
- `core/AST/intermediate/ast_signals_diagnostics.json` shows as `M` (runtime artifact); leave alone.
- Local `.venv` still broken per CLAUDE.md; CI uses 3.12; loop runs 3.13 from `/Library/Frameworks/...`.

---

## Files this iteration produced

**Source/test:**
- `extension/backend/core/orchestration/errors.py` (modified, 27a5d98 — `SynthesizerEmptyModelError` carries `srs_path` + class docstring)
- `extension/backend/core/orchestration/pipeline.py` (modified, 27a5d98 — `run_pipeline` widened with `srs_path` kwarg; pre-call guard added; post-call boundary check rewritten)
- `extension/backend/core/architect.py` (modified, 27a5d98 — `analyze_document` threads `srs_path=self._current_srs_path`)
- `extension/backend/tests/test_pipeline_orchestration.py` (modified, cc82e64 — T-EMPTY-1..T-EMPTY-4 appended)
- `extension/backend/tests/test_synthesizer_empty_model_error.py` (modified, cc82e64 — T-EMPTY-5 + T-EMPTY-6 appended)

**Persistent dev memory:**
- `development_docs/WP-CORE-5b-synthesizer-empty-model-policy.md` (created, this commit)
- `development_docs/INDEX.md` (modified, this commit — ACTIVE row #6)

**Audit state:**
- `.planning/pipeline_audit/CURRENT.md` (modified, this commit)
- `.planning/pipeline_audit/improvements_backlog.md` (modified, this commit — F-14 → SHIPPED row; F-11 → OPEN (DORMANT); summary updated to iteration-4)
- `.planning/pipeline_audit/decision_log.md` (already appended D-CODEX-REVIEW-WP-CORE-5 + D-PICK-WP-CORE-5b + D-CODEX-REVIEW-WP-CORE-5b)
- `.planning/pipeline_audit/findings/architect.md` (modified earlier this iteration — §F-11 DORMANT note; §F-14 SHIPPED status note + reframe)
- `.planning/pipeline_audit/handoff-2026-05-21-1145.md` (this file, this commit)

**Planning artifacts:**
- `docs/superpowers/specs/2026-05-21-wp-core-5-parallel-scout-rate-limit-design.md` (ABANDONED banner; preserved for audit trail; landed in next commit)
- `docs/superpowers/specs/2026-05-21-wp-core-5b-synthesizer-empty-model-policy-design.md` (v2, post-Codex; landed in next commit)
- `docs/superpowers/plans/2026-05-21-wp-core-5b-synthesizer-empty-model-policy.md` (landed in next commit)

---

## Goal-backward verification (iteration-level)

| Iteration-4 goal | Evidence |
|---|---|
| Pick a MAJOR finding per iteration-3 handoff (F-11 recommended) | F-11 picked → abandoned after Codex review surfaced 3 CRITICALs (dormant production path + wrong invariant) → pivoted to F-14 → WP-CORE-5b shipped. |
| Each commit gated on pytest ≥ baseline (RED commit accepts known-failing tests by design) | RED `cc82e64`: 6 failed + 332 passed = 338 collected (predicted exactly); GREEN `27a5d98`: 338 passed, 0 failed, 0 regressed; artifact commits: 338 passed. |
| Spec → Codex xhigh review → plan → SDD → dev_doc → state update | All 5 phases produced artifacts for WP-CORE-5b; WP-CORE-5 produced spec v1 + Codex review + abandon decision (no plan/SDD/dev_doc because abandoned). |
| No `git push` | Confirmed; main remains local. |
| Atomic Conventional Commits + Claude trailer | All commits conform. |
| Zero deferred WARNs (matching iteration-2 + iteration-3 standard) | Confirmed for WP-CORE-5b (third consecutive zero-deferred iteration). WP-CORE-5 was scope-abandoned, not "deferred WARNs" — its 3 CRITICAL findings were resolved by abandoning the WP entirely (correct disposition for wrong-scope work). |

Iteration 4 closed clean (despite the pivot). Two consecutive WPs surfaced audit-text inaccuracies during Codex review; the "production reachability" lesson is now part of loop discipline for iteration 5+.
