# Handoff — Domain Pipeline Hardening Loop (iteration 6 → 7)

**Written:** 2026-05-23 12:45 GMT+3
**Reason:** Iteration 6 SHIPPED — WP-CORE-7 (F-22 Refiner stage-aware re-runs + ArchitectGroundingError) closed end-to-end. Autonomous loop continues.

---

## State of the loop

### Baseline (sacred)

```
pytest -m "not integration" → 358 passed, 31 deselected
Pre-WP-CORE-7 baseline was 348. Net +10 tests, zero regression.
Cumulative: pre-loop 272 → post-iteration-6 358 (+86 tests over 6 iterations).
```

### HEAD at handoff (after DOC commit)

```
{pending}  chore(planning): WP-CORE-7 spec v2 + plan into git history
{this}     chore(artifacts): WP-CORE-7 dev_doc + audit state update + F-22 SHIPPED + F-23/24 NEW
ce56d99    fix(orchestration, architect, refiner): WP-CORE-7 Refiner stage-aware dispatch + ArchitectGroundingError
aea15e4    test(orchestration, architect): WP-CORE-7 red-phase tests
4c8580c    chore(planning): WP-CORE-6 spec v2 + plan ← pre-iteration-6 HEAD
```

4 commits on main during iteration 6 (RED + GREEN + DOC + PLANNING). **NOT pushed.** Loop rule: only push on explicit user "push it".

### Iteration 6 — what actually happened

1. **Initial pick:** F-22 per WP-CORE-6 handoff Codex W-4 priority. LIVE bug — every run with architect-stage ERRORs degraded silently.
2. **User chose mode C hybrid** via AskUserQuestion (1 architect re-run with feedback + hard-fail).
3. **Spec v1 → Codex xhigh review** surfaced 2 CRITICAL + 6 WARN + 2 NIT + 1 OQ. Key findings:
   - **C-1**: contract `VerifierIssue` has no `stage` field; `_to_contract_issue` adapter drops legacy `.stage`. D-5's `i.stage == "architect"` would AttributeError at runtime.
   - **C-2**: dispatcher should partition BEFORE entering specialist refine to avoid 2 wasted specialist re-runs.
   - **W-4**: `ArchitectGroundingError` payload should also carry non-architect issues for post-mortem visibility.
   - **W-5**: bare `except Exception` in `pipeline.py:98-112` contradicts explicit-failure mandate.
   - **W-6**: spec claim "existing PipelineError handlers in main.py catch it" was wrong — main.py uses bare Exception everywhere.
4. **Spec v2** adopted all CRITICAL+WARN inline:
   - `_issue_stage(issue)` helper derives stage from `target` prefix (avoids 13-callsite schema migration).
   - Pre-check verifier ONCE + thread result into `refine_until_clean(initial_result=...)` for common-path efficiency.
   - `ArchitectGroundingError` gains `residual_issues` parameter.
   - Bare `except Exception` narrowed to `except RefinementExhaustedError`.
   - **NEW backlog**: F-23 (typed `PipelineError` handler in main.py), F-24 (`srs_path` in `VerifierIssue` — A6-srs-path unlocked).
5. **WP-CORE-7 shipped clean.** RED `aea15e4` (10 RED-by-design failures — all by ImportError/TypeError in test body, no collection errors per W-2). GREEN `ce56d99` (358 passing, zero regression).

### WP-CORE-7 — SHIPPED (F-22 mode C hybrid)

- Spec v2: `docs/superpowers/specs/2026-05-22-wp-core-7-refiner-stage-aware-design.md`
- Plan: `docs/superpowers/plans/2026-05-22-wp-core-7-refiner-stage-aware.md` (pending PLANNING commit)
- Dev doc: `development_docs/WP-CORE-7-refiner-stage-aware.md`
- Production code:
  - `core/orchestration/errors.py` — `ArchitectGroundingError(PipelineError)` class
  - `core/orchestration/__init__.py` — re-export
  - `core/orchestration/pipeline.py` — `_issue_stage` + log helpers + `PipelineDeps.architect_with_feedback` + outer architect-refine loop + pre-check + narrowed exception catch
  - `core/refiner/loop.py` — optional `initial_result=` kwarg
  - `core/architect.py` — `_build_grounding_feedback_block` + `identify_contexts(feedback_issues=...)` + `architect_with_feedback_fn` closure
- Tests: 10 new (T-AGE-1, T-DISPATCH-1..5, T-FEEDBACK-1, T-LOG-1, T-LOG-2, T-INT-1) + 1 modified (T-DEGRADE-LOG-1 flip) across 4 files (1 NEW: `test_analyze_document_e2e.py`).
- Fixture updates: `_make_typed_deps` + 2 direct `PipelineDeps` construction sites widened with `architect_with_feedback`.
- WP-CORE-6 happy-path E2E (`test_architect_id_propagation.py`) updated: supporting_sentence_ids `[0, 1]` → `[0]` (Scout emits only index 0 for the two-sentence test input).
- Net diff: 7 production/test files in GREEN; 4 test files in RED.

---

## Backlog state (`.planning/pipeline_audit/improvements_backlog.md`)

### Ingestion layer (10 findings — unchanged from iteration 5)

| status | finding | severity | component |
|---|---|---|---|
| OPEN | F-1 | MAJOR | document_parser_readers.py — `read_pdf` no defensive handling |
| OPEN | F-2 | MAJOR | document_parser_readers.py — `read_txt` cp1254 silent binary garbage |
| SHIPPED | F-3 | MAJOR | empty-input contract + 6 callsite migration |
| OPEN | F-4 | MAJOR (uncertain) | TOC heuristic 120-line + cluster<2 |
| SHIPPED | F-5 | MAJOR | Kaynaklar parity + position guard |
| OPEN | F-6 | MINOR | `_should_merge` quote/bracket terminator gap |
| OPEN | F-7 | MINOR | DOCX zero try/except |
| OPEN | F-8 | MINOR (uncertain) | XXE / external-entity hardening |
| OPEN | F-9 | MINOR | zero logging anywhere |
| OPEN | F-10 | TRIVIAL | same SRS parsed twice per /generate-model |

### Orchestrator layer (14 findings)

| status | finding | severity | component |
|---|---|---|---|
| OPEN (DORMANT) | F-11 | MAJOR | parallel Scout rate-limit race (Scout path dead from prod) |
| OPEN | F-12 | MINOR | Specialist shape-error retry token tracking gap |
| SHIPPED | F-13 | MAJOR | _save_intermediate silent swallow + _current_srs_path |
| SHIPPED | F-14 | MAJOR | SynthesizerEmptyModelError taxonomy preservation |
| OPEN | F-15 | MINOR (downgraded TRIVIAL post-WP-CORE-7) | Refiner exhaustion observability — closed for architect path; specialist path retains degrade |
| OPEN | F-16 | TRIVIAL | dead `_split_text_into_chunks` |
| OPEN | F-17 | MINOR | stage_config validation deferred to first LLM call |
| OPEN | F-18 | MINOR | synthetic context descriptions in intermediate JSON |
| OPEN | F-19 | MINOR | exponential backoff comment off |
| OPEN | F-20 | MINOR (downgraded) | thread-safety docs gap |
| SHIPPED | F-21 | MAJOR | D1 verifier non-vacuous (Architect populates supporting_sentence_ids E2E) |
| SHIPPED | F-22 | MAJOR | Refiner stage-aware re-runs + ArchitectGroundingError (mode C hybrid) |
| **OPEN (NEW)** | **F-23** | **MAJOR** | **main.py typed PipelineError handler. WP-CORE-7 Codex W-6: 10 bare-Exception catches in main.py; ArchitectGroundingError serialized via str(e) only. Small scope (~2 endpoints).** |
| **OPEN (NEW)** | **F-24** | **MINOR** | **srs_path in VerifierIssue schema. WP-CORE-6 A6-srs-path OQ now unlocked post-F-22. Migration: schema widen + 13 callsites.** |

**Total OPEN MAJOR (live): 4** (F-1, F-2, F-4-uncertain, F-23) + **1 DORMANT** (F-11).

---

## Recommended next iteration (iteration 7)

### Option A (RECOMMENDED): F-23 main.py typed PipelineError handler

- **Effort:** S. Add `except PipelineError as e: ...` block before each bare-Exception catch in `/generate-model` (`:533`) and `/validate` (`:427`) endpoints. Return structured JSON `{type: type(e).__name__, srs_path: getattr(e, 'srs_path', None), details: str(e)}` so FastAPI clients can branch on failure mode.
- **Blast radius:** LOCAL (main.py only). No state change in production runs except response shape on failure paths.
- **Why now:** completes WP-CORE-7 enforcement story by surfacing typed run-manifest signal at FastAPI response boundary. Codex W-6 direct lineage.
- **Risk:** low. Existing bare-Exception catch remains as fallback. Test fixture for endpoint contract may need a small mock.

### Option B: F-1 PDF defensive handling (ingestion-layer pivot)

- **Effort:** M. `read_pdf` in `core/document_parser_readers.py` currently has no defensive handling for corrupted PDFs, password-protected PDFs, or empty PDFs (post-F-3, only empty-text guard exists).
- **Blast radius:** PIPELINE. Affects ingestion stage; surface is /generate-model first-call failure mode.
- **Why now:** 5 consecutive orchestrator-layer iterations (CORE-3 onward); pivot refreshes audit lens. F-1 is the highest-severity ingestion MAJOR-OPEN.

### Option C: F-2 cp1254 silent binary garbage

- **Effort:** S-M. `read_txt` defaults to cp1254 encoding; on non-Turkish UTF-8 files, silently produces binary garbage strings that propagate through Scout. Fix: detect encoding via chardet or magic-bytes, or raise `EncodingError` on cp1254-decoded garbage.

### Option D: F-24 srs_path in VerifierIssue (cap on WP-CORE-7)

- **Effort:** M (13-callsite migration). Adds run-manifest issue-level provenance. Lower priority than F-23.

**Coordinator recommendation for iteration 7:** **Option A (F-23)**. Smallest scope; directly continues WP-CORE-7 lineage; closes the run-manifest signal at the response boundary. Then pivot ingestion (F-1 or F-2) for iteration 8.

---

## Loop ritual reminder for the next coordinator

1. Read CLAUDE.md, AGENTS.md (full, fresh).
2. `git log --oneline -8` + `git status --short`.
3. Read this handoff + `.planning/pipeline_audit/CURRENT.md`.
4. Confirm baseline:

   ```bash
   cd extension/backend
   /Library/Frameworks/Python.framework/Versions/3.13/bin/python3 -m pytest -m "not integration" --tb=no -q 2>&1 | tail -3
   ```
   Must be `358 passed, 31 deselected`. If not, STOP — baseline broken before you started.
5. Choose iteration target per §"Recommended next iteration" (F-23 recommended).
6. **Production reachability subsection mandatory in spec §Motivation** — loop discipline lesson from iteration 4 (F-11 dormant scope miss).
7. Each commit gated on pytest ≥ 358. No `--no-verify`. No silent fallbacks.

---

## Non-negotiables carried forward

- Baseline pytest never drops.
- Smallest correct change; no overengineering.
- Each WP has spec + Codex review + plan + dev_doc.
- Atomic Conventional Commits + Claude trailer.
- No `git push` without explicit user "push it."
- Caveman mode active (technical preserve, fluff drop) for terminal chatter; code/commits/PRs/security stay normal.
- Turkish ↔ user, English ↔ code/commits.
- RQ5 deleted per D6; do not reference.
- No backwards-compat shims; no silent fallbacks.
- WP-CORE-4 invariant: any future stage retry wrapper using `_save_intermediate` MUST include `except IntermediateSaveError: raise` before generic `except Exception`.
- WP-CORE-5b invariant: any future pipeline-orchestration code constructing `SynthesizerEmptyModelError` MUST pass `srs_path` (default `"<unknown>"`).
- WP-CORE-6 invariant: any future Architect stage that produces context proposals MUST include `supporting_sentence_ids`. `extract_per_context_details` signature is `List[ContextHypothesis]`, NOT `List[str]`.
- **WP-CORE-7 invariant (NEW)**: every `VerifierIssue.target` (or legacy `.location`) MUST be prefixed with `'{stage}:'` (`scout:`, `architect:`, `specialist:`, `synthesizer:`). `_issue_stage` dispatch in `core/orchestration/pipeline.py` depends on this prefix.
- **WP-CORE-7 invariant (NEW)**: every constructor of `PipelineDeps` MUST supply `architect_with_feedback`. Production callsite at `architect.py:945`; 3 test fixture sites updated.
- **WP-CORE-7 invariant (NEW)**: architect-stage D1 ERRORs raise `ArchitectGroundingError` after 1 feedback rerun (mode C hybrid). Pipeline does NOT degrade silently on persistent architect-stage failure.

---

## Open follow-ups from iteration 6

- **F-23 NEW (MAJOR)** = WP-CORE-7 Codex W-6 backlog. Iteration 7 target.
- **F-24 NEW (MINOR)** = A6-srs-path unlocked post-F-22. Iteration 8+ candidate.
- **F-15 downgraded to TRIVIAL** — Refiner exhaustion observability closed for architect path; specialist path retains existing degrade-log per WP-CORE-6 C-4 contract.
- **F-11 stays DORMANT.** Same as iteration-5 handoff.
- **5-iteration orchestrator-layer streak** (CORE-3 ingestion → CORE-4/5b/6/7 orchestrator). Consider ingestion pivot (F-1 / F-2) for iteration 8 if F-23 ships fast.
- **EMSE paper Methods section needs update.** Post-WP-CORE-7: "D1 ERRORs trigger one issue-aware re-prompt of Architect; persistent grounding violations raise `ArchitectGroundingError` and fail the run. Run manifest captures issue list on both hard-fail and degrade paths." Flag for advisor (Hoca) at next review.
- **Pattern reinforced: layer-cake defense at three scopes.** WP-CORE-7 has three defense layers (Architect feedback re-prompt, dispatcher hard-fail, narrowed `except RefinementExhaustedError`). Pattern matches WP-CORE-5b + WP-CORE-6.
- **Pattern reinforced: Codex C-1 schema-vs-helper trade-off.** Twice now (WP-CORE-6 C-1 list-vs-ContextHypothesis, WP-CORE-7 C-1 stage-field-vs-target-prefix-helper) the cheaper helper-based fix was chosen over schema widening to avoid multi-callsite migration. Documented as a loop heuristic.

---

## Files this iteration produced

**Source/test:**
- `extension/backend/core/orchestration/errors.py` — modified (ce56d99) — ArchitectGroundingError class
- `extension/backend/core/orchestration/__init__.py` — modified (ce56d99) — re-export
- `extension/backend/core/orchestration/pipeline.py` — modified (ce56d99) — dispatcher + helpers + PipelineDeps widening
- `extension/backend/core/refiner/loop.py` — modified (ce56d99) — initial_result kwarg
- `extension/backend/core/architect.py` — modified (ce56d99) — feedback block + identify_contexts kwarg + architect_with_feedback_fn closure + PipelineDeps construction
- `extension/backend/tests/test_orchestration_errors.py` — modified (aea15e4) — T-AGE-1
- `extension/backend/tests/test_pipeline_orchestration.py` — modified (aea15e4 RED + ce56d99 fixture updates) — T-DISPATCH-1..5, T-LOG-1, T-LOG-2, T-DEGRADE-LOG-1 modified
- `extension/backend/tests/test_architect_identify_contexts.py` — modified (aea15e4) — T-FEEDBACK-1
- `extension/backend/tests/test_analyze_document_e2e.py` — created (aea15e4) — T-INT-1
- `extension/backend/tests/test_architect_id_propagation.py` — modified (ce56d99) — supporting_sentence_ids [0, 1] → [0] for two-sentence test input

**Persistent dev memory:**
- `development_docs/WP-CORE-7-refiner-stage-aware.md` — created (this commit)
- `development_docs/INDEX.md` — modified (this commit, ACTIVE row #8)

**Audit state:**
- `.planning/pipeline_audit/CURRENT.md` — modified (this commit)
- `.planning/pipeline_audit/improvements_backlog.md` — modified (this commit; F-22 → SHIPPED; F-23 + F-24 NEW; F-15 downgraded)
- `.planning/pipeline_audit/decision_log.md` — modified (this commit; D-PICK-WP-CORE-7 + D-CODEX-REVIEW-WP-CORE-7 appended)
- `.planning/pipeline_audit/findings/architect.md` — modified (this commit; §F-22 SHIPPED status added)
- `.planning/pipeline_audit/handoff-2026-05-23-1245.md` — this file

**Planning artifacts (PLANNING commit pending):**
- `docs/superpowers/specs/2026-05-22-wp-core-7-refiner-stage-aware-design.md` — v2 (created this iteration)
- `docs/superpowers/plans/2026-05-22-wp-core-7-refiner-stage-aware.md` — to be added in PLANNING commit

---

## Goal-backward verification (iteration-level)

| Iteration-6 goal | Evidence |
|---|---|
| Pick F-22 per WP-CORE-6 handoff Codex W-4 | F-22 picked; mode C hybrid chosen via AskUserQuestion. |
| Spec → Codex xhigh review → plan → SDD → dev_doc → state update | Spec v1 → review (2 CRITICAL + 6 WARN + 2 NIT + 1 OQ) → v2 → plan → RED + GREEN → dev_doc + state. |
| Each commit gated on pytest ≥ baseline (RED accepts known-failing tests) | RED `aea15e4`: 348 passed + 10 failed = 358 collected; GREEN `ce56d99`: 358 passed, 0 failed, 0 regression. |
| No `git push` | Confirmed; main remains local. |
| Atomic Conventional Commits + Claude trailer | All commits conform. |
| Codex CRITICAL+WARN handled inline | YES — C-1 (stage-derive helper), C-2 (pre-check + initial_result), W-1 (T-DISPATCH-5), W-2 (RED math + import guard), W-3 (T-FEEDBACK-1 exact format), W-4 (residual_issues), W-5 (narrowed catch), W-6 (main.py correction + F-23 backlog). |
| Codex NIT handled inline | YES — N-1 (per-attempt feedback), N-2 (OQ-4 rewording). |
| Codex OQ tracked | YES — OQ-1 (A6-srs-path) → F-24 backlog entry. |
| Production reachability subsection in spec | YES — §Motivation explicitly notes F-22 LIVE post-WP-CORE-6. |

Iteration 6 closed clean. Iteration 7 target: F-23 (small scope; continues WP-CORE-7 lineage).
