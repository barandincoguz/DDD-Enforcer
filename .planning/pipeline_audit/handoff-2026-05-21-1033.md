# Handoff — Domain Pipeline Hardening Loop (iteration 3 → 4)

**Written:** 2026-05-21 10:33 GMT+3
**Reason:** Iteration 3 (WP-CORE-4) fully shipped; natural breakpoint before iteration 4.

---

## State of the loop

### Baseline (sacred)

```
pytest -m "not integration" → 332 passed, 31 deselected
Pre-WP-CORE-4 baseline was 321.  Net +11 tests, zero regression.
Cumulative: pre-loop 272 → post-iteration-3 332 (+60 tests over 3 iterations).
```

### HEAD at handoff (anticipated after DOC + planning commits)

```
{pending}  chore(planning): WP-CORE-4 spec v2 + plan into git history     ← will be top after commits
{pending}  chore(artifacts): WP-CORE-4 dev_doc + audit state update
02e0fe9    fix(architect, main, orchestration): WP-CORE-4 IntermediateSaveError + srs_path propagation
0023fa2    test(architect, orchestration): WP-CORE-4 red-phase tests for save observability + srs_path propagation
d7dc188    chore(planning): WP-CORE-3 iteration 2 handoff + CURRENT pointer   ← pre-iteration-3 HEAD
```

4 new commits on main during iteration 3 (2 production + 2 planning/artifacts). **NOT pushed.** Loop rule: only push on explicit user "push it."

### WP-CORE-4 — SHIPPED (F-13 + `_current_srs_path` anomaly fold-in)

- Spec v2: `docs/superpowers/specs/2026-05-21-wp-core-4-intermediate-save-observability-design.md` (Codex xhigh reviewed; 2 CRITICAL + 5 WARN + 3 NITS + 7 OQ all handled inline — zero deferred this round).
- Plan: `docs/superpowers/plans/2026-05-21-wp-core-4-intermediate-save-observability.md`.
- Dev doc: `development_docs/WP-CORE-4-intermediate-save-observability.md`.
- Production code:
  - `core/orchestration/errors.py` — new `IntermediateSaveError(PipelineError)` class carrying stage/filepath/cause/srs_path.
  - `core/architect.py` — `_save_intermediate` rewritten to raise on `(OSError, TypeError, ValueError)`; `identify_contexts` retry handler gains `except IntermediateSaveError: raise` guard (Codex C-1); `__init__` initializes `self._current_srs_path = "<unknown>"`; `analyze_document(text, srs_path: Optional[str] = None)` widens signature + unconditional assignment per W-2.
  - `main.py` — 3 callsites pass srs_path (lifespan: single path; both batch endpoints: `"; "`-joined).
- Tests: 11 new (5 T-SAVE-* in new `tests/test_intermediate_save.py` + 4 T-SRS-* in new `tests/test_architect_srs_path.py` + 2 T-WIRE-MAIN-* appended to `tests/test_main_wiring.py`).
- New finding discovered during spec drafting: **F-21** (MAJOR) — `ContextHypothesis.supporting_sentence_ids` always defaulted to `[]` because Architect never populates it; D1 verifier has passed vacuously for every run in project history. Added to `findings/architect.md` + backlog, deferred to WP-CORE-5+ (requires Architect prompt + parsing change, out of WP-CORE-4 envelope).
- Downgrade: F-20 reclassified MAJOR (uncertain) → MINOR after verification that `core/token_tracker.py:44+97` already has `threading.Lock`-protected mutations. F-20 is now a documentation gap, not a thread-safety bug.

---

## Backlog state (`.planning/pipeline_audit/improvements_backlog.md`)

### Ingestion layer (10 findings)

| status | finding | severity | component |
|---|---|---|---|
| OPEN | F-1 | MAJOR | document_parser_readers.py — `read_pdf` no defensive handling for encrypted/image-only/malformed PDFs |
| OPEN | F-2 | MAJOR | document_parser_readers.py — `read_txt` silently emits binary garbage under cp1254 |
| SHIPPED (daefeb0) | F-3 | MAJOR | document_parser.py + main.py — empty-input contract + 6 callsite migration + latent post-loop guard fix |
| OPEN | F-4 | MAJOR (uncertain) | document_parser.py — TOC heuristic 120-line + cluster<2 |
| SHIPPED (25e6880) | F-5 | MAJOR | document_parser.py — Kaynaklar parity + position guard |
| OPEN | F-6 | MINOR | document_parser.py — `_should_merge` quote/bracket terminator gap |
| OPEN | F-7 | MINOR | document_parser_readers.py — DOCX zero try/except |
| OPEN | F-8 | MINOR (uncertain) | document_parser_readers.py — XXE / external-entity hardening |
| OPEN | F-9 | MINOR | both — zero logging anywhere |
| OPEN | F-10 | TRIVIAL | main.py:366,480 — same SRS parsed twice per `/generate-model` |

### Orchestrator layer (11 findings)

| status | finding | severity | component |
|---|---|---|---|
| OPEN | F-11 | MAJOR | core/architect.py — parallel Scout rate-limit reentrancy race |
| OPEN | F-12 | MINOR | core/architect.py — Specialist shape-error retry token tracking gap |
| SHIPPED (02e0fe9) | F-13 | MAJOR | core/architect.py + core/orchestration/errors.py + main.py — `_save_intermediate` silent swallow + `_current_srs_path` anomaly fold-in |
| OPEN | F-14 | MAJOR | core/architect.py + core/orchestration/pipeline.py — `SynthesizerEmptyModelError` escapes pipeline |
| OPEN | F-15 | MINOR | core/orchestration/pipeline.py — Refiner exhaustion fallback observability |
| OPEN | F-16 | TRIVIAL | core/architect.py — dead `_split_text_into_chunks` |
| OPEN | F-17 | MINOR | core/architect.py — stage_config validation deferred to first LLM call |
| OPEN | F-18 | MINOR | core/architect.py — synthetic context descriptions in intermediate JSON |
| OPEN | F-19 | MINOR | core/architect.py — exponential backoff comment off |
| OPEN | F-20 | MINOR (downgraded) | core/architect.py + core/token_tracker.py — thread-safety docs gap |
| OPEN | F-21 | MAJOR | core/architect.py + core/verifier/checks_deterministic.py — D1 verifier passes vacuously (NEW from WP-CORE-4 spec drafting) |

**Total OPEN MAJOR: 7** (F-1, F-2, F-4-uncertain, F-11, F-14, F-21). F-21 is new this iteration.

---

## Component catalog state (`.planning/pipeline_audit/component_catalog.md`)

- Priority 1 (ingestion): `document_parser.py` + `document_parser_readers.py` — **DONE** (10 findings; 2 shipped, 4 OPEN MAJOR, 3 OPEN MINOR, 1 OPEN TRIVIAL).
- Priority 2 (orchestrator): `core/architect.py` (923 LOC, was 752 pre-loop) + `core/orchestration/pipeline.py` (84 LOC) + `core/orchestration/errors.py` (now ~100 LOC with new `IntermediateSaveError`) — **DONE** (11 findings; 1 shipped, 3 OPEN MAJOR, 6 OPEN MINOR, 1 OPEN TRIVIAL).
- Priorities 3–12 — all PENDING.

---

## Recommended next iteration (iteration 4)

Three viable candidates. Tradeoffs below.

### Option A (RECOMMENDED): F-11 parallel Scout rate-limit race

- **Effort:** M-L (concurrency redesign — replace lock+sleep with semaphore-based gating or condition variable).
- **Blast radius:** PIPELINE (quota loss under free-tier; EMSE reviewer-visible if methodology section claims `min_delay=6s` invariant).
- **Why now:** highest-impact remaining OPEN MAJOR. Codex already identified the specific failure mode in WP-CORE-4 audit. Smaller follow-ups (F-12, F-17, F-20-docs) can ride along if scope permits.
- **Risk:** concurrency bugs are hard to test deterministically. RED tests need to exercise interleaved thread execution — pattern from `tests/test_token_tracker_concurrency.py` (existing) is reusable.
- **Loop-coherence:** continues priority-2 audit work; no new audit walk required.

### Option B: F-14 SynthesizerEmptyModelError pipeline escape

- **Effort:** M (policy decision + 1-2 file changes).
- **Blast radius:** PIPELINE (silent empty-model handoff to caller; degrades EMSE reproducibility).
- **Why now:** policy-decision WP — Codex iteration tends to surface contract clarity issues during such WPs (as WP-CORE-3 did for empty-input). Could expose adjacent design gaps.
- **Risk:** decision-heavy (hard-fail vs degrade-best-effort vs retry-with-relaxed-constraints) — could blow scope.

### Option C: F-21 vacuous D1 verifier pass

- **Effort:** M (Architect prompt + parsing change + cross-stage data flow).
- **Blast radius:** PIPELINE (EMSE methodology validity — D1 deterministic check is dead code in current state).
- **Why now:** discovered fresh in WP-CORE-4 spec drafting; details preserved in `findings/architect.md` §F-21 while still cache-hot.
- **Risk:** changes the Architect prompt (D1-strict LLM behavior may shift unexpectedly under prompt change). Needs validation against a real SRS run, not just unit tests.

**Coordinator recommendation for iteration 4:** **Option A (F-11)**. Concurrency correctness is foundational — F-14 and F-21 fixes will both need to coexist with parallel Scout, so locking down the rate-limit primitive first reduces re-work surface. Also: F-11's blast radius (cost overrun on free tier) is the most directly observable to a paying user, while F-14/F-21 are EMSE-methodology concerns.

**Alternative if cycle pressure prefers smaller**: pick a MINOR cluster (F-12 + F-15 + F-17 + F-18 + F-19 + F-20-docs) as a "architect.py polish" WP — collectively ~S-M effort, low risk, kills 6 OPEN findings in one iteration. Trades depth for breadth.

---

## Loop ritual reminder for the next coordinator

1. Read CLAUDE.md, AGENTS.md (full, fresh).
2. `git log --oneline -8` + `git status --short`.
3. Read this handoff + `.planning/pipeline_audit/CURRENT.md`.
4. Confirm baseline:

   ```bash
   cd extension/backend
   /Library/Frameworks/Python.framework/Versions/3.13/bin/python3 -m pytest -m "not integration" --tb=no -q 2>&1 | tail -3
   ```
   Must be `332 passed, 31 deselected`. If not, STOP — baseline broken before you started.
5. Choose iteration target per §"Recommended next iteration" (F-11 recommended).
6. For F-11/F-14/F-21 (close-lookup already done in iteration 3): pick a finding from `findings/architect.md` and run spec → Codex xhigh review → plan → SDD execute → dev_doc → state update. No new audit walk required.
7. For a MINOR-cluster polish WP: pick a coherent set, write spec → Codex xhigh review (may waive if scope is purely cosmetic — document the waiver in decision_log) → plan → SDD → dev_doc.
8. Either way: each commit gated on pytest ≥ 332. No `--no-verify`. No silent fallbacks.

---

## Non-negotiables carried forward

- Baseline pytest never drops.
- Smallest correct change; no overengineering.
- Each WP has spec + Codex review + plan + dev_doc (Codex review may be waived for pure-cosmetic WPs; document in decision_log).
- Atomic Conventional Commits + Claude trailer.
- No `git push` without explicit user "push it."
- Caveman mode active (technical preserve, fluff drop) for terminal chatter; code/commits/PRs/security stay normal.
- Turkish ↔ user, English ↔ code/commits.
- RQ5 deleted per D6; do not reference.
- No backwards-compat shims; no silent fallbacks.
- New invariant from WP-CORE-4: any future stage retry wrapper that calls `_save_intermediate` MUST include `except IntermediateSaveError: raise` before any generic `except Exception`. Pattern lives in `identify_contexts:483-498`.

---

## Open follow-ups from iteration 3

- **F-21 newly discovered.** Vacuous D1 pass — D1 verifier has reported zero violations for every project run because Architect builds `ContextHypothesis(context_name=n, description=f"{n} context")` without populating `supporting_sentence_ids`. EMSE methodology paper should note this if WP-CORE-5 doesn't ship before paper-revision.
- **F-20 downgraded to MINOR.** Token tracker thread-safety is fine; the gap is a missing comment at the call site documenting that the inner `_lock` covers parallel Scout.
- **Anomaly: typed Scout dump missing in `scout_fn`.** `analyze_document`'s inline `scout_fn` (lines 735-752) does NOT call `_save_intermediate` — the legacy `extract_domain_sentences` was the only call site, and it's dead from production. Observability gap; not blocking.
- **No deferred WARNs this round.** All 7 Codex CRITICAL/WARN findings + 3 NITS + 7 OQ were handled inline in spec v2. Two consecutive zero-deferred iterations (WP-CORE-3 + WP-CORE-4) — the loop default is now: handle inline at spec time, not "accept with rationale for future WP."
- **Pattern established: per-test import for RED phase.** When the symbol being tested doesn't exist yet (collection-breaking), import it inside the test function. Pattern in `tests/test_intermediate_save.py`.
- **Pattern established: StreamingResponse drain via asyncio.run.** When testing FastAPI streaming endpoints synchronously, `response.body_iterator` is an async iterator; wrap consumption in `asyncio.run(_drain(it))`. Pattern in `tests/test_main_wiring.py:test_generate_model_stream_endpoint_forwards_joined_srs_path`.
- **Pattern reinforced: minimal valid DomainModel stub.** Pydantic requires `project_metadata` + non-empty `bounded_contexts` + each `BoundedContext.ubiquitous_language.entities` ≥ 1 entity with all required fields. Stub helper in `tests/test_architect_facade.py` is the canonical source; copied to two new test files this iteration.
- `extension/backend/core/intermediate/*.json`, `validation_metrics_report.json`, `domain/model.PRE-FRESH-RUN-BACKUP.json` remain untracked runtime artifacts; loop did not touch.
- `AGENTS.md` shows as `M` (auto-memory hook); leave alone.
- `core/AST/intermediate/ast_signals_diagnostics.json` shows as `M` (runtime artifact); leave alone.
- Local `.venv` still broken per CLAUDE.md; CI uses 3.12; loop runs 3.13 from `/Library/Frameworks/...`.

---

## Files this iteration produced

**Source/test:**
- `extension/backend/core/orchestration/errors.py` (modified, 02e0fe9 — new `IntermediateSaveError` class)
- `extension/backend/core/architect.py` (modified, 02e0fe9 — `_save_intermediate` rewrite + re-raise guard + `_current_srs_path` init + `analyze_document` signature widen)
- `extension/backend/main.py` (modified, 02e0fe9 — 3 callsite updates)
- `extension/backend/tests/test_intermediate_save.py` (created, 0023fa2 + minor fixes 02e0fe9)
- `extension/backend/tests/test_architect_srs_path.py` (created, 0023fa2 + minor fixes 02e0fe9)
- `extension/backend/tests/test_main_wiring.py` (modified, 0023fa2 + async-drain fix 02e0fe9 — T-WIRE-MAIN-2 + T-WIRE-MAIN-3 appended)

**Persistent dev memory:**
- `development_docs/WP-CORE-4-intermediate-save-observability.md` (created, next commit)
- `development_docs/INDEX.md` (modified, next commit — ACTIVE row #5)

**Audit state:**
- `.planning/pipeline_audit/CURRENT.md` (modified, next commit)
- `.planning/pipeline_audit/improvements_backlog.md` (modified, next commit — F-13 → SHIPPED; F-11/12/14/15/16/17/18/19/20/21 added under orchestrator section; F-20 downgraded; F-21 NEW)
- `.planning/pipeline_audit/decision_log.md` (already appended D-CL2, D-PICK-WP-CORE-4, D-CODEX-REVIEW-WP-CORE-4)
- `.planning/pipeline_audit/findings/architect.md` (created by Explore subagent earlier this iteration; F-21 + F-20 downgrade added during spec drafting)
- `.planning/pipeline_audit/component_catalog.md` (modified by Explore subagent — priority-2 row PENDING → DONE)
- `.planning/pipeline_audit/handoff-2026-05-21-1033.md` (this file, next commit)

**Planning artifacts:**
- `docs/superpowers/specs/2026-05-21-wp-core-4-intermediate-save-observability-design.md` (created, after-next commit)
- `docs/superpowers/plans/2026-05-21-wp-core-4-intermediate-save-observability.md` (created, after-next commit)

---

## Goal-backward verification (iteration-level)

| Iteration-3 goal | Evidence |
|---|---|
| Pick WP-CORE-4 = F-13 per iteration-2 handoff recommendation (priority 2 close-lookup) | Iteration shipped F-13 + anomaly fold-in. Audit dispatched Explore subagent for priority-2 (architect.py + orchestration) before pick. |
| Each commit gated on pytest ≥ baseline | RED `0023fa2` accepted red signal (9 failed + 323 passed = 332 collected); GREEN `02e0fe9` hit 332 passing; subsequent commits artifact-only. |
| Spec → Codex xhigh review → plan → SDD → dev_doc → state update | All 5 phases produced artifacts; commits 0023fa2, 02e0fe9, plus DOC + planning commits pending. |
| No `git push` | Confirmed; main remains local. |
| Atomic Conventional Commits + Claude trailer | Both iteration-3 commits conform. |
| Zero deferred WARNs (matching iteration-2's standard) | Confirmed; all 2 CRITICAL + 5 WARN + 3 NITS + 7 OQ handled inline in spec v2. |

Iteration 3 closed clean. Next coordinator: pick up after the DOC + planning commits land, re-enter via ritual above.
