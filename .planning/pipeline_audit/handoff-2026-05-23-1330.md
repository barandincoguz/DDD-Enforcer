# Handoff — Domain Pipeline Hardening Loop (iteration 7 → 8)

**Written:** 2026-05-23 13:30 GMT+3
**Reason:** Iteration 7 SHIPPED — WP-CORE-8 (F-23 typed PipelineError handler in main.py) closed end-to-end. Autonomous loop continues.

## State of the loop

### Baseline (sacred)

```
pytest -m "not integration" → 365 passed, 31 deselected
Pre-WP-CORE-8 baseline was 358. Net +7 tests, zero regression.
Cumulative: pre-loop 272 → post-iteration-7 365 (+93 tests over 7 iterations).
```

### HEAD at handoff

```
{pending}  chore(planning): WP-CORE-8 spec v2 + plan into git history
{this}     chore(artifacts): WP-CORE-8 dev_doc + audit state update + F-23 SHIPPED
a2bca34    fix(main): WP-CORE-8 typed PipelineError response handler
72898af    test(main): WP-CORE-8 red-phase tests for typed PipelineError response
cecfee1    chore(planning): WP-CORE-7 spec v2 + plan ← pre-iteration-7 HEAD
```

4 commits on main during iteration 7. **NOT pushed.**

### Iteration 7 — what happened

1. F-23 picked per WP-CORE-7 Codex W-6 lineage. Small scope (`/generate-model` + `/generate-model-stream` only).
2. Spec v1 → Codex xhigh review: 0 CRITICAL + 4 WARN + 3 NIT + 1 OQ.
3. Spec v2 incorporated all WARN inline (SpecialistShapeError attrs added, T-SSE-1 added, T-HELPER-4 strengthened, TS wire-compat claim reframed). OQ-5 (lifespan path) deferred with revisit trigger.
4. RED `72898af` (7 RED-by-design failures: 5 ImportError, 2 KeyError; all at body execution).
5. GREEN `a2bca34` (365 passing, zero regression).

### WP-CORE-8 — SHIPPED (F-23)

- Spec v2: `docs/superpowers/specs/2026-05-23-wp-core-8-typed-pipeline-error-handler-design.md`
- Plan: `docs/superpowers/plans/2026-05-23-wp-core-8-typed-pipeline-error-handler.md` (pending PLANNING commit)
- Dev doc: `development_docs/WP-CORE-8-typed-pipeline-error-handler.md`
- Production code:
  - `main.py` — `_issue_to_dict`, `_scalarize`, `_build_pipeline_error_response` helpers; `PipelineError` import; typed catches in 2 endpoints; SSE event_generator dict-spread adapter
- Tests: 7 new (T-HELPER-1..5, T-ENDPOINT-1, T-SSE-1) across 2 NEW files.
- Net diff: +96/-1 main.py; +350 in 2 new test files.

## Backlog state

### Ingestion layer (10 findings — unchanged)

| status | finding | severity |
|---|---|---|
| OPEN | F-1 | MAJOR (read_pdf defensive) |
| OPEN | F-2 | MAJOR (read_txt cp1254) |
| SHIPPED | F-3 | empty-input contract |
| OPEN | F-4 | MAJOR-uncertain (TOC heuristic) |
| SHIPPED | F-5 | reference truncation |
| OPEN | F-6 / F-7 / F-8 / F-9 | MINOR |
| OPEN | F-10 | TRIVIAL |

### Orchestrator layer (14 findings)

| status | finding |
|---|---|
| SHIPPED | F-13, F-14, F-21, F-22, F-23 (5 total) |
| OPEN-DORMANT | F-11 |
| OPEN-MINOR | F-12, F-15 (downgraded TRIVIAL), F-17, F-18, F-19, F-20-downgraded, F-24-NEW |
| OPEN-TRIVIAL | F-16 |

**Total OPEN MAJOR (live): 3** (F-1, F-2, F-4-uncertain) + 1 DORMANT (F-11). **Orchestrator MAJOR-live count is now ZERO** for first time since audit began.

## Recommended next iteration (iteration 8)

### Option A (RECOMMENDED): F-2 cp1254 silent binary garbage

- **Effort:** S-M. `core/document_parser_readers.py:read_txt` defaults to cp1254 encoding. On UTF-8 files containing multibyte sequences, cp1254 silently produces garbage. Fix: detect via chardet OR magic bytes; on cp1254-decode-garbage signal raise `EncodingError(ValueError)`. Tested via real-world UTF-8 SRS fixture (Turkish + English mixed).
- **Blast radius:** PIPELINE. Affects ingestion stage first-line.
- **Why now:** smallest correct change among ingestion MAJORs; pivots audit lens after 5 orchestrator iterations; restores forward momentum on ingestion-layer audit.

### Option B: F-1 PDF defensive handling

- **Effort:** M. `read_pdf` has no defensive handling for corrupted / password-protected / empty PDFs (post-F-3 only catches empty text). Add typed exceptions: `CorruptedPDFError`, `EncryptedPDFError`, `EmptyPDFError(EmptySRSDocumentError)`.
- **Blast radius:** PIPELINE. Surface is /generate-model first-call failure.

### Option C: F-4 TOC heuristic 120-line + cluster<2

- **Effort:** M. `_truncate_at_toc` heuristic uses 120-line scan + cluster<2 check. Edge cases: malformed SRS with no TOC vs SRS with short fragmented TOC. Audit text marks "MAJOR-uncertain" — confirm reachability first.

### Option D: F-24 srs_path in VerifierIssue

- **Effort:** M (13-callsite migration). Adds run-manifest issue-level provenance. Lower priority than ingestion MAJORs.

**Coordinator recommendation for iteration 8:** **Option A (F-2)**. Smallest scope; closes silent-failure path in ingestion; pivot refresh.

## Loop ritual reminder

1. Read CLAUDE.md, AGENTS.md (full, fresh).
2. `git log --oneline -8` + `git status --short`.
3. Read this handoff + `.planning/pipeline_audit/CURRENT.md`.
4. Confirm baseline: `pytest -m "not integration"` must be `365 passed, 31 deselected`.
5. Choose iteration target. **Production reachability subsection mandatory.**
6. Each commit gated on pytest ≥ 365.

## Non-negotiables carried forward

- All prior invariants (WP-CORE-4 / 5b / 6 / 7) intact.
- **WP-CORE-8 NEW invariant**: every new `PipelineError` subclass under `core/orchestration/errors.py` SHOULD have its payload attributes added to `main._build_pipeline_error_response` scalar-attrs or list-attrs tuple, so the response retains typed taxonomy. The duck-typing fallback covers gracefully but loses typed structure.
- Baseline pytest never drops.
- Smallest correct change; no overengineering.
- Each WP has spec + Codex review + plan + dev_doc.
- Atomic Conventional Commits + Claude trailer.
- No `git push` without explicit user "push it."
- Caveman mode active for chatter; code/commits/security stay normal.

## Open follow-ups from iteration 7

- **F-23 SHIPPED**. Orchestrator MAJOR-live count = 0.
- **OQ-5 (lifespan typed handling)**: out-of-scope follow-up; revisit trigger is "startup auto-generation becomes EMSE run evidence".
- **TS handler upgrade**: `extension.ts:680-687` currently swallows SSE error events. Python payload now correct. TS fix is future work.
- **Audit pivot opportunity**: 5 consecutive orchestrator iterations. Pivot to ingestion (F-1 or F-2) recommended.

## Goal-backward verification (iteration-level)

| Iteration-7 goal | Evidence |
|---|---|
| Pick F-23 per WP-CORE-7 Codex W-6 lineage | F-23 picked; LIVE-in-production verified by Discovery D-1. |
| Spec → Codex xhigh review → plan → SDD → dev_doc → state update | All 4 commits clean: RED `72898af` → GREEN `a2bca34` → DOC `{this}` → PLANNING `{pending}`. |
| Each commit gated on pytest ≥ baseline | RED: 358 + 7 fail; GREEN: 365 pass, 0 regression. |
| Codex CRITICAL+WARN inline | YES — 0 CRITICAL + 4 WARN all inline (W-1 SpecialistShapeError attrs, W-2 T-SSE-1, W-3 TS reframe, W-4 round-trip strengthen). |
| Codex NIT confirmed | YES — N-1, N-2, N-3. |
| Codex OQ tracked | YES — OQ-5 (lifespan) deferred with revisit trigger. |
| Production reachability in spec | YES — §Motivation includes "Production reachability (loop discipline)" subsection. |
| EMSE Methods claim updated | YES — "typed pipeline errors surface through API" now true at both sync + SSE endpoints. |

Iteration 7 closed clean. Iteration 8 target: F-2 (cp1254 silent garbage) recommended.
