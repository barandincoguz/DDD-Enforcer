# Development Docs Index

> **Purpose**: Persistent design + implementation memory for the DDD-Enforcer EMSE submission. Two use cases drive what goes here:
>
> 1. **Paper revision substrate** — when revising the paper, every empirical claim or architectural decision should trace to a doc here that explains *why* it was made.
> 2. **Context recovery** — if a Claude session ends and a new one starts, reading the relevant docs here brings the new session up to speed without re-deriving decisions.

## Conventions

- **One file per Work Package** (WP) or per major architectural change. WPs follow the naming scheme in `todos/MASTER_PLAN.md`.
- **Filename**: `WP-<code>-<short-kebab>.md` (e.g. `WP-NEW-B-Stage-1-schema-probe.md`) or `arch-<topic>.md` for cross-cutting architecture notes.
- **Every doc leads with**: status, branch / commit SHAs, spec + plan paths, one-paragraph TL;DR.
- **Sections**: Motivation • Architectural decisions • File-level changes • Methodology applied • Empirical results (if any) • Limitations + follow-ups • Cross-references.
- **Cross-link** using `[[doc-name]]` syntax (no markdown link required — search-friendly).
- **Don't put commit messages here verbatim** — the git log is authoritative. Docs explain *why* and *how the pieces fit together*; commits explain *what changed line-by-line*.

## Active (shipped or in-flight, full docs)

| # | Doc | WP | Status | Headline |
|---|---|---|---|---|
| 1 | [WP-NEW-B Stage 1 — schema_probe real run](WP-NEW-B-Stage-1-schema-probe.md) | WP-NEW-B | SHIPPED 2026-05-19 | 6×3 conformance probe shipped real paper data; closed Gemini perfect (30/30), OSS-via-Ollama strict-schema near-zero (1/60) |
| 2 | [WP-CORE-1 — Typed pipeline + deterministic Synthesizer](WP-CORE-1-typed-pipeline.md) | WP-CORE-1 | SHIPPED 2026-05-20 | Pydantic typed contracts at every stage boundary + LLM-rewrite Synthesizer → deterministic merge + per-context narrow enrich + D6/D7/D8 invariants. Fixed live FM-CRASH; pipeline now runs E2E on D1 SRS (4 contexts × 7 D1-strict entities × 6 VOs × 6 aggregates). |
| 3 | [WP-CORE-2 — Reference-heading truncation correctness](WP-CORE-2-reference-truncate-fix.md) | WP-CORE-2 | SHIPPED 2026-05-21 | Regex locale gap (`Kaynaklar` missing) + mid-doc false-positive fixed via alternation expansion + optional trailing colon + position guard (`REFERENCE_HEADING_MIN_DOCUMENT_FRACTION = 0.5`). Baseline 272 → 305 (+33 tests, zero regression). |
| 4 | [WP-CORE-3 — Empty-input contract](WP-CORE-3-empty-input-contract.md) | WP-CORE-3 | SHIPPED 2026-05-21 | `EmptySRSDocumentError(ValueError)` raised by `parse_file`; six duplicated guards in `main.py` retired via per-path policy (HARD propagate / SOFT skip+log / MIXED batch via new `_parse_srs_batch` helper). Latent broken post-loop guard folded in. Baseline 305 → 321 (+16 tests). |
| 5 | [WP-CORE-4 — Intermediate-save observability](WP-CORE-4-intermediate-save-observability.md) | WP-CORE-4 | SHIPPED 2026-05-21 | `IntermediateSaveError(PipelineError)` raised by `_save_intermediate` (4 callsites); `identify_contexts` re-raise guard prevents silent rewrap into `ArchitectExtractionError`. `_current_srs_path` anomaly fold-in: `analyze_document(srs_path=…)` signature + unconditional reassignment + 3 main.py callsites + error-message binding. Baseline 321 → 332 (+11 tests). First orchestrator-layer iteration after two ingestion-layer wins. |
| 6 | [WP-CORE-5b — Synthesizer empty-model policy](WP-CORE-5b-synthesizer-empty-model-policy.md) | WP-CORE-5b | SHIPPED 2026-05-21 | F-14 taxonomy fix: pre-call guard at `pipeline.py` raises `SynthesizerEmptyModelError(PipelineError)` when `refined_specialist == []` (closes both initial-empty + refiner-shrink-success edges per Codex W-1). Post-call boundary check retained as belt-and-suspenders for injected synthesizers that bypass Pydantic via `model_construct` (Codex W-3). `srs_path` added to error per WP-CORE-4 symmetry. Pre-WP behavior: `pydantic.ValidationError` escaped `PipelineError` taxonomy. Production-dormant (Architect upstream guard intact); fix is contract cleanup for paper-methodology integrity. Baseline 332 → 338 (+6 tests). Third consecutive zero-deferred Codex review. Predecessor WP-CORE-5 (F-11 parallel Scout race) ABANDONED at v1 after Codex review surfaced 3 CRITICALs — F-11 dormant in current production code. |
| 7 | [WP-CORE-6 — D1 verifier non-vacuous](WP-CORE-6-d1-verifier-non-vacuous.md) | WP-CORE-6 | SHIPPED 2026-05-21 | F-21 (LIVE in production, every run): Architect's `identify_contexts` left `ContextHypothesis.supporting_sentence_ids` empty → D1 verifier passed vacuously forever → final `DomainModel.bounded_contexts[].supporting_sentence_ids` always `[]`. WP-CORE-6 closes the loop end-to-end: prompt rewrite (numbered sentences + object array with name + supporting_sentence_ids); strict-shape parser (rejects bare-string dict + top-level list per Codex W-2); line-pair-aware truncation (`_truncate_numbered_pairs` per Codex W-1, never chops `[N]` mid-prefix); `extract_per_context_details` signature widened from `List[str]` to `List[ContextHypothesis]` (Codex C-1 — closes Specialist rebuild ID-loss); D1 also flags empty IDs as ungrounded_context ERROR (honest signal, not enforcement); degrade-log emits full `exc.issues` list. Baseline 338 → 348 (+10 tests, zero regression). Codex xhigh: 4 CRITICAL + 4 WARN + 6 NIT + 1 OQ (A6-srs-path deferred with concrete revisit trigger — 4-iteration zero-deferred streak ends by design). NEW F-22 backlog (Refiner cannot re-run Architect) tracks full enforcement; queued as iteration-6 target. |
| 8 | [WP-CORE-7 — Refiner stage-aware re-runs + ArchitectGroundingError](WP-CORE-7-refiner-stage-aware.md) | WP-CORE-7 | SHIPPED 2026-05-23 | F-22 (LIVE; closes WP-CORE-6 D-3 half-fix): Refiner becomes stage-aware via `_issue_stage` helper deriving stage from `target` prefix (Codex C-1, avoids 13-callsite VerifierIssue schema widen). Mode C hybrid: 1 architect re-run with issue-aware feedback prompt (`PREVIOUS ATTEMPT FAILED VERIFICATION:` + per-issue lines) then hard-fail via new `ArchitectGroundingError(PipelineError)` carrying srs_path + issues + residual_issues (Codex W-4) + cycles_attempted. Pre-check verifier ONCE before specialist refine loop; thread result via `refine_until_clean(initial_result=...)` (Codex C-2, avoids double-verify on common path). Narrowed bare `except Exception` to `except RefinementExhaustedError` only (Codex W-5, explicit-failure mandate). `PipelineDeps.architect_with_feedback` field added; 3 fixture sites updated in lockstep. Baseline 348 → 358 (+10 tests, zero regression). Codex xhigh: 2 CRITICAL + 6 WARN + 2 NIT + 1 OQ; all CRITICAL+WARN inline; 2 NIT inlined; 1 OQ (A6-srs-path) → F-24 backlog (post-F-22 trigger fired). NEW: F-23 (typed PipelineError catch in main.py) + F-24 (srs_path in VerifierIssue) backlog entries. EMSE Methods claim "D1 enforces grounding" now true at both verifier and pipeline levels. |
| 9 | [WP-CORE-8 — Typed PipelineError handler in main.py](WP-CORE-8-typed-pipeline-error-handler.md) | WP-CORE-8 | SHIPPED 2026-05-23 | F-23 (LIVE; closes WP-CORE-7 Codex W-6 follow-up): main.py had 10 bare-Exception handlers, no PipelineError import; ArchitectGroundingError + SynthesizerEmptyModelError + IntermediateSaveError + SpecialistShapeError all collapsed to `{success:false, error: str(e)}` at FastAPI response boundary. WP-CORE-8 adds `_build_pipeline_error_response` + `_issue_to_dict` + `_scalarize` helpers preserving typed payload attrs (srs_path, issues, residual_issues, cycles_attempted, validation_errors, raw_excerpt, etc.); typed `except PipelineError` BEFORE bare-Exception fallback in `/generate-model` (line 427) + `/generate-model-stream` thread (line 533); SSE event_generator dict-spread keeps `event.error` as string for VSCode extension compat at `extension.ts:683` while typed siblings (error_type, srs_path, ...) ride along additively. Bare-Exception fallback retained per AGENTS.md "explicit failure" (non-pipeline bugs still surface separately). Codex xhigh: 0 CRITICAL + 4 WARN + 3 NIT + 1 OQ; all WARN inline (W-1 SpecialistShapeError attrs, W-2 T-SSE-1 added, W-3 TS-handler reframe, W-4 round-trip assertions strengthened); 1 OQ-5 (lifespan path) deferred with revisit trigger. Baseline 358 → 365 (+7 tests, zero regression). EMSE Methods claim "typed pipeline errors surface through API" now true at both sync and SSE endpoints. |
| 10 | [WP-CORE-9 — Mislabeled-file detection in read_txt](WP-CORE-9-mislabeled-file-detection.md) | WP-CORE-9 | SHIPPED 2026-05-23 | F-2 (LIVE; ingestion-layer pivot after 5 consecutive orchestrator iterations): `read_txt` cp1254 single-byte fallback could silently decode renamed binary files (`.docx`/`.pdf` saved as `.txt`) into gibberish on no-NUL printable content; common-case raised opaque `UnicodeDecodeError`. WP-CORE-9 adds `MisLabeledFileError(ValueError)` (file_path + detected_format attrs; readable message) + `_BINARY_MAGIC_SIGNATURES` tuple constant (10 signatures: 3 ZIP variants per Codex W-1, PDF, OLE compound, PNG, JPEG, 2 GIF variants, gzip) + `_detect_binary_signature` helper; signature check inserted BEFORE the encoding-decode loop in `read_txt`. Re-exported from `core.document_parser` for clean import path (Codex OQ; mirrors EmptySRSDocumentError). Dual benefit per Codex W-6: rare silent-accept case caught at byte-0 signature + common-case diagnostics improved from opaque UnicodeDecodeError to typed format-aware error. Scope `.txt`-only per Codex W-8 (F-1/F-7 cover read_pdf/read_docx symmetric defenses as future WPs). Codex xhigh: 1 CRITICAL + 8 WARN + 4 NIT + 1 OQ; all CRITICAL+WARN inline (C-1 T-MFE-5 reclassified GREEN-from-start; W-1 ZIP variant signatures; W-2 fixtures expanded no-NUL printable; W-3 BOM+later-substring test; W-4 near-miss helper test; W-5 VSCode reachability; W-6 dual-benefit reframe; W-7 _parse_srs_batch routing; W-8 .txt-only non-goal); 4 NIT inlined; OQ resolved with re-export. Baseline 365 → 373 (+8 tests, zero regression). |
| 15 | [WP-CORE-14 — Remove synthetic context descriptions](WP-CORE-14-synthetic-context-descriptions.md) | WP-CORE-14 | SHIPPED 2026-05-23 | F-18: architect_fn + merge.py both emitted f"{name} context" placeholder descriptions; intermediate JSON mismatched final DomainModel after enrich. Removed both; description flows empty when LLM-derived value absent; schema permits Field(default=""). Baseline 403 → 404 (+1 test). |
| 14 | [WP-CORE-13 — srs_path in VerifierIssue](WP-CORE-13-srs-path-in-verifier-issue.md) | WP-CORE-13 | SHIPPED 2026-05-23 | F-24 (MINOR; WP-CORE-6 A6-srs-path follow-up): optional srs_path field added to BOTH VerifierIssue classes (legacy dataclass + contract Pydantic). _to_contract_issue adapter propagates srs_path; check_d1 helper accepts optional kwarg. D1-only opt-in for v1; other checks opt in as updated. Default None preserves back-compat for 13 existing callsites. Baseline 397 → 403 (+6 tests). Completes srs_path threading sweep across orchestration error taxonomy. |
| 13 | [WP-CORE-12 — TOC heuristic layout-mode detection](WP-CORE-12-toc-heuristic-layout-mode.md) | WP-CORE-12 | SHIPPED 2026-05-23 | F-4 (MAJOR-uncertain → MAJOR-LIVE confirmed): toc_line_pattern required \\.{4,} dot leaders; pypdf layout-mode produces whitespace separators which `_normalize_line` collapses to `" | "`. Regex broadened to alternation: dot-leader | pipe-separator | raw-whitespace. Cluster<2 + 120-line window guards retained. Baseline 394 → 397 (+3 tests). Ingestion-layer MAJOR backlog now zero live. |
| 12 | [WP-CORE-11 — read_docx defensive handling](WP-CORE-11-docx-defensive-handling.md) | WP-CORE-11 | SHIPPED 2026-05-23 | F-7 (LIVE; ingestion-reader trilogy complete): `read_docx` was 14 LOC zero-defense; WP-CORE-11 adds `CorruptedDOCXError` + `EmptyDOCXError`; re-uses WP-CORE-9 `_detect_binary_signature` for symmetric `MisLabeledFileError`. Baseline 388 → 394 (+6 tests). |
| 11 | [WP-CORE-10 — read_pdf defensive handling](WP-CORE-10-pdf-defensive-handling.md) | WP-CORE-10 | SHIPPED 2026-05-23 | F-1 (LIVE; ingestion-layer continuation, symmetric to WP-CORE-9 for PDF reader): `read_pdf` was 3 LOC zero-defense. WP-CORE-10 adds `EncryptedPDFError` + `CorruptedPDFError` + `EmptyPDFError` (all `ValueError`); header-only magic-byte check re-uses `_detect_binary_signature` for `MisLabeledFileError` on non-`%PDF-` byte-0. Codex C-1: wraps constructor + lazy `len(reader.pages)` + `page.extract_text` failures under one `except PdfReadError` (PdfStreamError subclass per T-PDF-INHERIT). Codex W-3: header-only `open + read(_MAGIC_HEADER_BYTES)` avoiding multi-GB read_bytes(). Codex W-1: `__cause__` chain preserved. Codex W-5: strict byte-0 policy. Codex W-6: flat ValueError taxonomy. Re-exported from `core.document_parser`. Codex xhigh: 2 CRITICAL + 6 WARN + 1 NIT + 1 OQ; all CRITICAL+WARN inline. Baseline 373 → 388 (+15 tests, zero regression). |
| 16 | [WP-CORE-20 — EMSE-grade structured logging](WP-CORE-20-emse-grade-logging.md) | WP-CORE-20 | SHIPPED 2026-05-23 | F-9 (MINOR; large-scope cross-cutting): new `core/observability/` module — single-Pydantic-model `RunManifest`, per-run `StageEmitter` (ContextVar-backed), atomic JSON writer, `scripts/aggregate_runs.py` for N=10 pooled rates + per-run CSV + distributions JSON. Endpoint-entry manifest creation (Codex C-3) captures `no_input_files` / `srs_parse_failed` / `all_srs_empty`; LLM instrumentation at LLMResponse construction (Codex W-1, GeminiClient + OllamaClient); parallel-Scout `contextvars.copy_context()` propagation (Codex C-1); finalize-safely never masks original PipelineError (Codex W-2); pooled rates not mean-of-ratios (Codex W-3); inputs not mutated, SHA-256 fingerprinted (Codex W-4); SemVer schema with `min_supported_schema` gate (Codex W-5); single Pydantic source-of-truth eliminates writer-reader drift (Codex W-6); atomic tmp+fsync+replace write (Codex W-7); `monotonic_clock_source` + `instrumentation_overhead_ms` transparency fields (Codex W-8); `_ISSUE_TYPE_TO_CHECK_ID` mapping canonicalizes legacy `issue_type` and contract `check_id` to D1..D8/S1 (Codex C-5); `record_json_parse_failure` API for caller-side `_parse_json_response` paths (Codex C-4). Codex xhigh: 5 CRITICAL + 8 WARN + 1 OQ; all inline; OQ-1 resolved (`/validate` explicitly out of scope, WP-CORE-22 tracker). Baseline 404 → 459 (+55 tests, zero regression). EMSE Methods section claims now defensible: pooled p50/p95 latency per stage, `json_failed` rate per D1 6-model registry, refinement cycles, architect grounding hard-fail rate, cost per run/stage/model, D1–D8/S1 verifier check fire rate, specialist degrade rate, instrumentation overhead. Follow-up WP-CORE-20a: instrument Architect/Scout/Specialist manual-parse retry callsites with `record_json_parse_failure`. |

## Reserved (prior-session work; backfill on demand)

These WPs shipped in earlier sessions (before this folder existed). Full docs would require backfilling from git history. Pointers below cover what is needed for paper revision until full docs land.

| WP | Commit range | One-line summary | Backfill priority |
|---|---|---|---|
| **WP-00 Phase 0 hygiene** | `4a893c8`–`56919da` (4 commits, ≈2026-05-07) | Python 3.12 pin, `requirements.lock` hash-pinned, CI strictening to coverage 60% + pyright continue-on-error | Low — `git log` and `.github/workflows/backend-ci.yml` are self-documenting |
| **Planning docs** | `f5be4dc` and adjacents (≈2026-05-08) | `CLAUDE.md`, `todos/AGENT_QUICKSTART.md`, `todos/MASTER_PLAN.md`, `todos/WP_DAGILIM_BARAN_ALI.md` introduced | Low — the files in `todos/` ARE the documentation |
| **P3 Verifier+Refiner refactor** | `0bc10ec`–`f0da5d5` (25 commits, 2026-05-18→05-19) | 4-stage → 5-stage pipeline (Scout / Architect / Specialist / **Verifier** / **Refiner** / Synthesizer); FM-01/02/04/05/06/07/16/20/21 fixed; silent fallback removal + section-aware chunking + evidence-citation tightening (OQ1/OQ2/OQ3) | **Medium** — paper RQ1 leans on this; backfill before RQ1 write-up |
| **WP-01a Provider abstraction** | `b627505`–`e380983` (9 commits + 1 doc fix, 2026-05-19) | `core/llm/` package: 8 modules (base+errors+registry+retry+gemini+ollama+validator+_response_adapter+schema_probe). 6-model D1 registry. Retry+rotation decorator. `core/llm_client.py` → `core/llm/validator.py`. | **High** — paper Methods section references this; backfill before Methods write-up |

## Pending (not yet started)

| WP | Trigger / dependency | Notes |
|---|---|---|
| **WP-NEW-A AST drift injector** | RQ4 + Yaklaşım F unblock; ~3-5 days estimate | `scripts/inject_drift.py` V1-V6 quota CLI |
| **WP-NEW-B Stage 2** paper-side render | Stage 1 artifact (shipped) | Markdown table generator for `runs/probe-*.json` → RQ2 Table 7 appendix |
| **WP-NEW-C** prompt sensitivity ablation | Stage 1 findings — OSS conformance is bimodal | 3 prompt variants per pipeline, mean±std reporting |
| **WP-02** D2/D3 codebase sourcing | Public SRS + CLEAN codebases for banking + healthcare domains | Required before RQ3 cross-domain runs |

## How to use this folder going forward

- **Before starting a new WP**: spec it under `docs/superpowers/specs/`, plan it under `docs/superpowers/plans/`, *implement*, then write a doc here when it ships.
- **Inside a session**: if a doc here is touched (status change, new follow-up, empirical update), update the row in this INDEX too.
- **When paper-revising**: read the doc(s) for the WPs that produced the data or claim under revision; never recompute from git log without the doc.
- **Context-recovery primer**: read INDEX.md → most-recent doc → spec + plan → `git log --oneline main..main~10`. That sequence reconstructs ≈80% of a recent session's context in under 5 minutes.
